# 本地权重迁移 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或
> superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让同一份权重文件全机只下载一次、只占一份盘——档案页扫描出本地已有的文件，
下载前弹确认框由用户逐行选择「下载 / 移动 / 链接 / 复制」。

**架构：** 判定逻辑下沉 `src/lib/` 纯函数；执行层并入现有下载队列（`source = "local"`），
返回与 `startDownload` 同形状的 `DownloadHandle`，队列编排一行不动；路径映射由面板查自身容器
挂载表自动发现，用户按宿主机视角填自定义目录。

**技术栈：** Next.js App Router · better-sqlite3 · zod · vitest（node 环境，无 jsdom）·
shadcn/ui · next-intl（zh/en 必须对称）

**设计文档：** `docs/superpowers/specs/2026-09-03-本地权重迁移-design.md`

---

## 批次与依赖

| 批 | 内容 | 依赖 | 交付后可用性 |
|---|---|---|---|
| ① 底层 | 匹配判定、v17 迁移、inode 去重、挂载表发现、file_meta union | — | 无 UI 变化，但占盘统计修正立即生效 |
| ② 执行层 | local 执行器三条路径、队列接线、acquire API | ① | API 可用，无 UI |
| ③ 档案页 | scan API、确认弹层、stray 放开 | ①② | **第一个完整闭环**：档案页可用本地文件代替下载 |
| ④ 游离视图 | picker 徽标、`/files` 未登记视图 | ① | 独立可交付，可延后 |

批 ①②③ 构成第一个可交付闭环；批 ④ 只依赖批 ①，可与 ②③ 并行或延后。

## 文件结构

**新建**

| 文件 | 职责 |
|---|---|
| `src/lib/acquire-match.ts` | L1 匹配与动作矩阵判定（纯函数，零 IO） |
| `src/lib/acquire-match.test.ts` | 上者的测试 |
| `src/lib/acquire-plan.ts` | 确认弹层的行状态机（纯函数） |
| `src/lib/acquire-plan.test.ts` | 上者的测试 |
| `src/lib/unclaimed-view.ts` | 游离清单派生（refs===0 + 两标签 + inode 共用） |
| `src/lib/unclaimed-view.test.ts` | 上者的测试 |
| `src/server/download/localAcquire.ts` | local 执行器：校验 + move/link/copy，返回 `DownloadHandle` |
| `src/server/download/localAcquire.test.ts` | 上者的测试（真实临时目录） |
| `src/server/mounts.ts` | 自身容器挂载表 → 可用导入源路径（排除运行必需的三项） |
| `src/server/mounts.test.ts` | 上者的测试 |
| `src/app/api/v1/repos/[id]/scan/route.ts` | 深度扫描，返回候选，不入库 |
| `src/app/api/v1/repos/[id]/acquire/route.ts` | 统一提交入口（替换 `download` 路由） |
| `src/app/api/v1/files/unclaimed/route.ts` | 全库游离清单 |
| `src/app/api/v1/paths/mounts/route.ts` | 可用导入源路径清单（填自定义目录时的提示与校验） |
| `src/app/(panel)/models/repos/[id]/acquire-dialog.tsx` | 确认弹层组件 |
| `src/app/(panel)/files/unclaimed-table.tsx` | 未登记视图表格 |

**修改**

| 文件 | 改动 |
|---|---|
| `src/server/migrations.ts` | 追加 v17（4 个 ALTER） |
| `src/server/fsScanner.ts:52` | `ModelFile` 加 `ino` 字段 |
| `src/server/repoProfiles.ts:114` | `decorateProfileStats` 的 `bytes` 按 inode 去重 |
| `src/server/pathMaps.ts:11` | `getPathMaps` 并入挂载表发现结果 |
| `src/server/fileMeta.ts:230` | `listFileMeta` 口径扩成 union（配置引用 ∪ 游离文件） |
| `src/server/download/manager.ts:161,320,424` | `TaskRow` 加两字段、`buildRequest` 分支、执行器选择分支、`enqueueLocal` |
| `src/lib/repo-files-scan.ts:63` | 放开「排除所有档案目录」，改为标注所在档案 |
| `src/lib/repo-files-view.ts:131,273` | 逐分片记 stray、`isSelectable` 放行 stray |
| `src/lib/model-file-picker.ts` `src/components/models/model-file-picker.tsx:174` | `refs === 0` 时渲染「未被使用」徽标 |
| `src/app/(panel)/models/repos/[id]/repo-detail-view.tsx:309` | 扫描按钮、确认弹层接线 |
| `src/app/(panel)/files/page.tsx:143` | 二级栏第三格「未登记」 |
| `src/i18n/messages/{zh,en}.json` | 新增文案（两侧必须对称） |
| `src/app/api/v1/repos/[id]/download/route.ts` | **删除**（迁移到 `acquire`） |

## 通用约定

- **每个任务结束跑四件套**：`pnpm test` / `pnpm run lint` / `pnpm exec tsc --noEmit` / `pnpm run build`，
  全绿才 commit
- **i18n 对称**：任何新增文案必须同时改 `zh.json` 与 `en.json`，键路径逐字一致
- **不写 AI 标识**：commit message 与代码注释里不得出现 `Co-Authored-By`、模型名、版本标识注释
- **测试环境**：vitest 是 node 环境，**没有 jsdom**，组件测不了。所有可测判定必须下沉到
  `src/lib/*.ts`，组件只靠 `tsc --noEmit` + `next build` 守
- **git**：`git add` 与 `git commit` 必须分两条命令执行（避免 index.lock）
- **基线代码**：本计划按 `7f82cb9`（2026-09-03）复核过。写计划到开工之间，另一个会话
  改动了档案详情页与量化识别，实现前先记住两条：
  - **档案页已从表格行改成卡片网格**（`QuantCard`，`repeat(auto-fill,minmax(232px,1fr))`）。
    计划里凡说「行」的地方，落到 UI 上都是**一张卡**：一张卡 = 一个量化组，与「动作按组选」
    天然对齐，不必改结构。卡内分四段：文件名（`line-clamp-2` + Tooltip）／徽章行
    （量化 + mmproj + 分片数 + 右对齐大小）／`StateCell`／操作段（创建配置 + 归位 +
    `StrayMark`）。新增元素往哪段放，见任务 11、15 的落点说明
  - **量化正则已修**（`f2ed5eb`，`core/files.ts`）：`Q4_K_XL` / `Q6_K_XL` 这类两字母后缀
    不再被截断成 `Q4_K` 而与真的 `Q4_K` 撞成一组。分组键因此可信，测试夹具可以放心用
    unsloth 的 UD 量化名

---

# 批 ① 底层

## 任务 1：匹配判定与动作矩阵

**文件：**
- 创建：`src/lib/acquire-match.ts`
- 测试：`src/lib/acquire-match.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { actionsFor, matchLocalCandidate, mergeGroupMatch, type FileMatch, type LocalCandidate } from "./acquire-match";

const remote = { path: "Q4_K_M.gguf", size: 2600, oid: "a".repeat(64) };

function cand(over: Partial<LocalCandidate> = {}): LocalCandidate {
  return {
    absPath: "/host-models/loose/Q4_K_M.gguf",
    rel: "loose/Q4_K_M.gguf",
    size: 2600,
    fullSha256: null,
    inRepoDir: null,
    inModelsRoot: true,
    ...over,
  };
}

describe("matchLocalCandidate", () => {
  it("同名同大小命中", () => {
    expect(matchLocalCandidate(remote, [cand()])).not.toBeNull();
  });

  it("大小不同不命中——同名但下坏了的半成品不该被当成同一个文件", () => {
    expect(matchLocalCandidate(remote, [cand({ size: 999 })])).toBeNull();
  });

  it("改过名但缓存哈希等于远端 oid 时命中——跨仓库同一份文件的唯一判据", () => {
    const renamed = cand({
      absPath: "/host-models/hf/other/Model.Q4_K_M.gguf",
      rel: "hf/other/Model.Q4_K_M.gguf",
      fullSha256: "a".repeat(64),
    });
    expect(matchLocalCandidate(remote, [renamed])?.rel).toBe("hf/other/Model.Q4_K_M.gguf");
  });

  it("改过名且无缓存哈希时不命中——不能凭大小认领", () => {
    const renamed = cand({ absPath: "/host-models/x/Other.gguf", rel: "x/Other.gguf" });
    expect(matchLocalCandidate(remote, [renamed])).toBeNull();
  });

  it("远端 size 为 0 或缺失时一律不命中", () => {
    expect(matchLocalCandidate({ path: "a.gguf", size: 0 }, [cand({ size: 0 })])).toBeNull();
  });
});

describe("mergeGroupMatch", () => {
  const fm = (over: Partial<FileMatch> = {}): FileMatch => ({
    file: "m.gguf", candidate: cand(), actions: ["download", "move", "link"],
    defaultAction: "move", restriction: "none", ...over,
  });

  it("组内动作一致时组级取该动作", () => {
    const g = mergeGroupMatch("Q4_K_M", "model", [fm(), fm({ file: "m2.gguf" })]);
    expect(g.actions).toEqual(["download", "move", "link"]);
    expect(g.defaultAction).toBe("move");
  });

  it("组内动作不一致时取交集，默认降到 download——3 片里 1 片没有本地副本，整组不能笼统地说「移动」", () => {
    const g = mergeGroupMatch("Q4_K_M", "model", [
      fm(),
      fm({ file: "m2.gguf", candidate: null, actions: ["download"], defaultAction: "download" }),
    ]);
    expect(g.actions).toEqual(["download"]);
    expect(g.defaultAction).toBe("download");
  });

  it("组内一片在别的档案、一片游离：交集只剩 link", () => {
    const g = mergeGroupMatch("Q4_K_M", "model", [
      fm(),
      fm({ file: "m2.gguf", actions: ["download", "link"], defaultAction: "link", restriction: "in-repo" }),
    ]);
    expect(g.actions).toEqual(["download", "link"]);
    expect(g.defaultAction).toBe("link");
    expect(g.restriction).toBe("in-repo");
  });
});

describe("actionsFor", () => {
  it("models 内非档案目录：可移动可链接，默认移动", () => {
    const r = actionsFor(remote, cand());
    expect(r.actions).toEqual(["download", "move", "link"]);
    expect(r.defaultAction).toBe("move");
    expect(r.restriction).toBe("none");
  });

  it("在另一个档案目录内：禁用移动，默认链接", () => {
    const r = actionsFor(remote, cand({ inRepoDir: "hf/other/Repo" }));
    expect(r.actions).toEqual(["download", "link"]);
    expect(r.defaultAction).toBe("link");
    expect(r.restriction).toBe("in-repo");
  });

  it("models 外：可复制可移动（移动=复制后删源），禁用链接，默认复制", () => {
    const r = actionsFor(remote, cand({ rel: null, inModelsRoot: false, absPath: "/host-import/a.gguf" }));
    expect(r.actions).toEqual(["download", "copy", "move"]);
    expect(r.defaultAction).toBe("copy");
    expect(r.restriction).toBe("outside-root");
  });

  it("远端无 oid：只能下载——没有内容哈希可比对，不许凭名字挪", () => {
    const r = actionsFor({ path: "README.md", size: 100 }, cand());
    expect(r.actions).toEqual(["download"]);
    expect(r.defaultAction).toBe("download");
    expect(r.restriction).toBe("no-oid");
  });

  it("无候选：只能下载", () => {
    const r = actionsFor(remote, null);
    expect(r.actions).toEqual(["download"]);
    expect(r.defaultAction).toBe("download");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/acquire-match.test.ts`
预期：FAIL，报 `Failed to resolve import "./acquire-match"`

- [ ] **步骤 3：编写实现**

```ts
/**
 * 本地获取的匹配与动作判定（设计 §4.2 / §4.3）
 *
 * 全部零 IO：size 来自 scanTree 已有的 stat，fullSha256 来自 file_meta 缓存。
 * 真正读盘的 L2 校验发生在用户确认之后，由 server/download/localAcquire.ts 承担。
 */

/** 一次获取可以采取的手段 */
export type AcquireAction = "download" | "move" | "link" | "copy";

/** 动作受限的原因码，供 UI 解释为什么只能下载 */
export type AcquireRestriction = "none" | "no-oid" | "in-repo" | "outside-root";

/** 远端清单里的一个文件（HF LFS oid 即内容 sha256；非 LFS 文件没有） */
export interface RemoteFileRef {
  path: string;
  size: number;
  oid?: string;
}

/** 本地扫描出的一个候选文件 */
export interface LocalCandidate {
  /** panel 视角绝对路径 */
  absPath: string;
  /** models 根内的相对路径；models 外为 null */
  rel: string | null;
  size: number;
  /** file_meta 缓存的完整 sha256；没有则 null */
  fullSha256: string | null;
  /** 落在哪个档案的 targetDir 内；不在任何档案内为 null */
  inRepoDir: string | null;
  inModelsRoot: boolean;
}

export interface ActionsResult {
  actions: AcquireAction[];
  defaultAction: AcquireAction;
  restriction: AcquireRestriction;
}

const SHA256_PATTERN = /^[0-9a-f]{64}$/;

/** basename（远端路径固定用 /，本地相对路径同样用 /） */
function basename(p: string): string {
  const slash = Math.max(p.lastIndexOf("/"), p.lastIndexOf("\\"));
  return p.slice(slash + 1);
}

/**
 * L1 快筛：size 必须精确相等，且满足下列之一——
 * - basename 相同（最常见：同一份文件原样放在别处）
 * - 本地已缓存的 full_sha256 等于远端 oid（跨仓库改过名的同一文件，唯一可靠判据）
 *
 * 远端 size 不是正数时一律不匹配：宁可显示「未下载」，也不能凭一个名字就给出
 * 「把某个不知道是什么的文件挪进来」的按钮（沿用 repo-files-view.ts 的 I4 裁定）。
 */
export function matchLocalCandidate(
  remote: RemoteFileRef,
  candidates: readonly LocalCandidate[],
): LocalCandidate | null {
  if (remote.size <= 0) return null;
  const wantName = basename(remote.path);
  const oidUsable = remote.oid !== undefined && SHA256_PATTERN.test(remote.oid);

  for (const c of candidates) {
    if (c.size !== remote.size) continue;
    if (basename(c.absPath) === wantName) return c;
    if (oidUsable && c.fullSha256 !== null && c.fullSha256 === remote.oid) return c;
  }
  return null;
}

/**
 * 动作矩阵（设计 §4.3）。download 恒在首位——它永远是可行的兜底。
 *
 * 远端无 oid 时只给 download：L2 校验没有比对基准，挪一个无法证实的文件比
 * 多下一份危险得多（设计 D14）。
 */
export function actionsFor(remote: RemoteFileRef, candidate: LocalCandidate | null): ActionsResult {
  const onlyDownload: ActionsResult = {
    actions: ["download"],
    defaultAction: "download",
    restriction: "none",
  };
  if (candidate === null) return onlyDownload;

  if (remote.oid === undefined || !SHA256_PATTERN.test(remote.oid)) {
    return { ...onlyDownload, restriction: "no-oid" };
  }

  if (!candidate.inModelsRoot) {
    // 跨挂载点：rename 抛 EXDEV、硬链接跨文件系统不成立，只剩复制系动作
    return { actions: ["download", "copy", "move"], defaultAction: "copy", restriction: "outside-root" };
  }

  if (candidate.inRepoDir !== null) {
    // 移走会掏空那个档案（planFileMove 本就拒绝从档案目录移出）
    return { actions: ["download", "link"], defaultAction: "link", restriction: "in-repo" };
  }

  // 原位置是游离文件，留一份链接没意义
  return { actions: ["download", "move", "link"], defaultAction: "move", restriction: "none" };
}

/** 一个远端文件的匹配结果 */
export interface FileMatch extends ActionsResult {
  file: string;
  candidate: LocalCandidate | null;
}

/** 一个量化组的匹配结果：动作按组选，判定与执行按文件（设计 §4.4） */
export interface GroupMatch extends ActionsResult {
  quant: string | null;
  kind: "model" | "mmproj";
  files: FileMatch[];
}

/**
 * 组级动作 = 组内各文件可选动作的**交集**，默认动作在交集里挑：
 * 组内默认动作一致就取它，否则降到 download。
 *
 * 取交集而不是并集，是因为动作是整组一起执行的——3 片里有 1 片本地没有副本，
 * 整组就不能笼统地说「移动」；那一片必须走下载，于是整组只剩 download 可选。
 * 组内一片在别的档案（只能 link）、一片游离（可 move 可 link）时交集是 link，
 * 正好是唯一对两片都成立的手段。
 */
export function mergeGroupMatch(
  quant: string | null,
  kind: "model" | "mmproj",
  files: FileMatch[],
): GroupMatch {
  const order: AcquireAction[] = ["download", "move", "link", "copy"];
  const actions = order.filter((a) => files.every((f) => f.actions.includes(a)));

  const defaults = new Set(files.map((f) => f.defaultAction));
  const unified = defaults.size === 1 ? [...defaults][0]! : "download";
  const defaultAction = actions.includes(unified) ? unified : "download";

  // 组级 restriction 取组内第一个非 none 的，供 UI 解释为什么动作受限
  const restriction = files.find((f) => f.restriction !== "none")?.restriction ?? "none";

  return { quant, kind, files, actions, defaultAction, restriction };
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/acquire-match.test.ts`
预期：PASS，14 个用例全绿

- [ ] **步骤 5：Commit**

```bash
git add src/lib/acquire-match.ts src/lib/acquire-match.test.ts
git commit -m "feat(local-acquire): L1 匹配、动作矩阵与分片组动作聚合"
```

## 任务 2：migration v17

**文件：**
- 修改：`src/server/migrations.ts`（数组末尾追加一条）
- 修改：`src/server/download/manager.ts:161`（`TaskRow` 加两字段）
- 测试：`src/server/db.test.ts`（追加一条升级用例）

- [ ] **步骤 1：编写失败的测试**

追加到 `src/server/db.test.ts`：

```ts
it("v17：download_tasks 与 download_history 都有 source_path / local_action 列", () => {
  const db = openTestDb(); // 该文件已有的建库助手，跑完全部迁移
  const taskCols = (db.prepare("PRAGMA table_info(download_tasks)").all() as { name: string }[]).map(
    (c) => c.name,
  );
  const histCols = (db.prepare("PRAGMA table_info(download_history)").all() as { name: string }[]).map(
    (c) => c.name,
  );
  expect(taskCols).toContain("source_path");
  expect(taskCols).toContain("local_action");
  expect(histCols).toContain("source_path");
  expect(histCols).toContain("local_action");
  expect(db.pragma("user_version", { simple: true })).toBe(17);
});
```

> 注意：`openTestDb` 是该文件已有的助手名，实现前先 `grep -n "function openTestDb\|const openTestDb" src/server/db.test.ts` 确认实际名字并对齐，不要新造一个。

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/db.test.ts -t "v17"`
预期：FAIL，`expect(taskCols).toContain("source_path")` 不通过

- [ ] **步骤 3：编写实现**

`src/server/migrations.ts` 数组末尾追加：

```ts
  // v17：本地权重迁移（设计 §5）。两张下载表各加两列，纯追加、旧代码忽略即可回滚。
  // source 列是裸 TEXT NOT NULL 无 CHECK 约束（见 v10 建表），新增 "local" 取值
  // 不需要动 schema。两列对 "hf" / "url" 任务恒为 NULL。
  // source_path 统一存 panel 视角绝对路径（不是相对路径）——models 外的源必须是
  // 绝对路径，统一一种形态免去每个调用方判断。
  // download_history 同步加，否则归档后看不出这批是下来的还是挪来的。
  `
ALTER TABLE download_tasks   ADD COLUMN source_path  TEXT;
ALTER TABLE download_tasks   ADD COLUMN local_action TEXT;
ALTER TABLE download_history ADD COLUMN source_path  TEXT;
ALTER TABLE download_history ADD COLUMN local_action TEXT;
`,
```

`src/server/download/manager.ts` 的 `TaskRow`（161 行）追加两字段：

```ts
  updated_at: number;
  source_path: string | null;
  local_action: string | null;
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/db.test.ts`
预期：PASS，含既有的中间态升级用例

- [ ] **步骤 5：Commit**

```bash
git add src/server/migrations.ts src/server/download/manager.ts src/server/db.test.ts
git commit -m "feat(local-acquire): migration v17 两张下载表加 source_path/local_action"
```

---

## 任务 3：inode 去重

**文件：**
- 修改：`src/server/fsScanner.ts:52`（`ModelFile` 加 `ino`）、`:110` 附近（`statFile` 返回 `ino`）
- 修改：`src/server/repoProfiles.ts:114`（`decorateProfileStats` 按 inode 去重）
- 测试：`src/server/repoProfiles.test.ts`（追加硬链接用例）

- [ ] **步骤 1：编写失败的测试**

追加到 `src/server/repoProfiles.test.ts`：

```ts
import { decorateProfileStats } from "./repoProfiles";

it("硬链接共用的文件只计一次占盘——两个档案各报一次完整大小是误导", () => {
  const profiles = [
    { id: 1, repo: "a/A", baseDir: "hf", targetDir: "hf/a/A", createdAt: 0 },
    { id: 2, repo: "b/B", baseDir: "hf", targetDir: "hf/b/B", createdAt: 0 },
  ];
  // 同一个 inode 出现在两个档案目录下（硬链接），磁盘实际只占 1000
  const tree = [
    { folder: "hf/a/A", files: [{ rel: "hf/a/A/m.gguf", size: 1000, mtime: 0, ino: 42 }] },
    { folder: "hf/b/B", files: [{ rel: "hf/b/B/m.gguf", size: 1000, mtime: 0, ino: 42 }] },
  ];
  const stats = decorateProfileStats(profiles, tree);
  // 每个档案自己看仍是 1000（它确实有这个文件）
  expect(stats[0]!.bytes).toBe(1000);
  expect(stats[1]!.bytes).toBe(1000);
  // 但共用标记要在，供 UI 提示「与 X 共用」
  expect(stats[0]!.sharedBytes).toBe(1000);
  expect(stats[1]!.sharedBytes).toBe(1000);
});

it("同一档案内的硬链接只算一次", () => {
  const profiles = [{ id: 1, repo: "a/A", baseDir: "hf", targetDir: "hf/a/A", createdAt: 0 }];
  const tree = [
    {
      folder: "hf/a/A",
      files: [
        { rel: "hf/a/A/m.gguf", size: 1000, mtime: 0, ino: 42 },
        { rel: "hf/a/A/same.gguf", size: 1000, mtime: 0, ino: 42 },
      ],
    },
  ];
  expect(decorateProfileStats(profiles, tree)[0]!.bytes).toBe(1000);
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/repoProfiles.test.ts -t "硬链接"`
预期：FAIL，`ino` 不是 `ModelFile` 的字段（tsc 报错）且 `sharedBytes` 不存在

- [ ] **步骤 3：编写实现**

`src/server/fsScanner.ts`：

```ts
/** models 树中的一个文件：rel 相对根、size 字节、mtime 毫秒、ino inode 号
 *  （硬链接去重用：同 ino 的多个路径在磁盘上是同一份数据，占盘只能算一次） */
export interface ModelFile {
  rel: string;
  size: number;
  mtime: number;
  ino: number;
}
```

`statFile` / `statFileSafe` 的返回值同步带上 `ino: st.ino`（`statSync` 返回值本来就有，零成本）。
两处构造 `ModelFile` 的地方补上该字段。

`src/server/repoProfiles.ts` 的 `decorateProfileStats`：

```ts
export interface RepoProfileStats extends RepoProfile {
  fileCount: number;
  bytes: number;
  /** 其中与别处（别的档案 / 同档案内另一路径）共用同一 inode 的字节数，
   *  供 UI 提示「其中 X 与其他档案共用」。硬链接落地后这个数可以不为 0 */
  sharedBytes: number;
  dirExists: boolean;
}

export function decorateProfileStats(
  profiles: readonly RepoProfile[],
  tree: readonly FolderFiles[],
): RepoProfileStats[] {
  // 全树 inode → 出现次数：跨档案共用的判定需要全局视野，不能只看本档案内
  const inoCount = new Map<number, number>();
  for (const g of tree) for (const f of g.files) inoCount.set(f.ino, (inoCount.get(f.ino) ?? 0) + 1);

  return profiles.map((p) => {
    const entries = tree.filter(
      (g) => g.folder === p.targetDir || g.folder.startsWith(`${p.targetDir}/`),
    );
    const files = entries.flatMap((g) => g.files);
    const fileCount = files.length;

    // 本档案内按 inode 去重后求和：同一份数据被硬链接两次，占盘只有一份
    const seen = new Set<number>();
    let bytes = 0;
    let sharedBytes = 0;
    for (const f of files) {
      if (seen.has(f.ino)) continue;
      seen.add(f.ino);
      bytes += f.size;
      if ((inoCount.get(f.ino) ?? 0) > 1) sharedBytes += f.size;
    }
    return { ...p, fileCount, bytes, sharedBytes, dirExists: entries.length > 0 };
  });
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/repoProfiles.test.ts src/server/fsScanner.test.ts`
预期：PASS。若 `fsScanner.test.ts` 里有构造 `ModelFile` 字面量的既有用例，一并补 `ino` 字段

- [ ] **步骤 5：Commit**

```bash
git add src/server/fsScanner.ts src/server/repoProfiles.ts src/server/repoProfiles.test.ts src/server/fsScanner.test.ts
git commit -m "feat(local-acquire): 扫描带 inode，档案占盘按 inode 去重"
```

---

## 任务 4：挂载表发现与可用导入源

**文件：**
- 创建：`src/server/mounts.ts`、`src/server/mounts.test.ts`
- 创建：`src/app/api/v1/paths/mounts/route.ts`
- 修改：`src/server/pathMaps.ts:11`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { importableMounts } from "./mounts";
import type { ContainerMount } from "./adapters/types";

const mounts: ContainerMount[] = [
  { type: "bind", source: "/mnt/data/apps/llamapad/models", destination: "/host-models" },
  { type: "bind", source: "/mnt/data/apps/llamapad/data", destination: "/app/config" },
  { type: "bind", source: "/proc", destination: "/host/proc" },
  { type: "bind", source: "/var/run/docker.sock", destination: "/var/run/docker.sock" },
  { type: "bind", source: "/mnt/old-llama/models", destination: "/host-import" },
  { type: "volume", source: "somevol", destination: "/data" },
];

describe("importableMounts", () => {
  it("排除面板运行必需的三项与非 bind 挂载", () => {
    const got = importableMounts(mounts, "/host-models");
    expect(got.map((m) => m.panel)).toEqual(["/host-models", "/host-import"]);
  });

  it("models 那组恒在首位，供 UI 把它标为默认扫描范围", () => {
    expect(importableMounts(mounts, "/host-models")[0]!.host).toBe(
      "/mnt/data/apps/llamapad/models",
    );
  });

  it("挂载表为空（非容器环境）返回空数组，不抛错", () => {
    expect(importableMounts([], "/host-models")).toEqual([]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/mounts.test.ts`
预期：FAIL，`Failed to resolve import "./mounts"`

- [ ] **步骤 3：编写实现**

```ts
import type { ContainerMount, DockerAdapter } from "./adapters/types";
import { selfContainerId } from "./selfMounts";
import type { PathMap } from "@/core/paths";

/**
 * 可用导入源发现（设计 §6.1）
 *
 * 面板是容器，只看得见 compose 挂进来的路径。用户填一个宿主机路径时，能不能
 * 访问完全取决于有没有对应的 bind——所以与其让用户猜，不如把面板自己的挂载表
 * 读出来，既做换算映射（PathMap 的形状与 (source, destination) 一模一样），
 * 也做「当前可用范围」的提示与校验。
 *
 * 手法与 selfMounts.discoverHostModelsRoot 同源，只是那边找一个特定 destination，
 * 这边要全部。
 */

/** 面板运行必需、绝不会用来放模型的挂载点 */
const EXCLUDED_DESTINATIONS = new Set(["/app/config", "/host/proc", "/var/run/docker.sock"]);

/**
 * 从挂载表筛出可作为导入源的 bind，models 那组恒排首位。
 * 非 bind（volume / tmpfs）一律排除：它们没有宿主机路径可言。
 */
export function importableMounts(
  mounts: readonly ContainerMount[],
  modelsPanelRoot: string,
): PathMap[] {
  const usable = mounts
    .filter((m) => m.type === "bind" && !EXCLUDED_DESTINATIONS.has(m.destination))
    .map((m) => ({ host: m.source, panel: m.destination }));

  const modelsIdx = usable.findIndex((m) => m.panel === modelsPanelRoot);
  if (modelsIdx <= 0) return usable;
  const [models] = usable.splice(modelsIdx, 1);
  return [models!, ...usable];
}

/**
 * 发现结果的进程级存放处。挂 globalThis 而不是模块级变量——Next 多 bundle
 * 不共享模块作用域，这是 models 根自动发现（instrumentation.ts）踩过并写进
 * 注释的既有结论，此处沿用同一手法。
 */
const MOUNTS_KEY = "__llamapad_importable_mounts__";

export function setDiscoveredMounts(maps: readonly PathMap[]): void {
  (globalThis as Record<string, unknown>)[MOUNTS_KEY] = [...maps];
}

/** 未发现（非容器环境 / docker 不可达）时返回空数组，调用方一律按「只有 models」处理 */
export function getDiscoveredMounts(): PathMap[] {
  return ((globalThis as Record<string, unknown>)[MOUNTS_KEY] as PathMap[] | undefined) ?? [];
}

/** 查自身容器挂载表；任何一步取不到一律返回空数组，绝不抛错（同 discoverHostModelsRoot 的降级契约） */
export async function discoverImportableMounts(
  adapter: DockerAdapter,
  modelsPanelRoot: string,
  env: NodeJS.ProcessEnv = process.env,
): Promise<PathMap[]> {
  const containerId = selfContainerId(env);
  if (containerId === null) return [];
  try {
    const mounts = await adapter.inspectMounts(containerId);
    if (mounts === null) return [];
    return importableMounts(mounts, modelsPanelRoot);
  } catch {
    return [];
  }
}
```

启动钩子里调用一次并存起来：`src/instrumentation.ts` 现有那段发现 models 根的代码之后，
补 `setDiscoveredMounts(await discoverImportableMounts(adapter, panelRoot))`。
（先 `grep -n "discoverHostModelsRoot" src/instrumentation.ts` 找到落点，跟在它后面。）

`src/server/pathMaps.ts` 的 `getPathMaps` 并入发现结果：

```ts
/** 当前生效的路径映射表：models 那组（走 getModelsHost 优先级链）+ 启动时发现的其余 bind */
export function getPathMaps(): PathMap[] {
  const c = getPanelConfig();
  const models = { host: getModelsHost(), panel: c.paths.models.panel };
  const discovered = getDiscoveredMounts().filter((m) => m.panel !== models.panel);
  return [models, ...discovered];
}
```

`src/app/api/v1/paths/mounts/route.ts`：

```ts
/**
 * GET /api/v1/paths/mounts：当前可用的导入源路径（宿主机视角 + 面板视角成对给出）。
 * 用途有二：填自定义目录时给候选提示；填了个没挂载的路径时，前端据此说清
 * 「该路径在面板容器内不可见，需在 docker-compose.yml 增加挂载」而不是
 * 误导性的「目录不存在」。
 */
export async function GET(req: Request): Promise<Response> {
  const auth = await requireAuth(req, getDb());
  if (auth instanceof Response) return auth;
  return NextResponse.json({ mounts: getDiscoveredMounts() });
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/mounts.test.ts && pnpm exec tsc --noEmit`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/mounts.ts src/server/mounts.test.ts src/server/pathMaps.ts "src/app/api/v1/paths/mounts/route.ts"
git commit -m "feat(local-acquire): 面板查自身挂载表发现可用导入源与路径映射"
```

---

## 任务 5：file_meta 口径扩成 union

**文件：**
- 修改：`src/server/fileMeta.ts:230`（`listFileMeta`）
- 测试：`src/server/fileMeta.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
it("listFileMeta 也登记游离文件——L2 算出的哈希要有地方缓存", async () => {
  const world = makeWorld(); // 该文件已有的临时目录助手
  writeFileSync(path.join(world.root, "loose/orphan.gguf"), "x".repeat(100));
  // 不建任何引用 loose/orphan.gguf 的模型配置

  const entries = await listFileMeta(world.db, world.root);
  const loose = entries.find((e) => e.path === "loose/orphan.gguf");
  expect(loose).toBeDefined();
  expect(loose!.sampleSha256).not.toBeNull();
  expect(loose!.isOrphan).toBe(false);
});

it("models 外的文件不登记——path 受 ggufPathSchema 约束必须是根内相对路径", async () => {
  const world = makeWorld();
  const entries = await listFileMeta(world.db, world.root);
  expect(entries.every((e) => !e.path.startsWith("/"))).toBe(true);
});
```

> 实现前先 `grep -n "function makeWorld\|const world" src/server/fileMeta.test.ts` 对齐该文件实际的助手名与返回结构。

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/fileMeta.test.ts -t "游离文件"`
预期：FAIL，`loose` 为 `undefined`

- [ ] **步骤 3：编写实现**

`listFileMeta` 的登记集合从「全部模型配置的 gguf_file / mmproj_file 原始值」改为
「它们 ∪ models 树中未被任何配置引用的 .gguf 文件」：

```ts
  // 登记集合 = 配置引用的逻辑条目 ∪ 游离文件（refs === 0）
  //
  // 加游离文件是本批（本地权重迁移）的需要：L2 校验算出的 full_sha256 得有地方
  // 缓存，否则每次扫描都要把几十 GB 重读一遍；顺带让「未登记」视图的「有备注」
  // 标签成立。models 外的文件不登记——path 受 ggufPathSchema 约束必须是根内
  // 相对路径，而外部导入是一次性动作，导入完文件就在库内了，不值当为它改 schema。
  const referenced = new Set(buildRefMap(db, modelsRoot).keys());
  const unclaimed = scanTree(modelsRoot)
    .flatMap((g) => g.files)
    .filter((f) => f.rel.endsWith(".gguf") && !referenced.has(f.rel))
    .map((f) => f.rel);

  const paths = [...new Set([...configPaths, ...unclaimed])];
```

其余逻辑（逐个 `upsertFileMeta`、探测不到静默跳过）不变。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/fileMeta.test.ts`
预期：PASS。既有用例若断言了条目总数，需按新口径更新——这是口径变更的预期影响，不是回归

- [ ] **步骤 5：Commit**

```bash
git add src/server/fileMeta.ts src/server/fileMeta.test.ts
git commit -m "feat(local-acquire): file_meta 登记口径扩成配置引用与游离文件的并集"
```

- [ ] **批 ① 收尾：跑四件套**

```bash
pnpm test && pnpm run lint && pnpm exec tsc --noEmit && pnpm run build
```

---

# 批 ② 执行层

## 任务 6：local 执行器骨架与校验阶段

**文件：**
- 创建：`src/server/download/localAcquire.ts`、`src/server/download/localAcquire.test.ts`

**接口契约**：返回值必须与 `startDownload` 同形状（`DownloadHandle = { pause, cancel, result }`），
这样 `manager.kick()` 的整条 `handle.result.then(...)` 链路一行不用改。

**进度口径**（本任务确立，后续任务沿用）：进度必须单调递增，不能因为分阶段而倒退。
- `move`(同盘) / `link`：总工作量 = `expectedSize`（只有校验读一遍盘）
- `copy` / `move`(跨盘)：总工作量 = `expectedSize × 2`（校验读一遍 + 复制写一遍）

**暂停语义**：本地复制不支持续传（设计 §7.2），所以 `pause()` 与 `cancel()` 都是「中止并删
`.part`」，差别只在抛哪种错误——`pause` 抛 `PausedError` 让队列停住并把行标 paused，
`cancel` 抛 `CanceledError` 让队列接棒。恢复后从头重跑。

- [ ] **步骤 1：编写失败的测试**

```ts
import { mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import { createHash } from "node:crypto";
import { afterEach, describe, expect, it } from "vitest";
import { runLocalAcquire } from "./localAcquire";
import { isCanceledError } from "./downloader";

let root: string;
afterEach(() => {
  if (root) rmSync(root, { recursive: true, force: true });
});

function world(content = "hello-weights"): { src: string; dst: string; sha: string; size: number } {
  root = mkdtempSync(path.join(tmpdir(), "acquire-"));
  const src = path.join(root, "src.gguf");
  writeFileSync(src, content);
  return {
    src,
    dst: path.join(root, "dst.gguf"),
    sha: createHash("sha256").update(content).digest("hex"),
    size: Buffer.byteLength(content),
  };
}

describe("runLocalAcquire 校验阶段", () => {
  it("sha256 相符时进入执行阶段并返回算出的哈希", async () => {
    const w = world();
    const r = await runLocalAcquire({
      sourcePath: w.src,
      targetPath: w.dst,
      action: "link",
      sameFs: true,
      expectedSize: w.size,
      sha256: w.sha,
    }).result;
    expect(r.ok).toBe(true);
    expect(r.sha256).toBe(w.sha);
    expect(r.sha256Verified).toBe("match");
  });

  it("sha256 不符时 reject，且不产生任何目标文件", async () => {
    const w = world();
    await expect(
      runLocalAcquire({
        sourcePath: w.src,
        targetPath: w.dst,
        action: "link",
        sameFs: true,
        expectedSize: w.size,
        sha256: "b".repeat(64),
      }).result,
    ).rejects.toThrow(/内容不符/);
    expect(() => readFileSync(w.dst)).toThrow();
  });

  it("源文件大小与声明不符时 reject——先于哈希检查，省掉整趟读盘", async () => {
    const w = world();
    await expect(
      runLocalAcquire({
        sourcePath: w.src,
        targetPath: w.dst,
        action: "link",
        sameFs: true,
        expectedSize: w.size + 1,
        sha256: w.sha,
      }).result,
    ).rejects.toThrow(/大小不符/);
  });

  it("cancel 后 reject 为取消错误", async () => {
    const w = world("x".repeat(4_000_000));
    const h = runLocalAcquire({
      sourcePath: w.src,
      targetPath: w.dst,
      action: "copy",
      sameFs: false,
      expectedSize: w.size,
      sha256: w.sha,
    });
    await h.cancel();
    await expect(h.result).rejects.toSatisfy(isCanceledError);
  });

  it("进度回调的 total 对 link 是 size、对 copy 是 size × 2", async () => {
    const w = world();
    const totals: (number | null)[] = [];
    await runLocalAcquire(
      { sourcePath: w.src, targetPath: w.dst, action: "link", sameFs: true, expectedSize: w.size, sha256: w.sha },
      (p) => totals.push(p.total),
    ).result;
    expect(totals.every((t) => t === w.size)).toBe(true);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/download/localAcquire.test.ts`
预期：FAIL，`Failed to resolve import "./localAcquire"`

- [ ] **步骤 3：编写实现**（本任务只做校验阶段 + 骨架，执行阶段留给任务 7/8）

```ts
import { createHash } from "node:crypto";
import { createReadStream } from "node:fs";
import { stat, unlink } from "node:fs/promises";
import { CanceledError, PausedError, type DownloadHandle, type DownloadResult, type ProgressInfo } from "./downloader";

/**
 * 本地获取执行器（设计 §7.2）
 *
 * 与 startDownload 返回同一形状的 DownloadHandle，因此 manager.kick() 的
 * handle.result.then(...) 整条链路（完成落库 / 暂停保位 / 取消让位 / 失败计数 /
 * 批次归档）一行都不用改——这是本设计把风险控制住的关键。
 *
 * 两阶段：校验（流式读源算 sha256）→ 执行（rename / link / copy）。
 * 进度单调递增：总工作量 = 校验一遍 + （需要复制时）再写一遍，见 totalWorkOf。
 *
 * 不支持续传：下载器的续传靠 .part.meta.json 里的 url/etag 判定同源，本地复制
 * 没有这两样可比对，硬做等于凭路径猜。pause 与 cancel 都是中止并删 .part，
 * 差别只在抛哪种错误；恢复后从头重跑，本地盘重来的成本远低于网络下载。
 */

export interface LocalAcquireRequest {
  /** panel 视角绝对路径 */
  sourcePath: string;
  /** panel 视角绝对路径（最终落点） */
  targetPath: string;
  action: "move" | "link" | "copy";
  /** 源与目标是否同一文件系统。false 时 move 降级为「复制后删源」、link 直接失败 */
  sameFs: boolean;
  expectedSize: number;
  /** 期望 sha256（远端 LFS oid）。acquire 路由保证非空——无 oid 的文件根本不给挪 */
  sha256: string;
}

/** 需要复制时总工作量翻倍（校验读一遍 + 复制写一遍），保证进度不倒退 */
function totalWorkOf(req: LocalAcquireRequest): number {
  const needsCopy = req.action === "copy" || (req.action === "move" && !req.sameFs);
  return needsCopy ? req.expectedSize * 2 : req.expectedSize;
}

export function runLocalAcquire(
  req: LocalAcquireRequest,
  onProgress?: (p: ProgressInfo) => void,
): DownloadHandle {
  const controller = new AbortController();
  let pausing = false;

  const result: Promise<DownloadResult> = (async () => {
    const total = totalWorkOf(req);

    // 大小先于哈希：省掉一整趟读盘，且「下坏了的半成品」这种最常见的不匹配
    // 能立刻给出准确原因
    const st = await stat(req.sourcePath);
    if (st.size !== req.expectedSize) {
      throw new Error(`源文件大小不符：期望 ${req.expectedSize}，实际 ${st.size}`);
    }

    const hash = createHash("sha256");
    let read = 0;
    const started = Date.now();
    const stream = createReadStream(req.sourcePath, { signal: controller.signal });
    try {
      for await (const chunk of stream) {
        hash.update(chunk as Buffer);
        read += (chunk as Buffer).length;
        const elapsed = (Date.now() - started) / 1000;
        onProgress?.({ downloaded: read, total, bytesPerSec: elapsed > 0 ? read / elapsed : 0 });
      }
    } catch (error) {
      if (controller.signal.aborted) throw pausing ? new PausedError() : new CanceledError();
      throw error;
    }

    const actual = hash.digest("hex");
    if (actual !== req.sha256) {
      throw new Error(`内容不符：期望 sha256 ${req.sha256}，实际 ${actual}`);
    }

    // 执行阶段（任务 7 / 8 填入）
    await performAction(req, read, total, started, onProgress, controller, () => pausing);

    return { ok: true, bytes: req.expectedSize, sha256: actual, sha256Verified: "match" };
  })();

  return {
    pause(): void {
      pausing = true;
      controller.abort();
    },
    async cancel(): Promise<void> {
      controller.abort();
      await unlink(req.targetPath + ".part").catch(() => undefined);
    },
    result,
  };
}
```

> `CanceledError` / `PausedError` 是否已从 `downloader.ts` 导出？实现前
> `grep -n "export class\|isPausedError\|isCanceledError" src/server/download/downloader.ts` 确认。
> 若只导出了 `isPausedError` / `isCanceledError` 两个判定函数而没导出类，就一并导出类，
> 或在 `localAcquire.ts` 里用同款 `code` 字段构造错误——**两边判别方式必须一致**，
> 否则 `manager` 会把取消当成失败计入连续失败计数。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/download/localAcquire.test.ts -t "校验阶段"`
预期：前 4 个用例 PASS；`performAction` 尚未实现，最后一个进度用例可能仍失败——任务 7 补齐

- [ ] **步骤 5：Commit**

```bash
git add src/server/download/localAcquire.ts src/server/download/localAcquire.test.ts
git commit -m "feat(local-acquire): 本地执行器骨架与 sha256 校验阶段"
```

---

## 任务 7：move / link 执行器

**文件：**
- 修改：`src/server/download/localAcquire.ts`（补 `performAction` 的 rename / link 分支）
- 测试：`src/server/download/localAcquire.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
describe("performAction: move / link", () => {
  it("move 同盘：源消失、目标出现", async () => {
    const w = world();
    await runLocalAcquire({
      sourcePath: w.src, targetPath: w.dst, action: "move",
      sameFs: true, expectedSize: w.size, sha256: w.sha,
    }).result;
    expect(existsSync(w.src)).toBe(false);
    expect(readFileSync(w.dst, "utf8")).toBe("hello-weights");
  });

  it("link 同盘：源与目标都在，且是同一个 inode", async () => {
    const w = world();
    await runLocalAcquire({
      sourcePath: w.src, targetPath: w.dst, action: "link",
      sameFs: true, expectedSize: w.size, sha256: w.sha,
    }).result;
    expect(statSync(w.src).ino).toBe(statSync(w.dst).ino);
  });

  it("link 跨文件系统：直接拒绝，不做无谓尝试", async () => {
    const w = world();
    await expect(
      runLocalAcquire({
        sourcePath: w.src, targetPath: w.dst, action: "link",
        sameFs: false, expectedSize: w.size, sha256: w.sha,
      }).result,
    ).rejects.toThrow(/CROSS_DEVICE/);
  });

  it("目标目录不存在时自动创建——档案目录一定在，但子目录可能没有", async () => {
    const w = world();
    const nested = path.join(root, "a/b/c/dst.gguf");
    await runLocalAcquire({
      sourcePath: w.src, targetPath: nested, action: "move",
      sameFs: true, expectedSize: w.size, sha256: w.sha,
    }).result;
    expect(existsSync(nested)).toBe(true);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/download/localAcquire.test.ts -t "performAction"`
预期：FAIL，`performAction is not defined`

- [ ] **步骤 3：编写实现**

```ts
/** EXDEV 转成有码错误：裸 EXDEV 会冒泡成没有错误码的 500，用户看不懂 */
function isCrossDevice(error: unknown): boolean {
  return (error as NodeJS.ErrnoException).code === "EXDEV";
}

async function performAction(
  req: LocalAcquireRequest,
  readSoFar: number,
  total: number,
  started: number,
  onProgress: ((p: ProgressInfo) => void) | undefined,
  controller: AbortController,
  isPausing: () => boolean,
): Promise<void> {
  await mkdir(path.dirname(req.targetPath), { recursive: true });

  if (req.action === "link") {
    if (!req.sameFs) throw new Error("CROSS_DEVICE：源与目标不在同一文件系统，无法硬链接");
    try {
      await link(req.sourcePath, req.targetPath);
    } catch (error) {
      if (isCrossDevice(error)) throw new Error("CROSS_DEVICE：源与目标不在同一文件系统，无法硬链接");
      throw error;
    }
    return;
  }

  if (req.action === "move" && req.sameFs) {
    try {
      await rename(req.sourcePath, req.targetPath);
    } catch (error) {
      if (isCrossDevice(error)) {
        // sameFs 判定失误的兜底：调用方按「是否在 models 根内」推断，
        // 根内跨挂载点（用户把子目录单独挂了别的盘）确实可能骗过它
        throw new Error("CROSS_DEVICE：源与目标不在同一文件系统，请改用复制");
      }
      throw error;
    }
    return;
  }

  // copy，以及跨盘的 move（复制后删源）——任务 8 实现
  await copyStream(req, readSoFar, total, started, onProgress, controller, isPausing);
  if (req.action === "move") await unlink(req.sourcePath);
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/download/localAcquire.test.ts -t "performAction"`
预期：前 2 个与第 4 个 PASS；跨盘 link 用例 PASS；copy 相关仍待任务 8

- [ ] **步骤 5：Commit**

```bash
git add src/server/download/localAcquire.ts src/server/download/localAcquire.test.ts
git commit -m "feat(local-acquire): move 与 link 执行器，EXDEV 转 CROSS_DEVICE"
```

---

## 任务 8：copy 执行器

**文件：**
- 修改：`src/server/download/localAcquire.ts`（补 `copyStream`）
- 测试：`src/server/download/localAcquire.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
describe("copyStream", () => {
  it("copy：源保留、目标出现、内容一致", async () => {
    const w = world();
    await runLocalAcquire({
      sourcePath: w.src, targetPath: w.dst, action: "copy",
      sameFs: false, expectedSize: w.size, sha256: w.sha,
    }).result;
    expect(existsSync(w.src)).toBe(true);
    expect(readFileSync(w.dst, "utf8")).toBe("hello-weights");
  });

  it("跨盘 move：复制完成后源被删除", async () => {
    const w = world();
    await runLocalAcquire({
      sourcePath: w.src, targetPath: w.dst, action: "move",
      sameFs: false, expectedSize: w.size, sha256: w.sha,
    }).result;
    expect(existsSync(w.src)).toBe(false);
    expect(readFileSync(w.dst, "utf8")).toBe("hello-weights");
  });

  it("复制过程中取消：目标位置无半成品，.part 也被删除", async () => {
    const w = world("y".repeat(8_000_000));
    const h = runLocalAcquire({
      sourcePath: w.src, targetPath: w.dst, action: "copy",
      sameFs: false, expectedSize: w.size, sha256: w.sha,
    });
    setTimeout(() => void h.cancel(), 5);
    await expect(h.result).rejects.toSatisfy(isCanceledError);
    expect(existsSync(w.dst)).toBe(false);
    expect(existsSync(w.dst + ".part")).toBe(false);
  });

  it("进度跨越两阶段单调递增，最终等于 size × 2", async () => {
    const w = world("z".repeat(200_000));
    const seen: number[] = [];
    await runLocalAcquire(
      { sourcePath: w.src, targetPath: w.dst, action: "copy", sameFs: false, expectedSize: w.size, sha256: w.sha },
      (p) => seen.push(p.downloaded),
    ).result;
    expect(seen).toEqual([...seen].sort((a, b) => a - b));
    expect(seen.at(-1)).toBe(w.size * 2);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/download/localAcquire.test.ts -t "copyStream"`
预期：FAIL，`copyStream is not defined`

- [ ] **步骤 3：编写实现**

```ts
import { PART_SUFFIX } from "@/lib/download-part";

/**
 * 流式复制到 <target>.part 再 rename——沿用下载器的原子落盘手法，
 * 中途崩溃不会在目标位置留下半成品。用 .part 后缀还有一个连带好处：
 * repo-files-scan.isPartial 天然把它滤掉，扫描不会把正在写的文件
 * 当成「已存在的文件」。
 */
async function copyStream(
  req: LocalAcquireRequest,
  readSoFar: number,
  total: number,
  started: number,
  onProgress: ((p: ProgressInfo) => void) | undefined,
  controller: AbortController,
  isPausing: () => boolean,
): Promise<void> {
  const partPath = req.targetPath + PART_SUFFIX;
  let written = 0;
  try {
    await pipeline(
      createReadStream(req.sourcePath),
      async function* (source) {
        for await (const chunk of source) {
          written += (chunk as Buffer).length;
          const elapsed = (Date.now() - started) / 1000;
          onProgress?.({
            downloaded: readSoFar + written,
            total,
            bytesPerSec: elapsed > 0 ? (readSoFar + written) / elapsed : 0,
          });
          yield chunk;
        }
      },
      createWriteStream(partPath),
      { signal: controller.signal },
    );
  } catch (error) {
    await unlink(partPath).catch(() => undefined);
    if (controller.signal.aborted) throw isPausing() ? new PausedError() : new CanceledError();
    throw error;
  }
  await rename(partPath, req.targetPath);
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/download/localAcquire.test.ts`
预期：PASS，全部用例（含任务 6 那个进度用例）

- [ ] **步骤 5：Commit**

```bash
git add src/server/download/localAcquire.ts src/server/download/localAcquire.test.ts
git commit -m "feat(local-acquire): 流式复制执行器与跨盘移动"
```

---

## 任务 9：队列接线（`source = "local"`）

**文件：**
- 修改：`src/server/download/manager.ts:320`（`buildRequest` 旁加 `buildLocalRequest`）、
  `:424`（执行器选择抽成 `startTask`）、`:530`（`enqueueDownload` 接受外部 `batchId`）、
  新增 `enqueueLocal`
- 测试：`src/server/download/manager.test.ts`

**视角约定**（本任务确立）：`download_tasks.source_path` 存 **panel 视角绝对路径**——
执行器要用它直接读文件。API 层收的是宿主机视角（项目约定：所有配置路径以宿主机视角书写），
入库前用 `toPanel()` 换算，见任务 10。

- [ ] **步骤 1：编写失败的测试**

```ts
it("local 任务走本地执行器，完成后落库 completed 并写回 sha256", async () => {
  const w = makeManagerWorld(); // 该文件已有的建库+临时目录助手
  const src = path.join(w.root, "loose/a.gguf");
  mkdirSync(path.dirname(src), { recursive: true });
  writeFileSync(src, "payload");
  const sha = createHash("sha256").update("payload").digest("hex");

  const { batchId } = await w.manager.enqueueLocal({
    items: [{
      file: "a.gguf", sourcePath: src, action: "move",
      sameFs: true, size: 7, sha256: sha,
    }],
    targetDir: "hf/o/R",
    label: "o/R",
  });
  await w.manager.waitIdle();

  const row = w.db.prepare("SELECT * FROM download_tasks WHERE batch_id = ?").get(batchId) as {
    status: string; source: string; local_action: string; sha256: string;
  };
  expect(row.status).toBe("completed");
  expect(row.source).toBe("local");
  expect(row.local_action).toBe("move");
  expect(row.sha256).toBe(sha);
  expect(existsSync(path.join(w.root, "hf/o/R/a.gguf"))).toBe(true);
  expect(existsSync(src)).toBe(false);
});

it("local 任务校验失败时标 failed 且 error 含原因，队列继续接棒", async () => {
  const w = makeManagerWorld();
  const src = path.join(w.root, "loose/b.gguf");
  mkdirSync(path.dirname(src), { recursive: true });
  writeFileSync(src, "payload");

  const { batchId } = await w.manager.enqueueLocal({
    items: [{
      file: "b.gguf", sourcePath: src, action: "move",
      sameFs: true, size: 7, sha256: "c".repeat(64),
    }],
    targetDir: "hf/o/R",
    label: "o/R",
  });
  await w.manager.waitIdle();

  const row = w.db.prepare("SELECT status, error FROM download_tasks WHERE batch_id = ?").get(batchId) as {
    status: string; error: string;
  };
  expect(row.status).toBe("failed");
  expect(row.error).toMatch(/内容不符/);
  expect(existsSync(src)).toBe(true); // 校验没过，源文件必须原封不动
});

it("同一 batchId 里混合 download 与 local 任务，归档按批次统一收口", async () => {
  const w = makeManagerWorld();
  const batchId = "fixed-batch";
  await w.manager.enqueueDownload({
    files: [{ file: "x.gguf", size: 10 }],
    targetDir: "hf/o/R", source: "hf", repo: "o/R", label: "o/R", batchId,
  });
  await w.manager.enqueueLocal({ items: [/* 同上结构 */], targetDir: "hf/o/R", label: "o/R", batchId });
  const rows = w.db.prepare("SELECT source FROM download_tasks WHERE batch_id = ?").all(batchId);
  expect(rows).toHaveLength(2);
});
```

> `makeManagerWorld` / `waitIdle` 是占位名——实现前 `grep -n "function make\|waitIdle\|await flush" src/server/download/manager.test.ts`
> 对齐该文件实际的助手与「等队列跑完」的手法，不要新造一套。

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/download/manager.test.ts -t "local"`
预期：FAIL，`enqueueLocal is not a function`

- [ ] **步骤 3：编写实现**

三处改动。第一处，`buildRequest` 旁加：

```ts
  /** local 任务的请求组装：source_path 已是 panel 视角绝对路径，直接用 */
  function buildLocalRequest(task: TaskRow): LocalAcquireRequest {
    const targetPath = path.join(modelsRoot, task.target_rel);
    const sourcePath = task.source_path!;
    return {
      sourcePath,
      targetPath,
      action: task.local_action as "move" | "link" | "copy",
      // 同一文件系统的判定：源落在 models 根内即同盘。根内跨挂载点的极端情况
      // 由执行器的 EXDEV 兜底转成 CROSS_DEVICE，不在这里猜
      sameFs: sourcePath === modelsRoot || sourcePath.startsWith(modelsRoot + path.sep),
      expectedSize: task.expected_size!,
      sha256: task.sha256!,
    };
  }
```

第二处，`kick()` 里 `handle = downloader(req, onProgress)` 那一段抽成：

```ts
  /** 按 source 选执行器：两者返回同一形状的 DownloadHandle，下游链路无差别 */
  function startTask(task: TaskRow, onProgress: (p: ProgressInfo) => void): DownloadHandle {
    return task.source === "local"
      ? runLocalAcquire(buildLocalRequest(task), onProgress)
      : downloader(buildRequest(task), onProgress);
  }
```

`kick()` 内改为 `handle = startTask(next, onProgress);`。**其余一行不动**——完成落库、
暂停保位、取消让位、失败计数、停摆、`archiveIfBatchDone` 全部与源无关。

第三处，入队。`EnqueueDownloadArgs` 加可选 `batchId`（不传仍 `randomUUID()`），并新增：

```ts
export interface EnqueueLocalItem {
  /** 远端仓库内路径，决定 target_rel 的 basename */
  file: string;
  /** panel 视角绝对路径 */
  sourcePath: string;
  action: "move" | "link" | "copy";
  sameFs: boolean;
  size: number;
  sha256: string;
}

export interface EnqueueLocalArgs {
  items: EnqueueLocalItem[];
  targetDir: string;
  repoId?: number;
  label: string;
  /** 与同一次确认里的 download 任务共用一个批次；不传则新开一批 */
  batchId?: string;
}

async function enqueueLocal(args: EnqueueLocalArgs): Promise<{ taskIds: number[]; batchId: string }> {
  // 复用 enqueueDownload 的路径校验与 unfinishedByTarget 并发检查，
  // 但跳过磁盘空间预检的网络部分：link 不占盘、move 同盘不占盘，
  // 只有 copy 需要空间——按 copy 的总量检查
  // ...（实现时把 enqueueDownload 里的校验段抽成共用私有函数，不要复制粘贴）
}
```

**注意**：`enqueueDownload` 现有的「已存在则跳过」判断（`manager.ts:550-562`）对 local 任务
同样适用且必须保留——目标位置已有同名同大小文件时不该再挪一次。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/download/manager.test.ts`
预期：PASS，含既有的 45KB 全套下载队列用例（零回归是本任务的核心验收）

- [ ] **步骤 5：Commit**

```bash
git add src/server/download/manager.ts src/server/download/manager.test.ts
git commit -m "feat(local-acquire): 队列接入 local 源，执行器按 source 分派"
```

---

## 任务 10：acquire API 与服务端重验

**文件：**
- 创建：`src/app/api/v1/repos/[id]/acquire/route.ts`
- 删除：`src/app/api/v1/repos/[id]/download/route.ts`
- 创建：`src/server/acquireGuard.ts`、`src/server/acquireGuard.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { assertSourceAllowed, AcquireGuardError } from "./acquireGuard";

describe("assertSourceAllowed", () => {
  const roots = ["/host-models", "/host-import"];

  it("models 根内放行", () => {
    expect(() => assertSourceAllowed("/host-models/loose/a.gguf", roots)).not.toThrow();
  });

  it("已配置的自定义目录内放行", () => {
    expect(() => assertSourceAllowed("/host-import/old/a.gguf", roots)).not.toThrow();
  });

  it("允许范围之外一律拒绝", () => {
    expect(() => assertSourceAllowed("/etc/passwd", roots)).toThrow(AcquireGuardError);
  });

  it("前缀相同但不是目录边界的路径要拒绝——/host-models2 不是 /host-models 的子路径", () => {
    expect(() => assertSourceAllowed("/host-models2/a.gguf", roots)).toThrow(AcquireGuardError);
  });

  it(".. 逃逸在归一化后被拒绝", () => {
    expect(() => assertSourceAllowed("/host-models/../etc/passwd", roots)).toThrow(AcquireGuardError);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/acquireGuard.test.ts`
预期：FAIL，`Failed to resolve import "./acquireGuard"`

- [ ] **步骤 3：编写实现**

```ts
import { normalize, sep } from "node:path";

/**
 * acquire 的源路径守卫（设计 §8.1）
 *
 * 扫描结果不入库，用户确认时源路径由前端带回——所以服务端必须自己再验一遍，
 * 不能信前端。这与 README-LLM 批「前后端各回证一次」是同一个模式。
 *
 * 目录边界判定复用 core/paths 的语义：相等或以 root + 分隔符开头，
 * 纯字符串前缀不算（防 /host-models 与 /host-models2 误匹配）。
 */
export class AcquireGuardError extends Error {
  constructor(readonly code: "OUT_OF_SCOPE" | "NOT_FOUND" | "MISMATCH", message: string) {
    super(message);
    this.name = "AcquireGuardError";
  }
}

function isInside(root: string, p: string): boolean {
  const r = normalize(root);
  const t = normalize(p);
  return t === r || t.startsWith(r.endsWith(sep) ? r : r + sep);
}

export function assertSourceAllowed(sourcePath: string, allowedRoots: readonly string[]): void {
  if (!allowedRoots.some((root) => isInside(root, sourcePath))) {
    throw new AcquireGuardError(
      "OUT_OF_SCOPE",
      `源路径不在允许范围内: ${sourcePath}`,
    );
  }
}
```

`src/app/api/v1/repos/[id]/acquire/route.ts`：

```ts
/**
 * POST /api/v1/repos/:id/acquire：一次确认的统一提交入口（设计 §8）。
 *
 * 替换原 POST /repos/:id/download（只有档案页一个调用方，直接迁移，不留两个入口）。
 * body 逐行给出用户选的动作；download 之外的动作都要过三道重验：
 * 源确实存在、L1 仍然匹配、路径落在允许范围内。一次确认产生一个 batch，
 * 混合 download 与 local 两类任务，归档按 batch 统一收口。
 */
const itemSchema = z.strictObject({
  file: z.string().min(1),
  action: z.enum(["download", "move", "link", "copy"]),
  /** 宿主机视角绝对路径；action !== "download" 时必填 */
  sourceHostPath: z.string().min(1).optional(),
});
const bodySchema = z.strictObject({ items: z.array(itemSchema).min(1) });

export async function POST(req: Request, ctx: { params: Promise<{ id: string }> }): Promise<Response> {
  const auth = await requireAuth(req, getDb());
  if (auth instanceof Response) return auth;

  const parsed = bodySchema.safeParse(await req.json().catch(() => null));
  if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 });

  const db = getDb();
  const profile = getProfile(db, Number((await ctx.params).id));
  if (!profile) return NextResponse.json({ error: "NOT_FOUND" }, { status: 404 });

  const remote = await getRemoteGroups(db, profile.repo); // 复用档案页那条取数
  const allowedRoots = [getPanelModelsRoot(), ...getConfiguredScanDirs(db).map(toPanel)];
  const batchId = randomUUID();

  const downloads: DownloadFileInput[] = [];
  const locals: EnqueueLocalItem[] = [];

  for (const item of parsed.data.items) {
    const rf = findRemoteFile(remote, item.file);
    if (!rf) return NextResponse.json({ error: "UNKNOWN_FILE", file: item.file }, { status: 400 });

    if (item.action === "download") {
      downloads.push(toDownloadFile(rf));
      continue;
    }
    if (item.sourceHostPath === undefined) {
      return NextResponse.json({ error: "SOURCE_REQUIRED", file: item.file }, { status: 400 });
    }

    const sourcePath = toPanel(item.sourceHostPath);
    assertSourceAllowed(sourcePath, allowedRoots);          // 重验 3：范围
    const st = statSync(sourcePath);                         // 重验 1：存在
    if (!st.isFile()) throw new AcquireGuardError("NOT_FOUND", `不是普通文件: ${sourcePath}`);
    if (rf.oid === undefined || st.size !== rf.size) {        // 重验 2：L1 仍匹配
      return NextResponse.json({ error: "MISMATCH", file: item.file }, { status: 400 });
    }

    locals.push({
      file: item.file,
      sourcePath,
      action: item.action === "download" ? "copy" : item.action,
      sameFs: isInside(getPanelModelsRoot(), sourcePath),
      size: rf.size,
      sha256: rf.oid,
    });
  }

  const manager = getDownloadManager();
  if (downloads.length > 0) {
    await manager.enqueueDownload({
      files: downloads, targetDir: profile.targetDir, source: "hf",
      repo: profile.repo, repoId: profile.id, label: profile.repo, batchId,
    });
  }
  if (locals.length > 0) {
    await manager.enqueueLocal({
      items: locals, targetDir: profile.targetDir, repoId: profile.id, label: profile.repo, batchId,
    });
  }
  return NextResponse.json({ batchId, downloads: downloads.length, locals: locals.length });
}
```

**同时删除** `src/app/api/v1/repos/[id]/download/route.ts`，并把 `repo-detail-view.tsx` 里
`POST /repos/:id/download` 的调用改指 `acquire`（前端完整改造在任务 15，此处只做最小迁移
让构建不断）。

**两个助手的出处**：
- `toDownloadFile`（把远端文件转成下载任务条目，含 oid → sha256 的正则校验）目前只存在于
  前端组件 `repo-detail-view.tsx:122`。服务端要用，就把它连同 `SHA256_PATTERN` 一起下沉到
  `src/lib/acquire-match.ts`（那里已经有同一份正则），前端改为 import——**不要复制第二份**，
  该文件的注释里已经点名「三处同名常量」是笔待还的债，别再加一处。
- `isInside` 是 `core/paths.ts` 里的私有函数，本任务的 `acquireGuard.ts` 自带了一份等价实现。
  这是有意的：`core/paths` 那份没导出，且它服务于视角换算而非权限判定，两者语义会各自演化。

**scan-dirs 不新建路由**：设计 §8 列的 `GET/PUT /settings/scan-dirs` 由两处覆盖——读取走
`scanDirs.getConfiguredScanDirs`（服务端内部调用），写入随 `POST /repos/:id/scan` 的
`extraDirs` 参数落库。真要独立读写，现成的通用路由 `/api/v1/settings/scan_extra_dirs`
已经够用，无需新增文件。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/acquireGuard.test.ts && pnpm exec tsc --noEmit`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/acquireGuard.ts src/server/acquireGuard.test.ts "src/app/api/v1/repos/[id]/acquire/route.ts"
git rm "src/app/api/v1/repos/[id]/download/route.ts"
git commit -m "feat(local-acquire): acquire 统一提交入口与源路径重验守卫"
```

- [ ] **批 ② 收尾：跑四件套**

```bash
pnpm test && pnpm run lint && pnpm exec tsc --noEmit && pnpm run build
```

---

# 批 ③ 档案页

> **本批的测试形态说明**：vitest 是 node 环境、无 jsdom，组件测不了（项目既有约束）。
> 因此任务 11 / 13 的判定全部下沉到 `src/lib/` 并有完整单测；任务 14 / 15 是组件，
> 验收靠 `tsc --noEmit` + `next build` + 真机核对清单，**不写形同虚设的组件测试**。

## 任务 11：stray 扫描放开档案目录并逐分片记录

**文件：**
- 修改：`src/lib/repo-files-scan.ts:63`、`src/lib/repo-files-view.ts:131,273`
- 测试：`src/lib/repo-files-scan.test.ts`、`src/lib/repo-files-view.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
// repo-files-scan.test.ts
it("在别的档案目录内的文件也算候选，并标出所属档案", () => {
  const tree = [
    { folder: "hf/other/Repo", files: [{ rel: "hf/other/Repo/m.gguf", size: 100 }] },
  ];
  const got = scanRepoFiles(tree, "hf/mine/Repo", ["hf/mine/Repo", "hf/other/Repo"]);
  expect(got.strays).toEqual([
    { file: "m.gguf", rel: "hf/other/Repo/m.gguf", size: 100, inRepoDir: "hf/other/Repo" },
  ]);
});

it("本档案目录内的文件不算 stray（它已经在位）", () => {
  const tree = [{ folder: "hf/mine/Repo", files: [{ rel: "hf/mine/Repo/m.gguf", size: 100 }] }];
  expect(scanRepoFiles(tree, "hf/mine/Repo", ["hf/mine/Repo"]).strays).toEqual([]);
});

// repo-files-view.test.ts
it("多分片组逐片记录 stray，不再只认第一片", () => {
  const rows = mergeRepoRows({
    groups: [{
      quant: "Q4_K_M", label: "Q4_K_M", kind: "model", shards: 2, shardTotalDeclared: 2,
      totalSize: 200,
      files: [
        { path: "m-00001-of-00002.gguf", size: 100, oid: "a".repeat(64) },
        { path: "m-00002-of-00002.gguf", size: 100, oid: "b".repeat(64) },
      ],
    }],
    local: [],
    strays: [
      { file: "m-00001-of-00002.gguf", rel: "loose/m-00001-of-00002.gguf", size: 100, inRepoDir: null },
      { file: "m-00002-of-00002.gguf", rel: "loose/m-00002-of-00002.gguf", size: 100, inRepoDir: null },
    ],
    tasks: [], configs: [], targetDir: "hf/o/R",
  });
  expect(rows[0]!.strayRels).toEqual([
    "loose/m-00001-of-00002.gguf",
    "loose/m-00002-of-00002.gguf",
  ]);
});

it("stray 行可勾选——用户要能选中它再决定动作", () => {
  expect(isSelectable({ state: "stray" } as RepoRow)).toBe(true);
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/repo-files-scan.test.ts src/lib/repo-files-view.test.ts`
预期：FAIL——`strays` 为空数组（现状排除了所有档案目录）；`strayRels` 不存在；`isSelectable` 返回 false

- [ ] **步骤 3：编写实现**

`repo-files-scan.ts`：把「排除所有档案目录」改为「排除本档案目录，其余标出所属档案」。

```ts
export interface RepoFilesScan {
  local: { rel: string; size: number }[];
  strays: { file: string; rel: string; size: number; inRepoDir: string | null }[];
}

  // 原先排除全部档案目录（`repoDirOf(...) === null`）是因为 planFileMove 拒绝从
  // 档案目录移出，UI 给出的归位候选必须是服务端愿意接受的子集。本批引入硬链接后
  // 这个前提变了：别的档案里的文件不能「移」但可以「链接」，排除它等于把
  // 「两个仓库共用一份文件」这个最值钱的场景挡在门外。改为标出所属档案，
  // 由 acquire-match.actionsFor 决定它只能 link。
  const strays = tree
    .filter((g) => repoDirOf(g.folder, [targetDir]) === null) // 只排除本档案
    .flatMap((g) => g.files.map((f) => ({ f, dir: repoDirOf(g.folder, repoDirs) })))
    .filter(({ f }) => !isPartial(f.rel))
    .filter(({ f }) => !localNames.has(basename(f.rel)))
    .map(({ f, dir }) => ({ file: basename(f.rel), rel: f.rel, size: f.size, inRepoDir: dir }));
```

`repo-files-view.ts`：`RepoRow.strayRel: string | null` 改为 `strayRels: string[]`（按
`group.files` 顺序，缺片处不占位），匹配循环里去掉 `if (strayRel === null)` 的早退；
`isSelectable` 增加 `"stray"`：

```ts
export function isSelectable(row: RepoRow): boolean {
  return row.state === "absent" || row.state === "partial" || row.state === "stray";
}
```

**影响范围**：`strayRel` → `strayRels` 是破坏性重命名。写计划时这里只写了「归位按钮一处」，
按 `7f82cb9` 复核后，`grep -rn "strayRel" src` 共 **31 处命中**——2 个源文件 18 处
（含 2 处注释，`repo-detail-view.tsx:674,875`，改完记得同步措辞）+ 4 个测试文件 13 处。
其中两处需要先做决定再动手：

| 位置 | 现状 | 改法 |
|---|---|---|
| `repo-files-view.ts:44,101,131,142,154,172,214` | 字段声明、匹配循环早退、`localOnlyRows` 兜底 | 按上文改成数组，去掉 `if (strayRel === null)` 早退 |
| `repo-detail-view.tsx:162` `repositioningRel: string \| null` | 用 rel 字符串标识「哪一行正在归位」 | 一组现在有多个 rel，rel 不再能当行标识。**改用组身份** `` `${row.kind}:${row.files.join(",")}` ``（与 `QuantCard` 的 `key` 同一份表达式），状态名一并改成 `repositioningKey` |
| `repo-detail-view.tsx:339,340,344` `onReposition` | 取 `row.strayRel` 当 `from` | `from` 取 `row.strayRels[0]`（`planFileMove` 本就整组搬，见设计 §2 现状表），空数组直接 return |
| `repo-detail-view.tsx:577` `repositioning` prop | `row.strayRel !== null && repositioningRel === row.strayRel` | 换成组身份比较 |
| `repo-detail-view.tsx:678` `repositionButton` | `row.strayRel !== null` | `row.strayRels.length > 0` |
| `repo-detail-view.tsx:805-841` `StrayMark` | 气泡里渲染**一条** `strayAt` | 见下 |
| `repo-files-view.test.ts` / `repo-weights.test.ts:18` / `batch-create.test.ts` | `RepoRow` 夹具带 `strayRel: null` | 批量改成 `strayRels: []` |

`StrayMark` 是 `673583d`/`7f82cb9` 两个提交新加的组件（写计划时还不存在）：它把「在别处」
收成一个感叹号，路径细节进 Tooltip 气泡。改复数后**气泡里逐条列路径**，不新增文案键——
`strayAt` 照原样对每条 rel 渲染一次：

```tsx
<TooltipContent className="max-w-xs">
  <span className="font-medium">{t("stateStray")}</span>
  {row.strayRels.map((rel) => (
    <span key={rel} className="mt-0.5 block font-mono break-all">
      {t("strayAt", { dir: rel })}
    </span>
  ))}
</TooltipContent>
```

**连带影响 `isSelectable`**：它不止服务档案页，`repo-weights.ts:36` 也用它算 README 视图
权重卡的 `selectable`。放行 `stray` 之后，README 里「在别处」的权重也会变成可点、点了跳到
文件视图并勾选（`repo-detail-view.tsx:458` 的 `onPickWeight`）——这正是本期想要的效果，
不是回归。但 `repo-weights.test.ts:70` 有一条 `["stray", false]` 的参数化断言**会因此变红**，
要一并改成 `true`，别把它当成实现写错了。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/repo-files-scan.test.ts src/lib/repo-files-view.test.ts && pnpm exec tsc --noEmit`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/lib/repo-files-scan.ts src/lib/repo-files-view.ts src/lib/repo-files-scan.test.ts src/lib/repo-files-view.test.ts src/lib/repo-weights.test.ts src/lib/batch-create.test.ts "src/app/(panel)/models/repos/[id]/repo-detail-view.tsx"
git commit -m "feat(local-acquire): stray 扫描覆盖别的档案目录并逐分片记录"
```

---

## 任务 12：scan API

**文件：**
- 创建：`src/app/api/v1/repos/[id]/scan/route.ts`
- 修改：`src/server/repo/models.ts` 或新建 `src/server/scanDirs.ts`（读写 `settings.scan_extra_dirs`）

- [ ] **步骤 1：编写失败的测试**

```ts
// src/server/scanDirs.test.ts
import { describe, expect, it } from "vitest";
import { getConfiguredScanDirs, setConfiguredScanDirs } from "./scanDirs";

describe("scanDirs", () => {
  it("未配置时返回空数组", () => {
    const db = makeDb();
    expect(getConfiguredScanDirs(db)).toEqual([]);
  });

  it("写入后可读回，保持顺序", () => {
    const db = makeDb();
    setConfiguredScanDirs(db, ["/mnt/old/models", "/mnt/backup"]);
    expect(getConfiguredScanDirs(db)).toEqual(["/mnt/old/models", "/mnt/backup"]);
  });

  it("存的是坏 JSON 时降级为空数组，不抛错——设置项损坏不该让扫描整个报错", () => {
    const db = makeDb();
    db.prepare("INSERT INTO settings(key, value) VALUES('scan_extra_dirs', '{oops')").run();
    expect(getConfiguredScanDirs(db)).toEqual([]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/scanDirs.test.ts`
预期：FAIL，`Failed to resolve import "./scanDirs"`

- [ ] **步骤 3：编写实现**

```ts
// src/server/scanDirs.ts
/**
 * 自定义扫描目录（设计 §6.2）：宿主机视角路径数组，存 settings 表。
 * 坏 JSON 一律降级为空数组——一个损坏的设置项不该让整个扫描页报错。
 */
const KEY = "scan_extra_dirs";

export function getConfiguredScanDirs(db: Database.Database): string[] {
  const row = db.prepare("SELECT value FROM settings WHERE key = ?").get(KEY) as { value: string } | undefined;
  if (!row) return [];
  try {
    const parsed: unknown = JSON.parse(row.value);
    return Array.isArray(parsed) ? parsed.filter((x): x is string => typeof x === "string") : [];
  } catch {
    return [];
  }
}

export function setConfiguredScanDirs(db: Database.Database, dirs: readonly string[]): void {
  db.prepare("INSERT INTO settings(key, value) VALUES(?, ?) ON CONFLICT(key) DO UPDATE SET value = excluded.value")
    .run(KEY, JSON.stringify(dirs));
}
```

`src/app/api/v1/repos/[id]/scan/route.ts`：

```ts
/**
 * POST /api/v1/repos/:id/scan：深度扫描（设计 §8）。
 *
 * 与进页面时那次自动扫的区别只在范围——这里额外扫用户配置的自定义目录。
 * 结果不入库（用户决策 D2），一次性返回给前端。
 *
 * 自定义目录不可达时不算错误：返回 unreachable 清单让前端说清
 * 「该路径在面板容器内不可见，需在 docker-compose.yml 增加挂载」，
 * 而不是笼统的「目录不存在」——面板是容器，看不见宿主机大部分路径是常态。
 */
const bodySchema = z.strictObject({ extraDirs: z.array(z.string().min(1)).optional() });

export async function POST(req: Request, ctx: { params: Promise<{ id: string }> }): Promise<Response> {
  const auth = await requireAuth(req, getDb());
  if (auth instanceof Response) return auth;
  const parsed = bodySchema.safeParse(await req.json().catch(() => ({})));
  if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 });

  const db = getDb();
  const profile = getProfile(db, Number((await ctx.params).id));
  if (!profile) return NextResponse.json({ error: "NOT_FOUND" }, { status: 404 });

  const extraHostDirs = parsed.data.extraDirs ?? getConfiguredScanDirs(db);
  if (parsed.data.extraDirs) setConfiguredScanDirs(db, parsed.data.extraDirs);

  const modelsRoot = getPanelModelsRoot();
  const candidates: LocalCandidate[] = [];
  const unreachable: string[] = [];

  // models 根内：复用 scanTree + file_meta 的哈希缓存
  const metaByPath = new Map(listFileMetaRows(db).map((r) => [r.path, r.full_sha256]));
  for (const g of scanTree(modelsRoot)) {
    for (const f of g.files) {
      candidates.push({
        absPath: path.join(modelsRoot, f.rel),
        rel: f.rel,
        size: f.size,
        fullSha256: metaByPath.get(f.rel) ?? null,
        inRepoDir: repoDirOf(g.folder, listRepoDirs(db)),
        inModelsRoot: true,
      });
    }
  }

  // 自定义目录：换算成 panel 视角再扫，不可达的收进 unreachable
  for (const hostDir of extraHostDirs) {
    let panelDir: string;
    try {
      panelDir = toPanel(hostDir);
    } catch {
      unreachable.push(hostDir);
      continue;
    }
    if (!existsSync(panelDir)) {
      unreachable.push(hostDir);
      continue;
    }
    for (const g of scanTree(panelDir)) {
      for (const f of g.files) {
        candidates.push({
          absPath: path.join(panelDir, f.rel),
          rel: null,
          size: f.size,
          fullSha256: null,   // models 外不登记 file_meta，没有缓存哈希
          inRepoDir: null,
          inModelsRoot: false,
        });
      }
    }
  }

  // 按组聚合：动作是整组一起执行的，弹层一行 = 一个量化组（设计 §4.4）
  const remote = await getRemoteGroups(db, profile.repo);
  const groups = remote.groups.map((g) => {
    const files = g.files.map((rf) => {
      const candidate = matchLocalCandidate(rf, candidates);
      return { file: rf.path, candidate, ...actionsFor(rf, candidate) };
    });
    return mergeGroupMatch(g.quant, g.kind, files);
  });

  return NextResponse.json({ groups, unreachable, availableMounts: getDiscoveredMounts() });
}
```

> **实现前先对齐这些既有助手的真名与签名**，不要照抄上面的占位名——它们都已存在于代码库：
> ```bash
> grep -rn "export function listFileMeta\|full_sha256" src/server/fileMeta.ts   # 取 path → full_sha256 的映射
> grep -rn "repoDirOf\|scanRepoMarkers\|listProfiles" src/server/repoProfiles.ts src/lib/repo-path.ts  # 档案目录清单
> grep -rn "getRemoteGroups" src/server/hf/repoFiles.ts                          # 远端量化清单（带缓存）
> grep -rn "export function.*[Pp]rofile" src/server/repoProfiles.ts              # 按 id 取档案
> grep -rn "function makeDb\|openTestDb" src/server/*.test.ts                    # 测试建库助手
> ```
> 若某个能力确实没有现成函数（例如「全部档案的 targetDir 清单」），就地在
> `repoProfiles.ts` 补一个小导出，不要在 route 里内联拼 SQL。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/scanDirs.test.ts && pnpm exec tsc --noEmit`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/scanDirs.ts src/server/scanDirs.test.ts "src/app/api/v1/repos/[id]/scan/route.ts"
git commit -m "feat(local-acquire): 深度扫描接口与自定义目录配置"
```

---

## 任务 13：确认弹层行状态机

**文件：**
- 创建：`src/lib/acquire-plan.ts`、`src/lib/acquire-plan.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { buildRows, applyTaskUpdate, canSubmit, type AcquireRow } from "./acquire-plan";
import type { GroupMatch } from "./acquire-match";

const candidate = {
  absPath: "/host-models/loose/Q4_K_M.gguf", rel: "loose/Q4_K_M.gguf", size: 2600,
  fullSha256: null, inRepoDir: null, inModelsRoot: true,
};
// 一行 = 一个量化组；组内 files 是实际执行的单位
const match: GroupMatch = {
  quant: "Q4_K_M", kind: "model",
  files: [{ file: "Q4_K_M.gguf", candidate, actions: ["download", "move", "link"], defaultAction: "move", restriction: "none" }],
  actions: ["download", "move", "link"], defaultAction: "move", restriction: "none",
};

describe("buildRows", () => {
  it("按 defaultAction 预选动作", () => {
    expect(buildRows([match])[0]!.action).toBe("move");
  });

  it("无候选的组固定 download 且动作不可改", () => {
    const none: GroupMatch = {
      ...match,
      files: [{ file: "Q4_K_M.gguf", candidate: null, actions: ["download"], defaultAction: "download", restriction: "none" }],
      actions: ["download"], defaultAction: "download",
    };
    const rows = buildRows([none]);
    expect(rows[0]!.action).toBe("download");
    expect(rows[0]!.actions).toEqual(["download"]);
  });

  it("组内多个文件时行只有一行，files 全带上——提交时逐个文件发给 acquire", () => {
    const twoShards: GroupMatch = {
      ...match,
      files: [
        { file: "m-00001-of-00002.gguf", candidate, actions: ["download", "move"], defaultAction: "move", restriction: "none" },
        { file: "m-00002-of-00002.gguf", candidate, actions: ["download", "move"], defaultAction: "move", restriction: "none" },
      ],
      actions: ["download", "move"],
    };
    const rows = buildRows([twoShards]);
    expect(rows).toHaveLength(1);
    expect(rows[0]!.files).toHaveLength(2);
  });
});

describe("applyTaskUpdate", () => {
  it("下载中的任务把行推进到 executing 并带进度", () => {
    const rows = buildRows([match]);
    const next = applyTaskUpdate(rows, [{ file: "Q4_K_M.gguf", status: "downloading", downloadedBytes: 1300, totalBytes: 2600 }]);
    expect(next[0]!.phase).toBe("executing");
    expect(next[0]!.progress).toBeCloseTo(0.5);
  });

  it("失败且 error 含内容不符时，行退回可改为下载", () => {
    const rows = buildRows([match]);
    const next = applyTaskUpdate(rows, [{ file: "Q4_K_M.gguf", status: "failed", error: "内容不符：期望 sha256 …", downloadedBytes: 0, totalBytes: 2600 }]);
    expect(next[0]!.phase).toBe("failed");
    expect(next[0]!.canFallbackToDownload).toBe(true);
  });

  it("完成的行进入 done", () => {
    const rows = buildRows([match]);
    const next = applyTaskUpdate(rows, [{ file: "Q4_K_M.gguf", status: "completed", downloadedBytes: 2600, totalBytes: 2600 }]);
    expect(next[0]!.phase).toBe("done");
    expect(next[0]!.progress).toBe(1);
  });
});

describe("canSubmit", () => {
  it("全是 idle 时可提交", () => {
    expect(canSubmit(buildRows([match]))).toBe(true);
  });

  it("有行在执行中时不可重复提交", () => {
    const rows = buildRows([match]).map((r) => ({ ...r, phase: "executing" as const }));
    expect(canSubmit(rows)).toBe(false);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/acquire-plan.test.ts`
预期：FAIL，`Failed to resolve import "./acquire-plan"`

- [ ] **步骤 3：编写实现**

```ts
import type { AcquireAction, AcquireRestriction, FileMatch, GroupMatch } from "./acquire-match";

/**
 * 确认弹层的行状态机（设计 §9.1）
 *
 * 弹层里发生的事有三段：用户选动作（idle）→ 提交后服务端校验与执行（executing）→
 * 终态（done / failed）。进度直接取队列任务的 downloadedBytes / totalBytes——
 * 执行器已经把「校验 + 复制」折算成单调递增的总工作量（见 localAcquire.totalWorkOf），
 * 前端不需要知道现在是哪一阶段。
 */
export type AcquireRowPhase = "idle" | "executing" | "done" | "failed";

export interface AcquireRow {
  quant: string | null;
  kind: "model" | "mmproj";
  /** 组内各文件的匹配结果——动作按组选，提交时逐个文件发给 acquire */
  files: readonly FileMatch[];
  action: AcquireAction;
  actions: readonly AcquireAction[];
  restriction: AcquireRestriction;
  phase: AcquireRowPhase;
  progress: number | null;
  error: string | null;
  /** 失败后是否可以一键改为下载（校验不过的行都可以） */
  canFallbackToDownload: boolean;
}

/** 队列任务的一次状态推送（来自已有的 downloads SSE），按远端文件名对齐到组 */
export interface TaskUpdate {
  file: string;
  status: string;
  downloadedBytes: number;
  totalBytes: number;
  error?: string;
}

export function buildRows(groups: readonly GroupMatch[]): AcquireRow[] {
  return groups.map((g) => ({
    quant: g.quant,
    kind: g.kind,
    files: g.files,
    action: g.defaultAction,
    actions: g.actions,
    restriction: g.restriction,
    phase: "idle",
    progress: null,
    error: null,
    canFallbackToDownload: false,
  }));
}

/**
 * 把队列推送折算到组：组内多个文件各有一个任务，组的进度是它们的加权和，
 * 组的阶段取最"未完成"的那个——只要还有一片在跑，整组就是 executing。
 */
export function applyTaskUpdate(rows: readonly AcquireRow[], updates: readonly TaskUpdate[]): AcquireRow[] {
  const byFile = new Map(updates.map((u) => [u.file, u]));
  return rows.map((row) => {
    const mine = row.files.map((f) => byFile.get(f.file)).filter((u): u is TaskUpdate => u !== undefined);
    if (mine.length === 0) return row;

    const failed = mine.find((u) => u.status === "failed" || u.status === "cancelled");
    if (failed) {
      return {
        ...row,
        phase: "failed",
        error: failed.error ?? null,
        // 本地获取失败一律可退回下载——下载是永远可行的兜底
        canFallbackToDownload: row.action !== "download",
      };
    }

    const done = mine.length === row.files.length && mine.every((u) => u.status === "completed");
    if (done) return { ...row, phase: "done", progress: 1, error: null };

    const downloaded = mine.reduce((sum, u) => sum + u.downloadedBytes, 0);
    const total = mine.reduce((sum, u) => sum + u.totalBytes, 0);
    return { ...row, phase: "executing", progress: total > 0 ? downloaded / total : null };
  });
}

/** 有任何一行在执行中就不许再提交，防重复入队 */
export function canSubmit(rows: readonly AcquireRow[]): boolean {
  return rows.length > 0 && rows.every((r) => r.phase === "idle" || r.phase === "failed");
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/acquire-plan.test.ts`
预期：PASS，9 个用例

- [ ] **步骤 5：Commit**

```bash
git add src/lib/acquire-plan.ts src/lib/acquire-plan.test.ts
git commit -m "feat(local-acquire): 确认弹层行状态机"
```

---

## 任务 14：确认弹层组件

**文件：**
- 创建：`src/app/(panel)/models/repos/[id]/acquire-dialog.tsx`
- 修改：`src/i18n/messages/zh.json`、`src/i18n/messages/en.json`

> 组件无法单测（无 jsdom），本任务的正确性由任务 13 的状态机测试 + `tsc` + `build` +
> 步骤 4 的真机核对项共同保证。**不要为它写形同虚设的组件测试。**

- [ ] **步骤 1：补 i18n 文案（zh 与 en 必须逐键对称）**

`zh.json` 的 `pages.repos` 下追加：

```json
"acquireTitle": "确认获取方式",
"acquireDesc": "本地已有的文件可以直接挪进档案目录，不必重新下载。确认后才会开始执行。",
"acquireActionDownload": "下载",
"acquireActionMove": "移动",
"acquireActionLink": "链接",
"acquireActionCopy": "复制",
"acquireHintMove": "把文件挪进档案目录，原位置不再保留",
"acquireHintLink": "创建硬链接，两处共用同一份数据，不额外占盘",
"acquireHintCopy": "复制一份到档案目录，原文件保留",
"acquireSourceAt": "来自 {path}",
"acquireRestrictionNoOid": "该文件在远端没有内容校验值，无法确认本地文件是否为同一份，只能下载",
"acquireRestrictionInRepo": "文件属于档案 {repo}，移走会掏空那个档案，只能创建链接",
"acquireRestrictionOutside": "文件在 models 目录之外，跨文件系统无法移动或链接",
"acquirePhaseExecuting": "处理中",
"acquirePhaseDone": "已完成",
"acquireErrorMismatch": "内容与远端不符，已停止；可改为下载",
"acquireFallbackDownload": "改为下载",
"acquireSubmit": "确认执行",
"acquireLinkDeleteNote": "删除链接不会释放磁盘空间，另一处仍占用",
"acquireLinkSharedWith": "与 {path} 共用同一份数据",
"scanAction": "扫描",
"scanning": "扫描中…",
"scanExtraDirLabel": "额外扫描目录（宿主机路径，可留空）",
"scanUnreachable": "以下路径在面板容器内不可见，需要在 docker-compose.yml 增加对应挂载后重启面板：{paths}",
"scanAvailableMounts": "当前可访问的宿主机路径：{paths}"
```

`en.json` 同键位补英文，**键路径逐字一致**。

- [ ] **步骤 2：编写组件**

```tsx
"use client";

/**
 * 获取确认弹层（设计 §9.1）
 *
 * 用户点「下载选中项」后先进这里，逐行确认用什么手段拿到文件——本地已有的
 * 可以移动 / 链接 / 复制，本地没有的只能下载。确认后才真正入队。
 *
 * 行状态与进度全部由 lib/acquire-plan.ts 计算，本组件只管渲染与事件：
 * 这是项目的既定分工（vitest 无 jsdom，判定不下沉就没有测试）。
 */
export function AcquireDialog({ open, rows, onOpenChange, onChangeAction, onSubmit }: {
  open: boolean;
  rows: AcquireRow[];
  onOpenChange: (open: boolean) => void;
  onChangeAction: (file: string, action: AcquireAction) => void;
  onSubmit: () => void;
}): React.ReactElement {
  const t = useTranslations("pages.repos");
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-w-2xl">
        <DialogHeader>
          <DialogTitle>{t("acquireTitle")}</DialogTitle>
          <DialogDescription>{t("acquireDesc")}</DialogDescription>
        </DialogHeader>

        <div className="max-h-[60vh] space-y-2 overflow-y-auto">
          {rows.map((row) => (
            <div key={row.file} className="rounded-md border p-3">
              <div className="flex items-center justify-between gap-3">
                <span className="font-mono text-xs break-all">{row.file}</span>
                {row.actions.length > 1 ? (
                  <Select
                    value={row.action}
                    onValueChange={(v) => onChangeAction(row.file, v as AcquireAction)}
                    disabled={row.phase === "executing"}
                  >
                    {row.actions.map((a) => (
                      <SelectItem key={a} value={a}>
                        {t(`acquireAction${a[0]!.toUpperCase()}${a.slice(1)}`)}
                      </SelectItem>
                    ))}
                  </Select>
                ) : (
                  <span className="text-muted-foreground text-xs">{t("acquireActionDownload")}</span>
                )}
              </div>

              {row.candidate !== null && (
                <p className="text-muted-foreground mt-1 text-xs">
                  {t("acquireSourceAt", { path: row.candidate.rel ?? row.candidate.absPath })}
                </p>
              )}
              {row.restriction !== "none" && (
                <p className="text-muted-foreground mt-1 text-xs">{restrictionText(t, row)}</p>
              )}
              {row.phase === "executing" && row.progress !== null && (
                <Progress value={row.progress * 100} className="mt-2" />
              )}
              {row.phase === "failed" && (
                <div className="mt-2 flex items-center gap-2">
                  <span className="text-destructive text-xs">{row.error ?? t("acquireErrorMismatch")}</span>
                  {row.canFallbackToDownload && (
                    <Button size="sm" variant="outline" onClick={() => onChangeAction(row.file, "download")}>
                      {t("acquireFallbackDownload")}
                    </Button>
                  )}
                </div>
              )}
            </div>
          ))}
        </div>

        <DialogFooter>
          <Button onClick={onSubmit} disabled={!canSubmit(rows)}>
            {t("acquireSubmit")}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

> `Select` / `Progress` / `Dialog` 的确切用法以项目里已有的用例为准——
> 动手前先看 `repo-dialogs.tsx` 的 `MoveDialog`（同页面、同款 Select + Dialog 组合），
> 照它的写法来，不要按记忆里的 shadcn 通用写法猜。

- [ ] **步骤 3：类型与构建检查**

运行：`pnpm exec tsc --noEmit && pnpm run lint`
预期：零错误。若 `Select` 的 props 形状与 `repo-dialogs.tsx` 里的用法不一致，以后者为准

- [ ] **步骤 4：构建验证**

运行：`pnpm run build`
预期：构建成功。**真机核对项**（留给批 ③ 收尾一起做）：弹层能开、下拉能切、
禁用态正确、进度条会动、失败行的「改为下载」可点

- [ ] **步骤 5：Commit**

```bash
git add "src/app/(panel)/models/repos/[id]/acquire-dialog.tsx" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(local-acquire): 获取确认弹层组件与文案"
```

---

## 任务 15：档案页接线

**文件：**
- 修改：`src/app/(panel)/models/repos/[id]/repo-detail-view.tsx`

- [ ] **步骤 1：接入扫描按钮**

加「扫描」按钮，点击后 `POST /api/v1/repos/:id/scan`，可展开一个输入框填额外目录
（宿主机路径，逗号分隔）。返回的 `unreachable` 非空时在顶部显示 `scanUnreachable`
提示条并附 `scanAvailableMounts`。

⚠️ **落点不是「刷新」按钮旁边**。按 `7f82cb9` 复核：那个「刷新」按钮连同它所在的
整条远端状态行都包在 `{data.remote.ok && (…)}` 里（`repo-detail-view.tsx:507-531`），
远端不可达时整行不渲染。而**远端不可达恰恰是本地迁移最有价值的场景**（国内网络下是常态：
HF 打不开、但盘上已经躺着一份权重），扫描按钮跟着消失就等于把功能关在门外。

改法：把扫描按钮放进 `{data.remote.ok && …}` **之外**的一行——摆在 `summaryLine`
那条 `<p>`（`repo-detail-view.tsx:534-541`）所在的位置，把它和扫描按钮包成一个
`flex items-center justify-between` 行，摘要在左、扫描在右。这样两种远端状态下按钮都在。
额外目录输入框展开后占它下面一整行。

- [ ] **步骤 2：替换「下载选中项」的行为**

```tsx
// 原来：点击直接 POST /repos/:id/download
// 现在：先用扫描结果构造弹层行（一行 = 一个量化组），确认后才 POST /repos/:id/acquire
async function onDownloadSelected(): Promise<void> {
  const picked = [...selected].map((i) => rows[i]!);
  const groups = (scanResult?.groups ?? []).filter((g) =>
    picked.some((r) => r.quant === g.quant && r.kind === g.kind),
  );
  setAcquireRows(buildRows(groups));
  setAcquireOpen(true);
}

async function onAcquireSubmit(): Promise<void> {
  // 组展开成文件：动作是组级选的，但请求体逐文件——服务端要对每个文件
  // 单独重验并各建一个队列任务（设计 §4.4）
  const items = acquireRows.flatMap((row) =>
    row.files.map((f) => ({
      file: f.file,
      action: row.action,
      // 宿主机视角回传，服务端 toPanel 换算后重验（设计 §8.1）。
      // 组级动作是 download 时不传源，服务端也不会去读
      sourceHostPath:
        row.action === "download" || f.candidate === null ? undefined : f.candidate.hostPath,
    })),
  );
  await apiFetch(`/api/v1/repos/${id}/acquire`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ items }),
  });
  // 成功后靠已有的 downloads SSE 订阅推动 applyTaskUpdate 刷新行状态
}
```

> `f.candidate.hostPath` 要求 `LocalCandidate` 带一个宿主机视角字段。任务 12 的 scan API
> 组装候选时用 `toHost(absPath)` 填上它——前端展示与回传都用宿主机路径（项目约定），
> panel 视角只在服务端内部流转。实现任务 12 时一并把这个字段加进 `LocalCandidate`
> （任务 1 定义处补 `hostPath: string`，并更新那 3 个构造候选的测试夹具）。

- [ ] **步骤 3：接入进度更新与共用标注**

复用页面已有的下载 SSE 订阅，把任务更新喂给 `applyTaskUpdate(acquireRows, updates)`。
`strayRel` → `strayRels` 的重命名在任务 11 已改，此处确认归位按钮仍可用（它现在只是
弹层之外的快捷入口，行为不变）。

**共用标注（设计 §9.1）**：`GET /repos/:id/files` 的响应给每个本地文件补
`sharedWith: string[]`（同 inode 的其他路径，数据来自任务 3 加的 `ModelFile.ino`），
硬链接来的文件显示 `acquireLinkSharedWith` 标签；该文件的删除确认框里追加
`acquireLinkDeleteNote`「删除链接不会释放磁盘空间，另一处仍占用」——用户删掉 3 GB
却发现盘没变，这个困惑必须提前说清。文案键在任务 14 已一并加好。

标签**放在徽章行**（`repo-detail-view.tsx:757-778`，量化 / mmproj / 分片数那一行，
大小读数之前），沿用同一套 `<Badge variant="outline" className="h-4.5 px-1.5 …text-[10px]">`
形态，加一个 `Link2` 图标。不要放进操作段——那里现在是「创建配置 / 归位 / `StrayMark`」
三个动作元素，塞一个纯展示标签进去会打乱「这一段全是可点的」这个预期。共用路径本身走
Tooltip，与 `StrayMark` 同一个套路，别让它把窄卡撑宽。

- [ ] **步骤 4：验证**

运行：`pnpm exec tsc --noEmit && pnpm run lint && pnpm run build && pnpm test`
预期：全绿

- [ ] **步骤 5：Commit**

```bash
git add "src/app/(panel)/models/repos/[id]/repo-detail-view.tsx"
git commit -m "feat(local-acquire): 档案页接入扫描与获取确认流程"
```

- [ ] **批 ③ 收尾：真机核对**

1. 档案里一个量化已在 models 别处 → 扫描后行显示「在别处」且可勾选
2. 同一份 mmproj 已在另一个档案目录内 → 默认「链接」，执行后两档案都有该文件、磁盘占用不增
3. 故意改坏一个同名同大小的文件 → 确认后报「内容不符」，可一键改为下载
4. 自定义目录填一个没挂载的路径 → 提示「容器内不可见」并列出可用路径

---

# 批 ④ 游离视图

## 任务 16：文件选择弹层的游离徽标

**文件：**
- 修改：`src/components/models/model-file-picker.tsx:174`
- 修改：`src/i18n/messages/{zh,en}.json`

- [ ] **步骤 1：补文案**

`zh.json` 的 picker 文案域下加 `"unused": "未被使用"`，`en.json` 加 `"unused": "Unused"`。

- [ ] **步骤 2：改渲染**

```tsx
{item.refs > 0 ? (
  <span>{t("refs", { count: item.refs })}</span>
) : (
  // refs === 0 的文件不是「没信息」而是「还没人用」，这正是用户来选文件时
  // 最想先看到的那批——之前它们与已被占用的文件长得一模一样
  <Badge variant="outline" className="text-muted-foreground">{t("unused")}</Badge>
)}
```

注意现状是 `{item.refs > 0 && (…)}` 的短路写法，要改成三元，否则 refs===0 时什么都不渲染。

- [ ] **步骤 3：验证**

运行：`pnpm exec tsc --noEmit && pnpm run lint && pnpm run build`
预期：全绿

- [ ] **步骤 4：Commit**

```bash
git add src/components/models/model-file-picker.tsx src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(local-acquire): 文件选择弹层标出未被使用的文件"
```

---

## 任务 17：游离清单派生与接口

**文件：**
- 创建：`src/lib/unclaimed-view.ts`、`src/lib/unclaimed-view.test.ts`
- 创建：`src/app/api/v1/files/unclaimed/route.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { deriveUnclaimed } from "./unclaimed-view";

const tree = [
  { folder: "hf/o/R", files: [{ rel: "hf/o/R/a.gguf", size: 100, mtime: 0, ino: 1 }] },
  { folder: "loose", files: [
    { rel: "loose/b.gguf", size: 200, mtime: 0, ino: 2 },
    { rel: "loose/linked.gguf", size: 100, mtime: 0, ino: 1 },
  ] },
];

describe("deriveUnclaimed", () => {
  it("被引用的文件不算游离", () => {
    const got = deriveUnclaimed(tree, new Set(["loose/b.gguf"]), ["hf/o/R"], new Set());
    expect(got.map((f) => f.rel)).toEqual(["hf/o/R/a.gguf", "loose/linked.gguf"]);
  });

  it("标出所属档案目录", () => {
    const got = deriveUnclaimed(tree, new Set(), ["hf/o/R"], new Set());
    expect(got.find((f) => f.rel === "hf/o/R/a.gguf")!.inRepoDir).toBe("hf/o/R");
    expect(got.find((f) => f.rel === "loose/b.gguf")!.inRepoDir).toBeNull();
  });

  it("同 inode 的文件互相标为共用", () => {
    const got = deriveUnclaimed(tree, new Set(), ["hf/o/R"], new Set());
    expect(got.find((f) => f.rel === "loose/linked.gguf")!.sharedWith).toEqual(["hf/o/R/a.gguf"]);
  });

  it("有 file_meta 记录的标 hasMeta", () => {
    const got = deriveUnclaimed(tree, new Set(), ["hf/o/R"], new Set(["loose/b.gguf"]));
    expect(got.find((f) => f.rel === "loose/b.gguf")!.hasMeta).toBe(true);
  });

  it("非 .gguf 文件不进清单——游离视图只关心权重", () => {
    const withReadme = [{ folder: "loose", files: [{ rel: "loose/README.md", size: 10, mtime: 0, ino: 9 }] }];
    expect(deriveUnclaimed(withReadme, new Set(), [], new Set())).toEqual([]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/unclaimed-view.test.ts`
预期：FAIL，`Failed to resolve import "./unclaimed-view"`

- [ ] **步骤 3：编写实现**

```ts
import { repoDirOf } from "./repo-path";

/**
 * 游离文件清单派生（设计 §4.1 / §9.3）
 *
 * 游离 = refs === 0，附 inRepoDir / hasMeta 两个正交标签。
 * 硬链接产生的第二个路径同样算游离——它确实还没被任何配置引用；
 * 用 sharedWith 标出与它共用同一 inode 的其他路径，供 UI 提示
 * 「删除链接不会释放磁盘空间」。
 */
export interface UnclaimedFile {
  rel: string;
  size: number;
  ino: number;
  inRepoDir: string | null;
  hasMeta: boolean;
  /** 与本文件共用同一 inode 的其他路径 */
  sharedWith: string[];
}

export interface ScanNode {
  folder: string;
  files: { rel: string; size: number; mtime: number; ino: number }[];
}

export function deriveUnclaimed(
  tree: readonly ScanNode[],
  referenced: ReadonlySet<string>,
  repoDirs: readonly string[],
  metaPaths: ReadonlySet<string>,
): UnclaimedFile[] {
  // inode → 全部路径：共用关系要看全树，不能只看游离的那部分
  const byIno = new Map<number, string[]>();
  for (const g of tree) {
    for (const f of g.files) {
      const list = byIno.get(f.ino);
      if (list) list.push(f.rel);
      else byIno.set(f.ino, [f.rel]);
    }
  }

  const out: UnclaimedFile[] = [];
  for (const g of tree) {
    for (const f of g.files) {
      if (!f.rel.endsWith(".gguf")) continue;
      if (referenced.has(f.rel)) continue;
      out.push({
        rel: f.rel,
        size: f.size,
        ino: f.ino,
        inRepoDir: repoDirOf(g.folder, repoDirs),
        hasMeta: metaPaths.has(f.rel),
        sharedWith: (byIno.get(f.ino) ?? []).filter((r) => r !== f.rel),
      });
    }
  }
  return out;
}
```

`src/app/api/v1/files/unclaimed/route.ts` 是薄壳：`scanTree` + `buildRefMap` +
`listRepoDirs` + `file_meta` 的 path 集合，喂给 `deriveUnclaimed` 后返回。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/unclaimed-view.test.ts && pnpm exec tsc --noEmit`
预期：PASS，5 个用例

- [ ] **步骤 5：Commit**

```bash
git add src/lib/unclaimed-view.ts src/lib/unclaimed-view.test.ts src/app/api/v1/files/unclaimed/route.ts
git commit -m "feat(local-acquire): 游离文件清单派生与接口"
```

---

## 任务 18：/files 未登记视图

**文件：**
- 创建：`src/app/(panel)/files/unclaimed-table.tsx`
- 修改：`src/app/(panel)/files/page.tsx:143`（二级栏第三格）
- 修改：`src/i18n/messages/{zh,en}.json`

- [ ] **步骤 1：补文案**

`pages.files` 下追加：

```json
"navUnclaimed": "未登记",
"navUnclaimedTooltip": "{count} 个权重文件还没有对应的模型配置",
"unclaimedTitle": "未登记文件",
"unclaimedSummary": "{count} 个 · {size}",
"unclaimedEmpty": "没有未登记的权重文件",
"unclaimedBadgeInRepo": "档案内",
"unclaimedBadgeLoose": "游离",
"unclaimedBadgeHasMeta": "有备注",
"unclaimedSharedWith": "与 {path} 共用同一份数据",
"unclaimedActionCreate": "建配置",
"unclaimedActionRelocate": "归位到档案",
"unclaimedRelocateDisabled": "文件已在档案目录内，无需归位"
```

- [ ] **步骤 2：二级栏加第三格**

`page.tsx` 的 `navItems` 数组在 `FILES_VIEW_META_KEY` 那格之后追加：

```ts
    {
      key: FILES_VIEW_UNCLAIMED_KEY,
      name: t("navUnclaimed"),
      lead: { kind: "count" as const, value: unclaimed.length },
      meta: formatSize(unclaimed.reduce((s, f) => s + f.size, 0)),
      marker:
        unclaimed.length > 0
          ? { tone: "alert" as const, title: t("navUnclaimedTooltip", { count: unclaimed.length }) }
          : undefined,
    },
```

`groups` 数组同步加一条分隔：`{ beforeKey: FILES_VIEW_UNCLAIMED_KEY, label: "UNCLAIMED" }`。

- [ ] **步骤 3：编写表格组件**

每行渲染：文件名 + 大小 + 所在目录 + 标签（`档案内` / `游离` / `有备注` / `与 X 共用`）
+ 两个动作按钮。

- **建配置**：`router.push(\`/models/new?file=${encodeURIComponent(rel)}&step=2\`)`（现成深链）
- **归位到档案**：下拉选目标档案 → `POST /api/v1/files/move`。
  **`inRepoDir !== null` 的行必须禁用此按钮**并给 `unclaimedRelocateDisabled` 提示——
  `planFileMove` 拒绝从档案目录移出（`filesApi.ts:556`），给了必然 400

- [ ] **步骤 4：验证**

运行：`pnpm test && pnpm exec tsc --noEmit && pnpm run lint && pnpm run build`
预期：全绿

- [ ] **步骤 5：Commit**

```bash
git add "src/app/(panel)/files/unclaimed-table.tsx" "src/app/(panel)/files/page.tsx" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(local-acquire): 文件页未登记视图"
```

---

# 全批收尾

- [ ] **跑全套四件套**

```bash
pnpm test && pnpm run lint && pnpm exec tsc --noEmit && pnpm run build
```

- [ ] **按设计文档 §13 逐条真机验收**（10 项，见
  `docs/superpowers/specs/2026-09-03-本地权重迁移-design.md`）

- [ ] **回写设计文档的实施补记**：把施工中推翻或补充的决策写回 spec，
  并在 `docs/_internal/TASKS.md` 追加变更记录
