# 模型首页与配置路由迁移 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 `/models` 腾出来做模型区首页（正在运行 + 最近更新的仓库），配置列表迁到 `/models/profiles`，概览页那张运行卡精简并引流到首页。

**架构：** 二级栏从两组 tab 扩成三组（首页 / HuggingFace / 配置），判定仍集中在 `lib/models-tabs.ts`。首页是 server 组件，一次装配 `decorateRuntimeStatus` + `decorateProfileStats` + `scanTree`，交给两个展示区渲染。「最近更新」口径落在 `decorateProfileStats` 里顺带算出的最大 mtime，排序与截断下沉 `lib/recent-repos.ts`。

**技术栈：** Next.js App Router（server 组件 + `"use client"` 展示组件）、next-intl、Tailwind + shadcn/ui、vitest（node 环境，纯逻辑测试）。

**设计文档：** `docs/superpowers/specs/2026-09-21-模型首页与配置路由迁移-design.md`

---

## 文件结构

**新建**

| 文件 | 职责 |
|---|---|
| `src/app/(panel)/models/profiles/page.tsx` | 配置列表页，由现 `models/page.tsx` 整体迁入 |
| `src/app/(panel)/models/page.tsx`（改写） | 模型首页：二级栏 + 页头 + 两个展示区 |
| `src/app/(panel)/models/home-running.tsx` | 首页「正在运行」区（client，含操作按钮） |
| `src/app/(panel)/models/home-repos.tsx` | 首页「最近更新的仓库」区（client） |
| `src/components/repo-card.tsx` | 仓库卡片，首页与档案页共用 |
| `src/lib/recent-repos.ts` + `.test.ts` | 按最近更新排序并截断的纯函数 |

**修改**

| 文件 | 改动 |
|---|---|
| `src/lib/models-tabs.ts` + `.test.ts` | `ModelsTab` 加 `home`，三条 tab，判定改三步 |
| `src/server/repoProfiles.ts` + `.test.ts` | `RepoProfileStats` 加 `lastModified` |
| `src/app/(panel)/models/repos/repos-view.tsx` | 内联的 `RepoCard` 改为引用共享组件 |
| `src/app/(panel)/page.tsx` | 运行卡精简 + 虚线空态面板 |
| 9 处跳转点 | 见任务 1 步骤 6 的清单 |
| `src/i18n/messages/{zh,en}.json` | 新增首页与 tab 文案 |

**不动**：一切非路由的 `/models` 字面量（`model_mount`、`server/runtime.ts`、`server/doctor.test.ts`、`lib/proxy-rewrite.test.ts`、`lib/image-card-form.test.ts`、`core/schemas.ts`、`settings/custom-image-card.tsx`）——它们是容器内挂载点，与路由无关。

---

### 任务 1：配置列表迁到 `/models/profiles`，`/models` 换成首页骨架

一个任务内完成，因为拆开会留下坏掉的中间态（tab 指向不存在的路由，或 `/models` 404）。交付物：三个页面都能打开，二级栏三格互相跳得通，首页暂时只有页头与两个空壳区。

**文件：**
- 修改：`src/lib/models-tabs.ts`
- 测试：`src/lib/models-tabs.test.ts`
- 创建：`src/app/(panel)/models/profiles/page.tsx`
- 改写：`src/app/(panel)/models/page.tsx`
- 修改：`src/i18n/messages/zh.json`、`src/i18n/messages/en.json`
- 修改：9 处跳转点（见步骤 6）

- [ ] **步骤 1：改测试，先把三组 tab 的期望写下来**

替换 `src/lib/models-tabs.test.ts` 中 `resolveModelsTab` 与 `MODELS_TABS` 两个 describe 块：

```ts
describe("resolveModelsTab", () => {
  it("/models 落 home（只有恰好等于才算）", () => {
    expect(resolveModelsTab("/models")).toBe("home");
  });

  it("/models/profiles 落 configs", () => {
    expect(resolveModelsTab("/models/profiles")).toBe("configs");
  });

  it("/models/repos 落 repos", () => {
    expect(resolveModelsTab("/models/repos")).toBe("repos");
  });

  it("/models/repos/12（详情页）落 repos", () => {
    expect(resolveModelsTab("/models/repos/12")).toBe("repos");
  });

  it("/models/new 落 configs——编辑流程中二级栏该高亮「配置」而不是「首页」", () => {
    expect(resolveModelsTab("/models/new")).toBe("configs");
  });

  it("/models/llama3/edit 落 configs（同上）", () => {
    expect(resolveModelsTab("/models/llama3/edit")).toBe("configs");
  });

  it("/models/repos-other 落 configs（前缀不是目录边界）", () => {
    expect(resolveModelsTab("/models/repos-other")).toBe("configs");
  });
});

describe("MODELS_TABS", () => {
  it("编号固定 01–03，与 key 顺序一一对应（固定有序集合的前导位语义）", () => {
    expect(MODELS_TABS.map((tab) => [tab.key, tab.number, tab.href])).toEqual([
      ["home", "01", "/models"],
      ["repos", "02", "/models/repos"],
      ["configs", "03", "/models/profiles"],
    ]);
  });
});
```

同文件 `buildModelsTabItems` 的三个用例改为：

```ts
describe("buildModelsTabItems", () => {
  it("按 pathname 判定 selected，而不是要求调用方自己算好", () => {
    const items = buildModelsTabItems("/models", stubT);
    expect(items.map((i) => [i.key, i.selected])).toEqual([
      ["home", true],
      ["repos", false],
      ["configs", false],
    ]);
  });

  it("/models/repos 与其详情页子路由都落 repos 选中", () => {
    expect(buildModelsTabItems("/models/repos", stubT).find((i) => i.key === "repos")?.selected).toBe(true);
    expect(buildModelsTabItems("/models/repos/12", stubT).find((i) => i.key === "repos")?.selected).toBe(true);
  });

  it("name/meta 经 t() 取值，键名带 tabs.<key> 前缀", () => {
    const items = buildModelsTabItems("/models/profiles", stubT);
    expect(items.find((i) => i.key === "configs")).toMatchObject({
      name: "t:tabs.configs.name",
      meta: "t:tabs.configs.meta",
    });
  });

  it("href 与编号原样透传自 MODELS_TABS", () => {
    const items = buildModelsTabItems("/models", stubT);
    expect(items.map((i) => [i.href, i.lead.text])).toEqual([
      ["/models", "01"],
      ["/models/repos", "02"],
      ["/models/profiles", "03"],
    ]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/models-tabs.test.ts`
预期：FAIL，`resolveModelsTab("/models")` 返回 `"configs"` 而非 `"home"`，`MODELS_TABS` 只有两条。

- [ ] **步骤 3：改 `src/lib/models-tabs.ts`**

```ts
export type ModelsTab = "home" | "repos" | "configs";

export const MODELS_TABS: ReadonlyArray<{ key: ModelsTab; number: string; href: string }> = [
  { key: "home", number: "01", href: "/models" },
  { key: "repos", number: "02", href: "/models/repos" },
  { key: "configs", number: "03", href: "/models/profiles" },
];

// 兜底方向是 configs 而不是 home：配置的子路由（/models/new、/models/<name>/edit
// 与 /duplicate）都该让二级栏高亮「配置」——用户正在编辑一个配置，不是在看首页。
// 所以 home 必须是精确匹配，不能写成"其余全部"
export const DEFAULT_MODELS_TAB: ModelsTab = "configs";

export function resolveModelsTab(pathname: string): ModelsTab {
  if (pathname === "/models/repos" || pathname.startsWith("/models/repos/")) return "repos";
  if (pathname === "/models") return "home";
  return DEFAULT_MODELS_TAB;
}
```

文件头注释里「两组」的表述同步改成三组，并说明顺序是首页 / HuggingFace / 配置。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/models-tabs.test.ts`
预期：PASS。

- [ ] **步骤 5：迁配置页，建首页骨架**

把现 `src/app/(panel)/models/page.tsx` **整体**移到 `src/app/(panel)/models/profiles/page.tsx`，内部只改三处：

1. `buildModelsTabItems("/models", t)` → `buildModelsTabItems("/models/profiles", t)`
2. 同目录 client 组件的 import 路径加一层 `../`（`./models-table` → `../models-table`，`./namespace-create-nav-button`、`./empty-state-actions`、`./repo-create-nav-button` 同理）
3. 文件头注释里的路由表述改成 `/models/profiles`

这些 client 组件**留在 `models/` 目录不动**：`models-table.tsx` 等文件名已经表明归属，再挪一层目录只会让 diff 变大、且首页后续也可能复用其中一部分。

新建 `src/app/(panel)/models/page.tsx`（首页骨架，两个区本任务先留空壳，任务 3/4 填充）：

```tsx
import { Box } from "lucide-react";
import { getTranslations } from "next-intl/server";

import { PageHeader } from "@/components/shell/page-header";
import { SecondaryNav } from "@/components/shell/secondary-nav";
import { buildModelsTabItems } from "@/lib/models-tabs";

// db + 运行状态 + 文件扫描（fs）→ 全动态渲染
export const dynamic = "force-dynamic";

/**
 * 模型区首页（2026-09-21 设计 §3）：进入模型区最先想知道的两件事——哪些模型
 * 在跑、手上有哪些仓库。配置列表已迁往 /models/profiles，本页不按命名空间切片。
 */
export default async function ModelsHomePage() {
  const t = await getTranslations("pages.models");
  const tHome = await getTranslations("pages.modelsHome");
  const tabItems = buildModelsTabItems("/models", t);

  return (
    // 负边距与定高：与 profiles/repos 两页同款过渡做法，见那两处的同款注释
    <div className="-mx-[34px] -mt-7 -mb-12 flex h-[calc(100%+76px)]">
      <SecondaryNav
        kicker="MODELS"
        title={t("title")}
        items={tabItems}
        // 本页没有第二层可选集合，queryKey/current 只是满足 props 契约——
        // items 全是 href 型，选中态由各自的 selected 覆盖决定
        queryKey="tab"
        current="home"
      />
      <div className="flex min-w-0 flex-1 flex-col">
        <PageHeader icon={Box} title={tHome("title")} subtitle={tHome("subtitle")} stats={[]} />
        <div className="min-h-0 flex-1 overflow-y-auto" />
      </div>
    </div>
  );
}
```

- [ ] **步骤 6：改 9 处跳转点**

全部改成 `/models/profiles`（语义都是「回到 / 去往配置列表」）：

| 文件:行 | 现状 |
|---|---|
| `src/app/(panel)/models/[name]/edit/edit-form.tsx:204` | `router.push("/models")` |
| `src/app/(panel)/models/[name]/edit/edit-form.tsx:274` | `render={<Link href="/models" />}` |
| `src/app/(panel)/models/new/wizard.tsx:247` | `router.push("/models")` |
| `src/app/(panel)/models/new/wizard.tsx:382` | `render={<Link href="/models" />}` |
| `src/app/(panel)/models/[name]/duplicate/duplicate-form.tsx:190` | `render={<Link href="/models" />}` |
| `src/app/(panel)/page.tsx:238` | `render={<Link href="/models" />}`（任务 5 会再改这块，此处先随大流改掉） |
| `src/app/(panel)/chat/page.tsx:136` | `render={<Link href="/models" />}` |
| `src/app/api/v1/proxy/llama/[[...path]]/route.ts:135,140` | `hint: "/models"`（两处，含第 46 行文档注释里的同一串） |
| `src/lib/onboarding.ts:35` | `{ id: "start", href: "/models" }` |

`src/components/shell/sidebar.tsx:70` 的 `{ href: "/models", … }` **保持不变**——它是模型区入口，应落到首页；选中态走 `startsWith("/models")`，三个页面都覆盖。

`src/lib/onboarding.test.ts:29` 的期望数组同步改为 `["/settings?tab=library", "/models/new", "/models/profiles", "/chat"]`。

- [ ] **步骤 7：补文案**

`src/i18n/messages/zh.json` 的 `pages.models.tabs` 加一条，并新建 `pages.modelsHome`：

```json
"tabs": {
  "home": { "name": "首页", "meta": "运行中与仓库" },
  "configs": { "name": "配置", "meta": "模型配置列表" },
  "repos": { "name": "HuggingFace", "meta": "仓库下载与文件状态" }
}
```

```json
"modelsHome": {
  "title": "模型",
  "subtitle": "运行状态与仓库概览",
  "statRunning": "运行中",
  "statConfigs": "配置",
  "statRepos": "仓库"
}
```

`en.json` 对称：`home` → `{ "name": "Home", "meta": "Running & repos" }`；`modelsHome` → `{ "title": "Models", "subtitle": "Runtime status and repo overview", "statRunning": "Running", "statConfigs": "Configs", "statRepos": "Repos" }`。

两份文件的 key 必须严格对称。

- [ ] **步骤 8：验证**

```bash
pnpm exec vitest run src/lib/models-tabs.test.ts src/lib/onboarding.test.ts
pnpm exec tsc --noEmit
pnpm run lint
```
预期：测试 PASS，tsc 与 lint 无输出。

再全量跑一次确认没碰坏别处：`pnpm test`

- [ ] **步骤 9：Commit**

```bash
git add src/lib/models-tabs.ts src/lib/models-tabs.test.ts src/lib/onboarding.ts src/lib/onboarding.test.ts "src/app/(panel)/models" "src/app/(panel)/page.tsx" "src/app/(panel)/chat/page.tsx" "src/app/api/v1/proxy/llama" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "refactor(models): 配置列表迁到 /models/profiles，/models 改为模型首页"
```

---

### 任务 2：档案带上「最近更新」时间

**文件：**
- 修改：`src/server/repoProfiles.ts`（`RepoProfileStats` 接口与 `decorateProfileStats`）
- 测试：`src/server/repoProfiles.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `src/server/repoProfiles.test.ts` 的 `describe("decorateProfileStats")` 内追加。构造 tree 的写法照抄该 describe 内既有用例（`folder` + `files[{ rel, size, mtime, ino }]`）：

```ts
it("lastModified 取档案目录内文件的最大 mtime", () => {
  const profile = { id: 1, repo: "o/R", baseDir: "hf", targetDir: "hf/o/R", createdAt: 100 };
  const tree = [
    {
      folder: "hf/o/R",
      files: [
        { rel: "hf/o/R/a.gguf", size: 10, mtime: 5_000, ino: 1 },
        { rel: "hf/o/R/b.gguf", size: 20, mtime: 9_000, ino: 2 },
      ],
    },
  ];
  const [stats] = decorateProfileStats([profile], tree);
  expect(stats!.lastModified).toBe(9_000);
});

it("文件 mtime 早于档案创建时间时仍用 mtime（拷入的旧文件保留原 mtime，是真实的更新时刻）", () => {
  const profile = { id: 1, repo: "o/R", baseDir: "hf", targetDir: "hf/o/R", createdAt: 9_999 };
  const tree = [
    { folder: "hf/o/R", files: [{ rel: "hf/o/R/a.gguf", size: 10, mtime: 1_000, ino: 1 }] },
  ];
  const [stats] = decorateProfileStats([profile], tree);
  expect(stats!.lastModified).toBe(1_000);
});

it("目录为空或不存在时回退 createdAt", () => {
  const profile = { id: 1, repo: "o/R", baseDir: "hf", targetDir: "hf/o/R", createdAt: 100 };
  const [stats] = decorateProfileStats([profile], []);
  expect(stats!.lastModified).toBe(100);
  expect(stats!.dirExists).toBe(false);
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/repoProfiles.test.ts`
预期：FAIL，`lastModified` 是 `undefined`（属性不存在）。

- [ ] **步骤 3：实现**

`RepoProfileStats` 接口追加：

```ts
  /** 档案目录内文件的最大 mtime（毫秒）；目录为空或不存在时回退 createdAt。
   *  文件落盘那一刻就是它，所以下载完成、手动拷入、外部工具写入都算数——
   *  比只查 download_history 更准，且不需要第二次扫盘（mtime 随 scanTree 已在手上）。 */
  lastModified: number;
```

`decorateProfileStats` 的 map 回调内，在既有 `for (const f of files)` 循环之后、`return` 之前加：

```ts
    // files 已在上面算过，这里只多走一遍取 max（不能并进上面那个循环：那个循环
    // 按 inode 去重跳过了重复项，而 mtime 要看全部文件）
    const lastModified =
      files.length > 0 ? files.reduce((max, f) => (f.mtime > max ? f.mtime : max), 0) : p.createdAt;
```

并把 `lastModified` 加进 return 的对象字面量。

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/repoProfiles.test.ts`
预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/server/repoProfiles.ts src/server/repoProfiles.test.ts
git commit -m "feat(repos): 档案统计带上最近更新时间"
```

---

### 任务 3：首页「最近更新的仓库」区

**文件：**
- 创建：`src/lib/recent-repos.ts`、`src/lib/recent-repos.test.ts`
- 创建：`src/components/repo-card.tsx`
- 修改：`src/app/(panel)/models/repos/repos-view.tsx`（删内联 `RepoCard`，改引用共享组件）
- 创建：`src/app/(panel)/models/home-repos.tsx`
- 修改：`src/app/(panel)/models/page.tsx`、`src/i18n/messages/{zh,en}.json`

- [ ] **步骤 1：编写失败的测试**

`src/lib/recent-repos.test.ts`：

```ts
import { describe, expect, it } from "vitest";

import { pickRecentRepos } from "./recent-repos";

const mk = (id: number, lastModified: number) => ({ id, lastModified });

describe("pickRecentRepos", () => {
  it("按 lastModified 倒序取前 5 个", () => {
    const rows = [mk(1, 100), mk(2, 900), mk(3, 500), mk(4, 700), mk(5, 300), mk(6, 800)];
    expect(pickRecentRepos(rows).map((r) => r.id)).toEqual([2, 6, 4, 3, 5]);
  });

  it("不足 5 个时原样返回（按顺序排好）", () => {
    expect(pickRecentRepos([mk(1, 100), mk(2, 900)]).map((r) => r.id)).toEqual([2, 1]);
  });

  it("lastModified 并列时按 id 倒序——新建的排前面，且结果稳定", () => {
    expect(pickRecentRepos([mk(1, 500), mk(3, 500), mk(2, 500)]).map((r) => r.id)).toEqual([3, 2, 1]);
  });

  it("不改动入参数组", () => {
    const rows = [mk(1, 100), mk(2, 900)];
    pickRecentRepos(rows);
    expect(rows.map((r) => r.id)).toEqual([1, 2]);
  });

  it("limit 可覆盖", () => {
    const rows = [mk(1, 100), mk(2, 900), mk(3, 500)];
    expect(pickRecentRepos(rows, 2).map((r) => r.id)).toEqual([2, 3]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/recent-repos.test.ts`
预期：FAIL，模块不存在。

- [ ] **步骤 3：实现纯函数**

`src/lib/recent-repos.ts`：

```ts
/**
 * 首页「最近更新的仓库」的排序与截断（2026-09-21 设计 §3.3）。
 *
 * 泛型只约束用到的两个字段，不绑死 RepoProfileStats——本文件是纯逻辑层，
 * 不该为了排序而 import server 侧类型。
 */
export const RECENT_REPOS_LIMIT = 5;

export function pickRecentRepos<T extends { id: number; lastModified: number }>(
  profiles: readonly T[],
  limit: number = RECENT_REPOS_LIMIT,
): T[] {
  // 并列时按 id 倒序兜底：同一批下载落盘的档案 mtime 可能一模一样，
  // 不给第二判据的话顺序取决于入参顺序，看起来就像随机跳动
  return [...profiles].sort((a, b) => b.lastModified - a.lastModified || b.id - a.id).slice(0, limit);
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/recent-repos.test.ts`
预期：PASS。

- [ ] **步骤 5：抽出共享的 `RepoCard`**

把 `src/app/(panel)/models/repos/repos-view.tsx` 里的 `function RepoCard({ profile })` **整体**剪切到新文件 `src/components/repo-card.tsx`，加 `"use client"` 与 `export`，`RepoProfileEntry` 接口一并移过去并导出（`repos-view.tsx` 改为从新文件 import 这个类型，不再自己声明）。卡片内部实现一行不改——本步骤只搬家，避免把「抽取」与「改样式」混进同一个 diff。

`repos-view.tsx` 顶部改为：

```tsx
import { RepoCard, type RepoProfileEntry } from "@/components/repo-card";
```

- [ ] **步骤 6：写「最近更新的仓库」区**

`src/app/(panel)/models/home-repos.tsx`：

```tsx
"use client";

import Link from "next/link";
import { Archive, ArrowRight } from "lucide-react";
import { useTranslations } from "next-intl";

import { RepoCard, type RepoProfileEntry } from "@/components/repo-card";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

/**
 * 首页「最近更新的仓库」区（2026-09-21 设计 §3.3）：只列最近更新的几个，
 * 全量在 /models/repos。排序与截断已由 server 侧 pickRecentRepos 做完，
 * 本组件只负责渲染，不再自己排一遍。
 */
export function HomeRepos({ repos, total }: { repos: RepoProfileEntry[]; total: number }) {
  const t = useTranslations("pages.modelsHome");
  const tRepos = useTranslations("pages.repos");

  return (
    <section className="px-7 pb-6">
      <div className="mb-3 flex items-center justify-between gap-2">
        <h2 className="text-sm font-semibold">{t("reposTitle")}</h2>
        {total > 0 && (
          <Button variant="ghost" size="sm" nativeButton={false} render={<Link href="/models/repos" />}>
            {t("reposViewAll", { total })}
            <ArrowRight className="size-3.5" />
          </Button>
        )}
      </div>
      {repos.length === 0 ? (
        <Card>
          <CardContent className="flex flex-col items-center justify-center gap-3 py-12 text-center">
            <span className="flex size-12 items-center justify-center rounded-xl bg-muted text-muted-foreground">
              <Archive className="size-6" />
            </span>
            <p className="text-sm font-medium">{tRepos("emptyTitle")}</p>
            <p className="max-w-md text-sm text-muted-foreground">{tRepos("emptyDescription")}</p>
          </CardContent>
        </Card>
      ) : (
        <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-3">
          {repos.map((profile) => (
            <RepoCard key={profile.id} profile={profile} />
          ))}
        </div>
      )}
    </section>
  );
}
```

- [ ] **步骤 7：接进首页**

`src/app/(panel)/models/page.tsx` 补数据装配与渲染：

```tsx
import { pickRecentRepos } from "@/lib/recent-repos";
import { getDb } from "@/server/db";
import { scanTree } from "@/server/fsScanner";
import { getPanelModelsRoot } from "@/server/locators";
import { decorateProfileStats, listProfiles } from "@/server/repoProfiles";
import { HomeRepos } from "./home-repos";
```

函数体内：

```tsx
  const tree = scanTree(getPanelModelsRoot());
  const profiles = decorateProfileStats(listProfiles(getDb()), tree);
  const recentRepos = pickRecentRepos(profiles);
```

把 `<div className="min-h-0 flex-1 overflow-y-auto" />` 换成：

```tsx
        <div className="min-h-0 flex-1 overflow-y-auto pt-6">
          <HomeRepos repos={recentRepos} total={profiles.length} />
        </div>
```

页头 `stats` 补上仓库读数：`{ value: profiles.length, label: tHome("statRepos") }`。

- [ ] **步骤 8：补文案**

`pages.modelsHome` 追加（zh）：

```json
"reposTitle": "最近更新的仓库",
"reposViewAll": "查看全部 {total} 个"
```

en：`"reposTitle": "Recently updated repos"`、`"reposViewAll": "View all {total}"`。

- [ ] **步骤 9：验证**

```bash
pnpm exec vitest run src/lib/recent-repos.test.ts
pnpm exec tsc --noEmit
pnpm run lint
pnpm test
```
预期：全部 PASS / 无输出。

- [ ] **步骤 10：Commit**

```bash
git add src/lib/recent-repos.ts src/lib/recent-repos.test.ts src/components/repo-card.tsx "src/app/(panel)/models" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(models): 首页展示最近更新的仓库"
```

---

### 任务 4：首页「正在运行」区

**文件：**
- 创建：`src/app/(panel)/models/home-running.tsx`
- 修改：`src/app/(panel)/models/page.tsx`、`src/i18n/messages/{zh,en}.json`

本任务没有可下沉的判定逻辑（纯展示 + 既有组件复用），不写单测，靠 tsc / lint / 全量测试守。

- [ ] **步骤 1：写「正在运行」区**

`src/app/(panel)/models/home-running.tsx`。卡片内容照搬概览页 `(panel)/page.tsx:150-215` 现有的那套字段与组件（`CopyCurlButton`、`ModelEndpointActions`、`RuntimeCardActions` 三个都是既有 client 组件，直接复用），区别只在容器：这里是横向平铺的 grid，不是概览页那种纵向 divide-y 列表。

```tsx
"use client";

import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { useFormatter, useTranslations } from "next-intl";

import { CopyCurlButton } from "@/components/copy-curl-button";
import { ModelEndpointActions } from "@/components/model-endpoint-actions";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { RuntimeCardActions } from "../runtime-card-actions";

/** 与 server/modelsView.ts 的 RunningModelView 同构（客户端不引 server 模块） */
export interface RunningEntry {
  model: string;
  displayName: string;
  container: string;
  startedAt: string | null;
  hostPort: number | null;
  configuredHostPort: number | null;
}

/**
 * 首页「正在运行」区（2026-09-21 设计 §3.2）：每个运行中模型一张卡、横向平铺
 * （运行中通常 1–3 个，铺一行信息给得最足）。概览页那张卡已精简为两行摘要并
 * 引流到这里，两边形态不同，刻意不共享组件——共享只会退化成一堆开关参数。
 */
export function HomeRunning({
  models,
  defaultModel,
}: {
  models: RunningEntry[];
  defaultModel: string | null;
}) {
  const t = useTranslations("pages.modelsHome");
  const format = useFormatter();

  return (
    <section className="px-7 pb-6">
      <h2 className="mb-3 text-sm font-semibold">{t("runningTitle")}</h2>
      {models.length === 0 ? (
        <Card className="border-dashed">
          <CardContent className="flex flex-col items-center justify-center gap-3 py-12 text-center">
            <p className="text-sm font-medium">{t("runningEmptyTitle")}</p>
            <p className="max-w-md text-sm text-muted-foreground">{t("runningEmptyHint")}</p>
            <Button size="sm" nativeButton={false} render={<Link href="/models/profiles" />}>
              {t("gotoProfiles")}
              <ArrowRight className="size-3.5" />
            </Button>
          </CardContent>
        </Card>
      ) : (
        <div className="grid grid-cols-1 gap-3 lg:grid-cols-2 2xl:grid-cols-3">
          {models.map((entry) => {
            const isDefault = entry.model === defaultModel;
            return (
              <Card key={entry.model}>
                <CardContent className="flex flex-col gap-3">
                  <div>
                    <div className="flex min-w-0 items-center gap-2">
                      <span className="truncate font-mono text-lg leading-tight font-bold">
                        {entry.displayName}
                      </span>
                      {isDefault && models.length > 1 && (
                        <Badge variant="outline" className="shrink-0 px-1.5 py-0 text-[10px]">
                          {t("defaultBadge")}
                        </Badge>
                      )}
                    </div>
                    <div className="text-[13px] text-muted-foreground">{entry.model}</div>
                  </div>

                  <dl className="flex flex-col gap-1.5 text-xs">
                    <div className="flex items-center justify-between gap-3">
                      <dt className="shrink-0 text-muted-foreground">{t("fieldContainer")}</dt>
                      <dd className="truncate font-mono" title={entry.container}>
                        {entry.container}
                      </dd>
                    </div>
                    <div className="flex items-center justify-between gap-3">
                      <dt className="shrink-0 text-muted-foreground">{t("fieldPort")}</dt>
                      <dd className="flex items-center gap-0.5 font-mono tabular-nums">
                        {entry.hostPort !== null ? `:${entry.hostPort}` : "—"}
                        {entry.hostPort !== null && (
                          <>
                            <CopyCurlButton hostPort={entry.hostPort} size="icon" />
                            <ModelEndpointActions hostPort={entry.hostPort} />
                          </>
                        )}
                      </dd>
                    </div>
                    <div className="flex items-center justify-between gap-3">
                      <dt className="shrink-0 text-muted-foreground">{t("fieldStartedAt")}</dt>
                      <dd className="tabular-nums">
                        {entry.startedAt
                          ? format.dateTime(new Date(entry.startedAt), {
                              month: "2-digit",
                              day: "2-digit",
                              hour: "2-digit",
                              minute: "2-digit",
                              hour12: false,
                            })
                          : "—"}
                      </dd>
                    </div>
                  </dl>

                  <RuntimeCardActions
                    modelName={entry.model}
                    displayName={entry.displayName}
                    isDefault={isDefault}
                    showSetDefault={models.length > 1}
                  />
                </CardContent>
              </Card>
            );
          })}
        </div>
      )}
    </section>
  );
}
```

`RuntimeCardActions` 现住在 `src/app/(panel)/runtime-card-actions.tsx`，从 `models/` 引用写 `../runtime-card-actions` 即可——跨目录相对引用在本仓库是既有做法（`models/repos/repos-view.tsx` 就 import 了 `../repo-create-nav-button`），lint 不拦。任务 5 精简概览页之后这个组件只剩首页在用，届时再挪窝（见任务 5 步骤 1）。

- [ ] **步骤 2：接进首页**

`src/app/(panel)/models/page.tsx` 补：

```tsx
import { getRuntimeService } from "@/server/locators";
import { decorateRuntimeStatus } from "@/server/modelsView";
import { createModelRepo } from "@/server/repo/models";
import { HomeRunning } from "./home-running";
```

函数体内：

```tsx
  const status = await decorateRuntimeStatus(getDb(), getRuntimeService());
  const configCount = createModelRepo(getDb()).listModels().length;
```

`listModels(namespace?)` 是 `ModelRepo` 接口的既有方法（`src/server/repo/models.ts:70`），不传参即返回全部模型。

渲染顺序：`<HomeRunning>` 在 `<HomeRepos>` 之前。页头 `stats` 补齐三个读数：

```tsx
          stats={[
            { value: status.models.length, label: tHome("statRunning"), tone: "hot" },
            { value: configCount, label: tHome("statConfigs") },
            { value: profiles.length, label: tHome("statRepos") },
          ]}
```

`createModelRepo(db).listModels()` 的方法名以 `src/server/repo/models.ts` 的实际导出为准——实现前先确认，若不叫这个名字则用该文件里列出全部模型的那个方法。

- [ ] **步骤 3：补文案**

`pages.modelsHome` 追加（zh）：

```json
"runningTitle": "正在运行",
"runningEmptyTitle": "当前没有运行中的模型",
"runningEmptyHint": "到配置列表里挑一个模型启动，启动后的端口与操作会显示在这里。",
"gotoProfiles": "去配置列表",
"defaultBadge": "默认",
"fieldContainer": "容器",
"fieldPort": "端口",
"fieldStartedAt": "启动于"
```

en：`"runningTitle": "Running"`、`"runningEmptyTitle": "No models are running"`、`"runningEmptyHint": "Pick a model from the config list and start it; its port and controls will show up here."`、`"gotoProfiles": "Go to configs"`、`"defaultBadge": "Default"`、`"fieldContainer": "Container"`、`"fieldPort": "Port"`、`"fieldStartedAt": "Started"`。

- [ ] **步骤 4：验证**

```bash
pnpm exec tsc --noEmit
pnpm run lint
pnpm test
```
预期：无输出 / 全量 PASS。

- [ ] **步骤 5：Commit**

```bash
git add "src/app/(panel)/models" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(models): 首页展示正在运行的模型"
```

---

### 任务 5：概览页运行卡精简

**文件：**
- 修改：`src/app/(panel)/page.tsx:135-245`
- 修改：`src/i18n/messages/{zh,en}.json`

- [ ] **步骤 1：精简有运行中模型的分支**

`(panel)/page.tsx` 运行状态卡内，保留顶部的运行中 Badge，把每个模型那一大段 `dl` + `RuntimeCardActions` 换成两行摘要：

```tsx
                    <div className="flex flex-col divide-y lg:max-h-[420px] lg:overflow-y-auto">
                      {status.models.map((entry) => {
                        const isDefault = entry.model === status.defaultModel;
                        return (
                          <div key={entry.model} className="flex items-center justify-between gap-3 py-2.5 first:pt-2 last:pb-0">
                            <div className="flex min-w-0 items-center gap-2">
                              <span className="truncate font-mono text-sm font-semibold">
                                {entry.displayName}
                              </span>
                              {isDefault && status.models.length > 1 && (
                                <Badge
                                  variant="outline"
                                  className="shrink-0 px-1.5 py-0 text-[10px]"
                                  title={t("defaultBadgeHint")}
                                >
                                  {t("defaultBadge")}
                                </Badge>
                              )}
                            </div>
                            <span className="shrink-0 font-mono text-xs tabular-nums text-muted-foreground">
                              {entry.hostPort !== null ? `:${entry.hostPort}` : "—"}
                            </span>
                          </div>
                        );
                      })}
                    </div>

                    <Button
                      variant="outline"
                      size="sm"
                      className="mt-3 w-full"
                      nativeButton={false}
                      render={<Link href="/models" />}
                    >
                      {t("viewDetail")}
                      <ArrowRight className="size-3.5" />
                    </Button>
```

随之清理该文件中因此不再使用的 import（`CopyCurlButton`、`ModelEndpointActions`、`RuntimeCardActions`、以及只为启动时间格式化而存在的 `startedFmt` —— 逐个确认文件内确实再无引用后才删，`(panel)/page.tsx` 里别处可能还在用）。

`RuntimeCardActions` 至此只剩模型首页一个调用方，把 `src/app/(panel)/runtime-card-actions.tsx` 移到 `src/app/(panel)/models/runtime-card-actions.tsx`（实现一行不改），任务 4 里 `home-running.tsx` 的 `../runtime-card-actions` 相应改成 `./runtime-card-actions`。留在 `(panel)/` 根目录会让人以为概览页还在用它。

- [ ] **步骤 2：空态改虚线面板**

把 `status.models.length > 0` 的 else 分支整体换成占满卡片宽度的虚线面板，两态只换文案：

```tsx
                  <div className="flex flex-col items-center justify-center gap-2.5 rounded-lg border border-dashed px-4 py-8 text-center">
                    <div className="text-sm font-medium">
                      {hasModels ? t("idleTitle") : t("idleEmptyTitle")}
                    </div>
                    <p className="max-w-xs text-[13px] text-muted-foreground">
                      {hasModels ? t("idleHint") : t("idleEmptyHint")}
                    </p>
                    <Button
                      size="sm"
                      className="mt-1"
                      nativeButton={false}
                      render={<Link href="/models/profiles" />}
                    >
                      {hasModels ? t("gotoModels") : t("gotoCreate")}
                      <ArrowRight className="size-3.5" />
                    </Button>
                  </div>
```

原本空态顶上的 `statusIdle` Badge 一并去掉——虚线面板本身已经表达"这里是空的"，再挂一个灰点徽标是重复。若 `statusIdle` 文案在别处无引用，从两份 i18n 文件中删除。

- [ ] **步骤 3：调整文案**

文案都在 `pages.overview` 分组下（`(panel)/page.tsx:57` 取的就是它）。逐条：

| key | 现值（zh） | 改成（zh） | 改成（en） |
|---|---|---|---|
| `viewDetail` | 新增 | `查看详情` | `View details` |
| `gotoCreate` | 新增 | `去创建配置` | `Create a config` |
| `gotoModels` | `去模型列表选择` | `去配置列表挑一个` | `Pick one from configs` |
| `idleHint` | `请从模型列表选择要启动的模型，可以同时运行多个模型。` | `到配置列表里选一个模型启动，可以同时运行多个。` | `Start a model from the config list; several can run at once.` |
| `idleEmptyHint` | `先到模型列表创建模型配置（或等待 M2 下载向导上线），再回到这里启动。` | `先创建一份模型配置，指定 GGUF 权重与启动参数，之后就能启动它。` | `Create a model config with its GGUF weights and launch parameters first, then start it.` |

`idleTitle`（`当前没有模型在运行`）与 `idleEmptyTitle`（`还没有模型配置`）表述仍然准确，**不动**。

`idleEmptyHint` 必须改：现值里的「或等待 M2 下载向导上线」是早就过时的话——下载向导在 M2 就已上线，这句话留到现在是漏网的。

- [ ] **步骤 4：验证**

```bash
pnpm exec tsc --noEmit
pnpm run lint
pnpm test
```
预期：无输出 / 全量 PASS。

- [ ] **步骤 5：Commit**

```bash
git add "src/app/(panel)/page.tsx" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(overview): 运行状态卡精简并引流到模型首页"
```

---

## 收尾

全部任务完成后：

```bash
pnpm test
pnpm run lint
pnpm exec tsc --noEmit
pnpm run build
HTTP_PROXY=http://10.22.33.1:20172 HTTPS_PROXY=http://10.22.33.1:20172 ./deploy/llamapad.sh build
```

`pnpm run build` 是组件层唯一的把关手段（vitest 是 node 环境、没有 jsdom，组件测不了），不能省。最后一条是本项目改完代码的固定收尾，产出 `llamapad:dev` 镜像。

真机验收清单（自动化测不到，需人工点）：

1. 一级侧栏「模型」落到首页，二级栏三格互跳正常，首页/配置/档案各自高亮正确
2. `/models/new`、`/models/<name>/edit` 页面的二级栏高亮「配置」而不是「首页」
3. 编辑保存后、向导完成后回到 `/models/profiles` 而不是首页
4. 首页运行区：有模型在跑时卡片横向平铺，停止/重启/设为默认可用；没有时显示虚线空态并能跳到配置列表
5. 首页仓库区：只列 5 个、顺序与最近下载/写入一致，「查看全部」跳得通
6. 概览页运行卡只剩两行摘要 + 「查看详情」，空态是虚线面板

---

## 执行结果（2026-09-21）

五个任务按计划完成，逐任务审查 + 覆盖全部改动的最终审查均已通过。提交：

```
09656a0 refactor(models): 配置列表迁到 /models/profiles，/models 改为模型首页
7bb3cc2 feat(repos): 档案统计带上最近更新时间
f20c303 feat(models): 首页展示最近更新的仓库
d4d9b65 feat(models): 首页展示正在运行的模型
99b8c8f feat(overview): 运行状态卡精简并引流到模型首页
1db6d2c docs(models): 手册同步 503 hint 与运行卡入口的新路由/新落点
b864017 fix(models): 收尾模型首页迁移的审查发现——二级栏入口、文案口径与过期注释
```

验证：`pnpm test` 209 files / 3417 passed / 16 skipped；`tsc --noEmit` 与 `eslint` 均无输出；
`next build` 由 `./deploy/llamapad.sh build` 的 docker 构建覆盖（本地 shell 沙箱跑 `pnpm run build`
会被 SIGKILL，与机器内存无关），镜像 `llamapad:dev` 已产出并确认含 `models/profiles` 路由产物。

### 计划本身暴露的两处缺口（已修，记录备查）

1. **§2.2 的跳转点清单漏了一处**：`llm-extract-panel.tsx:290` 用的是 `action={{ href: "/models" }}`
   这种对象字面量写法，写计划时的 grep 只匹配了 `href="/models"`，没覆盖到。以后做路由迁移，
   三种写法都要扫：`href="…"`、`href: "…"`、`router.push("…")`。
2. **§3.2 漏了端口顺延提示的渲染**：把概览页运行卡的字段搬到首页时，`configuredHostPort` 传了
   却没人读，「配置端口 :X 被占用，已顺延」这条信息在首页缺失。返工轮补回。

### 搁置项（经裁定不在本期处理）

- **首页运行卡是响应式栅格而非严格「一行」**（`grid-cols-1 lg:grid-cols-2 2xl:grid-cols-3`）：
  1–3 个模型时表现与 §3.2 一致，更多时折行优于挤压。
- **首页 PageHeader 标题「模型」与二级栏 title 同字重复**：属视觉观感，真机看过再定。
- **`/models?ns=xxx` 旧书签会静默丢掉 `ns`**：§7 已裁定本期不做兼容重定向，`/models` 本身仍有效、
  不是死链。若要补，只需在首页把 `ns` 透传重定向到 `/models/profiles?ns=`。
- **手册里「模型列表」的措辞**：本期只统一了 UI 里 6 个跳转按钮的文案口径，没有全面把这个概念
  改名，手册中相关描述仍沿用旧叫法。全面术语统一是另一件事。

### 真机验收清单（自动化覆盖不到，需人工点）

1. **二级栏高亮矩阵**（本期最核心、恰好零组件测试覆盖）：依次访问 `/models`、`/models/profiles`、
   `/models/profiles?ns=<空间>`、`/models/new`、`/models/<name>/edit`、`/models/<name>/duplicate`、
   `/models/repos`、`/models/repos/<id>`，高亮应分别落在 首页 / 配置 ×5 / HuggingFace ×2。
   重点看编辑页与向导页——它们靠「兜底落 configs」这条，判错了用户会以为自己跑到首页去了。
2. **首页运行区三态**：0 个（虚线空态 + 「去配置列表」）、1 个（默认徽标应**不**出现）、
   ≥2 个（徽标 + 「设为默认」出现）；在首页实点停止 / 重启 / 设为默认，确认刷新链路正常
   ——`RuntimeCardActions` 换了宿主页面。
3. **端口顺延提示**：配两个 `host_port` 相同的模型先后启动，第二个的卡片应出现「配置端口
   :18080 被占用，已顺延」，且显示端口与「复制 curl」拷出的地址一致。这条是返工补的，无自动化覆盖。
4. **就绪窗口期观感**：启动 27B，在容器已起、`/health` 未通的约 35 秒里看首页运行卡——端口与
   复制 curl 都已渲染但连不上。`RunningModelView.ready` 就在 props 里却没用上，看过再定要不要补加载态。
5. **最近更新排序口径**：`touch` 某档案目录里的文件 → 刷新首页应跳第一位；再试空档案目录
   （回退 created_at）、目录被手动删掉（应带「目录缺失」徽标）、同批下载的多个档案（mtime 并列，顺序应稳定）。
6. **首页扫盘开销**：158G 模型库的真机上，`/models` 现在是侧栏「模型」的落点，每次访问都要
   全树 `scanTree` + 对每个运行中模型探 `/health`。实测首屏耗时，明显偏慢再考虑加缓存。
7. **概览页空态两分支**：清空配置看「还没有模型配置 / 去创建配置」，建一个但不启动看
   「当前没有运行中的模型 / 去配置列表挑一个」，两个按钮都应落到 `/models/profiles`。
8. **中英双语**：切 en 过一遍三个页面的页头、二级栏、首页两区空态，重点看新增的
   `pages.modelsHome` 与改过的 `pages.overview` 那几条。
