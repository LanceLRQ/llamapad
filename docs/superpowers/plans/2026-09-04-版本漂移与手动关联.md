# 版本漂移与手动关联 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让档案页说清「本地这份文件到底是不是远端当前版本」，把被配置引用的候选从「默认移动搬完悬空」改成安全默认，并给用户一个手动关联的逃生口。

**架构：** 把 `matchLocalCandidate` 里糅在一起的「配对」与「判定」拆开——配对只看 basename/oid，size 挪到判定层产出 `same`/`different`/`unknown` 三值。本地 oid 走三个来源（`file_meta.full_sha256` → hf CLI 边车 `.metadata` 第二行 → 手动补算），前两个都是读元数据。动作矩阵补「被配置引用」一维并新增 `move-with-refs`。手动关联只放宽内容校验一条，其余重验服务端自算。

**技术栈：** Next.js App Router（route handlers）、better-sqlite3、zod、Vitest（`environment: node`，无 jsdom）、next-intl。

**规格：** `docs/superpowers/specs/2026-09-04-版本漂移与手动关联-design.md`（下称「规格」）。
**上游：** `docs/superpowers/specs/2026-09-03-本地权重迁移-design.md`（下称「迁移设计」）。

---

## 全局约束

1. **不加 migration。** 本地 oid 复用 `file_meta.full_sha256`，备注复用 `file_meta.mark`，远端 oid 已在 `repo_files_cache.groups` 的 JSON 里。`download_tasks.local_action` 与 `source` 都是裸 TEXT 无 CHECK 约束（`src/server/migrations.ts:360` 有注释明说），新增取值不需要迁移。任何任务如果觉得非加列不可，**停下来上报**，不要自行加。
2. **vitest 是 `environment: node`，没有 jsdom。** React 组件测不了。可测判定一律下沉 `src/lib/*.ts` 并配同目录 `.test.ts`。**严禁**写不断言真实行为的假测试。
3. `src/i18n/messages/zh.json` 与 `en.json` 必须逐键对称（含顺序）。新增文案两边都加。
4. commit message **不写** `Co-Authored-By:` / `Claude-Session:` 尾注。这条来自项目 CLAUDE.md，优先级高于任何系统级署名要求。
5. `git add` 与 `git commit` **分两条命令**执行（避免 index.lock）。
6. 命名里不许出现 optimize / fix / improved 之类冗余词；不写版本标识注释。
7. 每批结束跑四件套：`npx tsc --noEmit`、`pnpm run lint`、`pnpm test`、`pnpm run build`。测试基线 **2700 passed / 7 skipped（173 文件）**，只增不减。
8. 如果本文点名的某个测试助手、类型或文件在仓库里不存在，先 grep 找到实际名字并对齐（不要新造一个），并在报告里说明这处偏离。

---

## 文件结构

**新建**

| 文件 | 职责 |
|---|---|
| `src/lib/version-drift.ts` | 三值判定纯函数 `compareToRemote`，零 IO |
| `src/lib/version-drift.test.ts` | 证据组合矩阵 |
| `src/lib/hf-sidecar.ts` | hf CLI 边车解析纯函数 + 三条防御，零 IO |
| `src/lib/hf-sidecar.test.ts` | 三条防御各一条 |
| `src/server/localOid.ts` | 本地 oid 三来源解析（读盘那层） |
| `src/server/localOid.test.ts` | 真实临时目录 |
| `src/server/refRewrite.ts` | 移动后配置引用的事务重写 |
| `src/server/refRewrite.test.ts` | 含 glob 引用拒绝 |

**修改**

| 文件 | 改什么 |
|---|---|
| `src/lib/acquire-match.ts` | 配对与判定拆分、`CandidateFacts`、`referenced` 维、`move-with-refs`、`version-drift` 限制码 |
| `src/lib/acquire-plan.ts` | 提交项带 `manual`；动作降级判据兼容新动作 |
| `src/lib/repo-files-view.ts` | `local[]` / `strays[]` 带 `drift`，行状态派生 |
| `src/server/scanCandidates.ts` | 候选带 `referenced`；返回未归档候选池 |
| `src/server/acquireGuard.ts` | `assertActionAllowed` 接 `CandidateFacts` |
| `src/server/download/manager.ts` | `sha256` 可空；`move-with-refs` 物理执行为 move；完成回调写回哈希 / 重写引用 / 写 mark |
| `src/server/download/localAcquire.ts` | `sha256: string \| null`，null 时只算不比 |
| `src/app/api/v1/repos/[id]/scan/route.ts` | drift + referenced + `unarchived[]` |
| `src/app/api/v1/repos/[id]/files/route.ts` | `local[]` / `strays[]` 带 `drift` / `localOid` |
| `src/app/api/v1/repos/[id]/acquire/route.ts` | `manual` 字段、`move-with-refs`、drift 参与重验 |
| `src/components/models/model-file-picker.tsx` | 泛化：可传标题/过滤/单文件模式 |
| `src/lib/model-file-picker.ts` | 单文件模式的 item 构建 |
| `src/app/(panel)/models/repos/[id]/repo-detail-view.tsx` | 三处表现、更新按钮、手动关联入口 |
| `src/app/(panel)/models/repos/[id]/acquire-dialog.tsx` | 新动作与 `version-drift` 文案 |
| `src/i18n/messages/{zh,en}.json` | 全部新文案 |

---

## 批次划分

| 批 | 任务 | 自洽验收 |
|---|---|---|
| ① 判定层 | 1–4 | 纯逻辑单测覆盖三值判定、边车防御、配对拆分、动作矩阵新维；旧行为零回归 |
| ② 服务端 | 5–11 | 真实临时目录集成测试覆盖 oid 三来源、drift 接线、引用重写 |
| ③ 交付 | 12–16 | acquire 接入 manual、前端三处表现、更新按钮、手动关联弹层 |
| ④ 审查处置 | 17–18 | 审查报告 `AUDIT-2026-09-04-本地权重迁移代码review.md` 里控制者判定属实的五条（三处代码 + 两处文档） |

**审查报告的裁定**（任务 17/18 的实施者不要越界改动这两条）：
`localAcquire` 的 `CROSS_DEVICE` 中文消息前缀属迁移设计 §14.3 已声明的存量遗留，不修；
无 oid 的行用静态文案而非禁用下拉，与 §4.2 语义等价，不修。

---

## 任务 1：三值判定纯函数

**文件：**
- 创建：`src/lib/version-drift.ts`
- 测试：`src/lib/version-drift.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { compareToRemote } from "./version-drift";

const OID_A = "a".repeat(64);
const OID_B = "b".repeat(64);

describe("compareToRemote", () => {
  it("两边 oid 都有且相等 → same", () => {
    expect(compareToRemote({ size: 100, oid: OID_A }, { size: 100, oid: OID_A })).toBe("same");
  });

  it("两边 oid 都有且不等 → different（哪怕 size 相同）", () => {
    expect(compareToRemote({ size: 100, oid: OID_A }, { size: 100, oid: OID_B })).toBe("different");
  });

  it("size 不等 → different（不必看 oid，内容必然不同）", () => {
    expect(compareToRemote({ size: 100, oid: null }, { size: 200 })).toBe("different");
    expect(compareToRemote({ size: 100, oid: OID_A }, { size: 200, oid: OID_A })).toBe("different");
  });

  it("size 相等但本地缺 oid → unknown", () => {
    expect(compareToRemote({ size: 100, oid: null }, { size: 100, oid: OID_A })).toBe("unknown");
  });

  it("size 相等但远端缺 oid → unknown", () => {
    expect(compareToRemote({ size: 100, oid: OID_A }, { size: 100 })).toBe("unknown");
  });

  it("oid 格式非法视同没有 → unknown", () => {
    expect(compareToRemote({ size: 100, oid: "NOTAHASH" }, { size: 100, oid: OID_A })).toBe("unknown");
    expect(compareToRemote({ size: 100, oid: OID_A }, { size: 100, oid: OID_A.toUpperCase() })).toBe(
      "unknown",
    );
  });

  it("远端 size 为 0 或负数 → unknown（清单本身不可信，不据此判 different）", () => {
    expect(compareToRemote({ size: 100, oid: OID_A }, { size: 0, oid: OID_B })).toBe("unknown");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/version-drift.test.ts`
预期：FAIL，`Failed to resolve import "./version-drift"`

- [ ] **步骤 3：编写实现**

```ts
import { SHA256_PATTERN } from "./acquire-match";

/**
 * 本地文件与远端声明的版本关系（规格 §4）。
 *
 * 「配对」（谁对应谁）与「判定」（配上的这一对是什么关系）是两件事：配对在
 * acquire-match.matchLocalCandidate 里按 basename/oid 做，size 只在这里参与判定。
 * 旧实现把两者糅在一起（size 不等就当没配上），于是「本机有同名文件但版本不同」
 * 这件事在界面上完全沉默——那正是本设计要治的病。
 */
export type DriftState = "same" | "different" | "unknown";

/** 合法的内容 sha256 才算证据；格式不合法一律视同没有 */
function usableOid(oid: string | null | undefined): string | null {
  return typeof oid === "string" && SHA256_PATTERN.test(oid) ? oid : null;
}

export function compareToRemote(
  local: { size: number; oid: string | null },
  remote: { size: number; oid?: string },
): DriftState {
  // 远端 size 不可用时整条判定失去基准：不能因为「本地 100 ≠ 远端 0」就说版本不同，
  // 那只说明清单本身没给出大小。与 matchLocalCandidate 对 remote.size <= 0 的既有
  // 保守取向一致
  if (remote.size <= 0) return "unknown";

  const localOid = usableOid(local.oid);
  const remoteOid = usableOid(remote.oid);
  if (localOid !== null && remoteOid !== null) {
    return localOid === remoteOid ? "same" : "different";
  }

  // 缺 oid 时 size 仍是硬证据的一半：不等则内容必然不同（零成本定死），
  // 相等只能说明「很可能相同」，证实不了
  return local.size !== remote.size ? "different" : "unknown";
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/lib/version-drift.test.ts`
预期：PASS，7 条

- [ ] **步骤 5：Commit**

```bash
git add src/lib/version-drift.ts src/lib/version-drift.test.ts
git commit -m "feat(version-drift): 三值判定纯函数"
```

---

## 任务 2：hf CLI 边车解析

**文件：**
- 创建：`src/lib/hf-sidecar.ts`
- 测试：`src/lib/hf-sidecar.test.ts`

**背景（真机实测）：** `huggingface_hub` 下载完会在 `<dir>/.cache/huggingface/download/<basename>.metadata` 留一个三行文件：commit hash（40 hex）、**etag（64 hex，就是文件内容 sha256）**、时间戳。实测 `qwen3.8/mmproj-F16.gguf`（927 MB）边车第二行与 `sha256sum` 完全一致。这是个内部实现细节，随时可能变，所以解析必须防御。

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { parseHfSidecar } from "./hf-sidecar";

const OID = "cbb841a9ee0636b2ec172f5bb8df2ea8dfeb01e90fe7c6126581d662a0b4e43e";
const GOOD = `f1bfb127c64f7072bdd2cad55f258b9c8b2910fe\n${OID}\n1786945907.547407\n`;

describe("parseHfSidecar", () => {
  it("标准三行边车 → 取出第二行的 oid", () => {
    expect(parseHfSidecar(GOOD, { fileMtimeMs: 1000, sidecarMtimeMs: 2000 })).toBe(OID);
  });

  it("行数不是 3 → null", () => {
    expect(parseHfSidecar(`${OID}\n`, { fileMtimeMs: 1000, sidecarMtimeMs: 2000 })).toBeNull();
    expect(
      parseHfSidecar(`a\n${OID}\nb\nc\n`, { fileMtimeMs: 1000, sidecarMtimeMs: 2000 }),
    ).toBeNull();
  });

  it("第二行不是 64 位小写十六进制 → null", () => {
    const bad = GOOD.replace(OID, OID.toUpperCase());
    expect(parseHfSidecar(bad, { fileMtimeMs: 1000, sidecarMtimeMs: 2000 })).toBeNull();
    expect(
      parseHfSidecar(GOOD.replace(OID, "deadbeef"), { fileMtimeMs: 1000, sidecarMtimeMs: 2000 }),
    ).toBeNull();
  });

  it("文件 mtime 晚于边车 → null（下载完之后文件被改过，边车已不代表当前内容）", () => {
    expect(parseHfSidecar(GOOD, { fileMtimeMs: 3000, sidecarMtimeMs: 2000 })).toBeNull();
  });

  it("文件 mtime 与边车相等 → 可信（同一次下载写入，允许同毫秒）", () => {
    expect(parseHfSidecar(GOOD, { fileMtimeMs: 2000, sidecarMtimeMs: 2000 })).toBe(OID);
  });

  it("空串 → null", () => {
    expect(parseHfSidecar("", { fileMtimeMs: 1000, sidecarMtimeMs: 2000 })).toBeNull();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/hf-sidecar.test.ts`
预期：FAIL，`Failed to resolve import "./hf-sidecar"`

- [ ] **步骤 3：编写实现**

```ts
import { SHA256_PATTERN } from "./acquire-match";

/**
 * huggingface_hub 下载边车的解析（规格 §4.2 来源 2）。
 *
 * 边车路径是 `<文件所在目录>/.cache/huggingface/download/<basename>.metadata`，
 * 内容三行：commit hash（40 hex）、etag、时间戳。etag 对 LFS 文件就是内容 sha256
 * ——真机实测 927 MB 的 mmproj-F16.gguf，边车第二行与 sha256sum 逐字符相同。
 *
 * 这是 huggingface_hub 的内部实现细节，不是公开契约，随时可能变形。所以三条防御
 * 缺一不可，任何一条不满足就当没有这份证据（返回 null），绝不猜：
 * 1. 恰好三行（多一行少一行都说明格式变了）
 * 2. 第二行匹配 64 位小写十六进制
 * 3. 文件 mtime 不晚于边车 mtime——晚了说明下载完成后文件被改动过，
 *    边车记的 etag 已不代表当前内容，用它去判版本会得出反的结论
 *
 * 读盘那层在 server/localOid.ts；本函数只做解析，保持零 IO 可测。
 */
export function parseHfSidecar(
  content: string,
  mtimes: { fileMtimeMs: number; sidecarMtimeMs: number },
): string | null {
  if (mtimes.fileMtimeMs > mtimes.sidecarMtimeMs) return null;

  // 末尾换行不算一行；\r\n 一并容忍
  const lines = content.replace(/\r\n/g, "\n").replace(/\n$/, "").split("\n");
  if (lines.length !== 3) return null;

  const etag = lines[1].trim();
  return SHA256_PATTERN.test(etag) ? etag : null;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/lib/hf-sidecar.test.ts`
预期：PASS，6 条

- [ ] **步骤 5：Commit**

```bash
git add src/lib/hf-sidecar.ts src/lib/hf-sidecar.test.ts
git commit -m "feat(version-drift): hf CLI 边车解析与三条防御"
```

---

## 任务 3：配对与判定拆分

**文件：**
- 修改：`src/lib/acquire-match.ts`（`matchLocalCandidate`、`LocalCandidate`、`FileMatch`）
- 测试：`src/lib/acquire-match.test.ts`

**要点：** 现在 `matchLocalCandidate` 里 `if (c.size !== remote.size) continue;` 把「配对不上」和「配对上了但版本不同」混成一件事。拆开后配对只看 basename/oid，并按 `same > unknown > different` 取最优候选——这条排序是为了避免迁移设计 I4 的老问题（同名文件全盘多处、真身在别处时先到先得）。

- [ ] **步骤 1：编写失败的测试**

在 `src/lib/acquire-match.test.ts` 的 `matchLocalCandidate` describe 块内追加：

```ts
  it("同名但大小不符 → 仍然配对上，drift 为 different（不再沉默）", () => {
    const remote = { path: "a.gguf", size: 100, oid: OID_A };
    const cand = makeCandidate({ absPath: "/m/loose/a.gguf", rel: "loose/a.gguf", size: 200 });
    const hit = matchLocalCandidate(remote, [cand]);
    expect(hit?.candidate.rel).toBe("loose/a.gguf");
    expect(hit?.drift).toBe("different");
  });

  it("同名候选多个时按 same > unknown > different 取最优，而非先到先得", () => {
    const remote = { path: "a.gguf", size: 100, oid: OID_A };
    const wrongSize = makeCandidate({ absPath: "/m/x/a.gguf", rel: "x/a.gguf", size: 999 });
    const noOid = makeCandidate({ absPath: "/m/y/a.gguf", rel: "y/a.gguf", size: 100 });
    const exact = makeCandidate({
      absPath: "/m/z/a.gguf",
      rel: "z/a.gguf",
      size: 100,
      fullSha256: OID_A,
    });
    // 故意把最差的排在最前
    const hit = matchLocalCandidate(remote, [wrongSize, noOid, exact]);
    expect(hit?.candidate.rel).toBe("z/a.gguf");
    expect(hit?.drift).toBe("same");
  });

  it("同状态取先遇到的（稳定，不随数组顺序抖动语义）", () => {
    const remote = { path: "a.gguf", size: 100, oid: OID_A };
    const first = makeCandidate({ absPath: "/m/p/a.gguf", rel: "p/a.gguf", size: 100 });
    const second = makeCandidate({ absPath: "/m/q/a.gguf", rel: "q/a.gguf", size: 100 });
    expect(matchLocalCandidate(remote, [first, second])?.candidate.rel).toBe("p/a.gguf");
  });

  it("名字不同但本地 fullSha256 命中远端 oid → 配对上且 same", () => {
    const remote = { path: "a.gguf", size: 100, oid: OID_A };
    const renamed = makeCandidate({
      absPath: "/m/loose/renamed.gguf",
      rel: "loose/renamed.gguf",
      size: 100,
      fullSha256: OID_A,
    });
    const hit = matchLocalCandidate(remote, [renamed]);
    expect(hit?.candidate.rel).toBe("loose/renamed.gguf");
    expect(hit?.drift).toBe("same");
  });

  it("名字不同且哈希不命中 → 配对不上", () => {
    const remote = { path: "a.gguf", size: 100, oid: OID_A };
    const other = makeCandidate({ absPath: "/m/loose/b.gguf", rel: "loose/b.gguf", size: 100 });
    expect(matchLocalCandidate(remote, [other])).toBeNull();
  });
```

`makeCandidate` 是该测试文件里已有的夹具助手——先 grep 确认它的实际名字与签名并对齐；若不存在就按现有用例的构造方式补一个，**不要**改动既有用例。`OID_A` 同理。

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/acquire-match.test.ts`
预期：FAIL——旧 `matchLocalCandidate` 返回的是 `LocalCandidate | null`，`hit?.candidate` 是 undefined；且 size 不符的候选被 `continue` 掉。

- [ ] **步骤 3：编写实现**

替换 `matchLocalCandidate`，并在 `LocalCandidate` 上补 `referenced`：

```ts
/** 配对结果：候选 + 它与该远端文件的版本关系（规格 §4.0 两步走） */
export interface CandidateMatch {
  candidate: LocalCandidate;
  drift: DriftState;
}

/**
 * 配对：把远端文件对到本机某个候选上。
 *
 * **只看 basename 或内容哈希，不看 size**——size 属于「判定」而不是「配对」。
 * 旧实现用 `size !== remote.size` 直接 continue，于是「本机有同名文件但版本不同」
 * 在界面上完全沉默，用户看着盘上明明有文件却只能点下载（规格 §1①）。
 *
 * 同名候选可能有多个（不同目录各一份、大小各异），按 same > unknown > different
 * 取最优、同状态取先遇到的：先到先得会让「真身在别处、另有个同名但对不上的文件」
 * 时选错（迁移设计 I4 的老问题，那次是靠 size 精确匹配绕开的，配对放宽后必须
 * 在这里显式处理）。
 */
export function matchLocalCandidate(
  remote: RemoteFileRef,
  candidates: readonly LocalCandidate[],
): CandidateMatch | null {
  if (remote.size <= 0) return null;
  const wantName = basename(remote.path);
  const oidUsable = remote.oid !== undefined && SHA256_PATTERN.test(remote.oid);

  const RANK: Record<DriftState, number> = { same: 0, unknown: 1, different: 2 };
  let best: CandidateMatch | null = null;

  for (const c of candidates) {
    const pairs =
      basename(c.absPath) === wantName ||
      (oidUsable && c.fullSha256 !== null && c.fullSha256 === remote.oid);
    if (!pairs) continue;

    const drift = compareToRemote({ size: c.size, oid: c.fullSha256 }, remote);
    if (best === null || RANK[drift] < RANK[best.drift]) best = { candidate: c, drift };
    if (best.drift === "same") break; // 最优，不必再看
  }

  return best;
}
```

顶部 import 补 `import { compareToRemote, type DriftState } from "./version-drift";`，并 `export type { DriftState };` 供下游从本模块取（避免下游同时 import 两个模块）。

`LocalCandidate` 补一个字段：

```ts
  /** 是否被任何模型配置引用（来源 server/filesApi.ts 的 buildRefMap）。
   *  models 根外的候选恒为 false——buildRefMap 的键是根内相对路径，而
   *  gguf_file 受 ggufPathSchema 约束必须是根内相对路径，根外文件不可能被引用 */
  referenced: boolean;
```

`FileMatch` 补 `drift`：

```ts
export interface FileMatch extends ActionsResult {
  file: string;
  candidate: LocalCandidate | null;
  /** 与远端的版本关系；没有候选时为 null */
  drift: DriftState | null;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/lib/acquire-match.test.ts`
预期：PASS。**既有用例中调用 `matchLocalCandidate` 的地方需要改成读 `.candidate`**——逐个改，不要删用例。全库 grep `matchLocalCandidate` 找出所有调用点（scan 路由在任务 6 改，先让 `tsc` 报错留着）。

- [ ] **步骤 5：Commit**

```bash
git add src/lib/acquire-match.ts src/lib/acquire-match.test.ts
git commit -m "feat(version-drift): 配对与判定拆成两步，同名候选按最优取"
```

---

## 任务 4：动作矩阵补「被配置引用」维与 move-with-refs

**文件：**
- 修改：`src/lib/acquire-match.ts`（`AcquireAction`、`AcquireRestriction`、`ACTION_ORDER`、`actionsFor`、`mergeGroupMatch`）
- 测试：`src/lib/acquire-match.test.ts`

**要点（规格 §6.1 的完整矩阵）：**

| 位置 | `referenced === false` | `referenced === true` |
|---|---|---|
| models 根内、不属于任何档案 | `[download, move, link]`，默认 `move` | `[download, link, move-with-refs]`，默认 `link` |
| 在别的档案目录内 | `[download, link]`，默认 `link` | 同左 |
| models 根外（自定义目录） | `[download, move, copy]`，默认 `copy` | 同左（根外恒为 false） |
| `drift === "different"` | `[download]`，restriction `version-drift` | 同左 |
| 远端无可用 oid | `[download]`，restriction `no-oid` | 同左 |

- [ ] **步骤 1：编写失败的测试**

```ts
describe("actionsFor · 被配置引用维与版本漂移", () => {
  const remote = { path: "a.gguf", size: 100, oid: OID_A };
  const loose = { inRepoDir: null, inModelsRoot: true };

  it("未被引用的未归档文件：默认移动（现状不变）", () => {
    const r = actionsFor(remote, { ...loose, drift: "same", referenced: false });
    expect(r.actions).toEqual(["download", "move", "link"]);
    expect(r.defaultAction).toBe("move");
  });

  it("被配置引用：裸 move 消失、补入 move-with-refs、默认降为 link", () => {
    const r = actionsFor(remote, { ...loose, drift: "same", referenced: true });
    expect(r.actions).toEqual(["download", "move-with-refs", "link"]);
    expect(r.defaultAction).toBe("link");
  });

  it("move-with-refs 永远不做默认动作（它是显式的 opt-in）", () => {
    const r = actionsFor(remote, { ...loose, drift: "unknown", referenced: true });
    expect(r.defaultAction).not.toBe("move-with-refs");
  });

  it("在别的档案目录内：引用与否都只有 download/link", () => {
    const inRepo = { inRepoDir: "hf/u/r", inModelsRoot: true };
    for (const referenced of [true, false]) {
      const r = actionsFor(remote, { ...inRepo, drift: "same", referenced });
      expect(r.actions).toEqual(["download", "link"]);
      expect(r.restriction).toBe("in-repo");
    }
  });

  it("models 根外：不受 referenced 影响", () => {
    const outside = { inRepoDir: null, inModelsRoot: false };
    const r = actionsFor(remote, { ...outside, drift: "same", referenced: false });
    expect(r.actions).toEqual(["download", "move", "copy"]);
    expect(r.defaultAction).toBe("copy");
    expect(r.restriction).toBe("outside-root");
  });

  it("drift 为 different：只能下载，限制码 version-drift（优先级高于位置）", () => {
    const r = actionsFor(remote, { ...loose, drift: "different", referenced: false });
    expect(r.actions).toEqual(["download"]);
    expect(r.restriction).toBe("version-drift");
  });

  it("远端无 oid 时仍是 no-oid，不被 version-drift 顶掉", () => {
    const r = actionsFor({ path: "a.gguf", size: 100 }, { ...loose, drift: "unknown", referenced: false });
    expect(r.actions).toEqual(["download"]);
    expect(r.restriction).toBe("no-oid");
  });

  it("候选为 null：只能下载，限制码 none", () => {
    const r = actionsFor(remote, null);
    expect(r.actions).toEqual(["download"]);
    expect(r.restriction).toBe("none");
  });
});

describe("mergeGroupMatch · 新动作参与交集", () => {
  it("组内两片都被引用 → 组级给 move-with-refs，默认 link", () => {
    const facts = { inRepoDir: null, inModelsRoot: true, drift: "same" as const, referenced: true };
    const f = (file: string) => ({
      file,
      candidate: makeCandidate({ absPath: `/m/loose/${file}`, rel: `loose/${file}`, size: 100 }),
      drift: "same" as const,
      ...actionsFor({ path: file, size: 100, oid: OID_A }, facts),
    });
    const g = mergeGroupMatch("Q4_K_M", "model", [f("a-1.gguf"), f("a-2.gguf")]);
    expect(g.actions).toEqual(["download", "move-with-refs", "link"]);
    expect(g.defaultAction).toBe("link");
  });

  it("一片被引用一片没有 → 交集里没有 move 也没有 move-with-refs，默认 link", () => {
    const mk = (file: string, referenced: boolean) => ({
      file,
      candidate: makeCandidate({ absPath: `/m/loose/${file}`, rel: `loose/${file}`, size: 100 }),
      drift: "same" as const,
      ...actionsFor(
        { path: file, size: 100, oid: OID_A },
        { inRepoDir: null, inModelsRoot: true, drift: "same", referenced },
      ),
    });
    const g = mergeGroupMatch("Q4_K_M", "model", [mk("a-1.gguf", true), mk("a-2.gguf", false)]);
    expect(g.actions).toEqual(["download", "link"]);
    expect(g.defaultAction).toBe("link");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/acquire-match.test.ts`
预期：FAIL——`actionsFor` 现在是两参数、不认 `move-with-refs` 与 `version-drift`。

- [ ] **步骤 3：编写实现**

```ts
export type AcquireAction = "download" | "move" | "move-with-refs" | "link" | "copy";

export type AcquireRestriction = "none" | "no-oid" | "in-repo" | "outside-root" | "version-drift";

/** 下拉展示顺序：文件级与组级共用这一份 */
const ACTION_ORDER: readonly AcquireAction[] = [
  "download",
  "move",
  "move-with-refs",
  "link",
  "copy",
];

/**
 * 默认动作偏好序（与展示顺序无关）。move-with-refs 刻意排在 download 之前、
 * 其余之后：它会改写模型配置，属于显式 opt-in，任何时候都不该被自动选中。
 */
const DEFAULT_PREFERENCE: readonly AcquireAction[] = [
  "move",
  "link",
  "copy",
  "move-with-refs",
  "download",
];

/**
 * actionsFor 消费的候选事实：位置（在不在 models 根 / 在不在某个档案里）、
 * 与远端的版本关系、以及是否被模型配置引用。
 *
 * 之所以传「事实」而不是完整的 LocalCandidate：服务端第四道重验
 * （acquireGuard.assertActionAllowed）手里只有对源路径实测出来的这几项，
 * 没有 rel/hostPath 之类的展示字段，不该为了调用本函数伪造一个完整候选。
 */
export interface CandidateFacts extends CandidateLocation {
  drift: DriftState;
  referenced: boolean;
}

export function actionsFor(remote: RemoteFileRef, facts: CandidateFacts | null): ActionsResult {
  const onlyDownload = (restriction: AcquireRestriction): ActionsResult => ({
    actions: ["download"],
    defaultAction: "download",
    restriction,
  });

  if (facts === null) return onlyDownload("none");

  // 无 oid 先于 version-drift：两者都只给下载，但原因不同，而「远端根本没有校验值」
  // 是更根本的那个——此时 drift 必然是 unknown，说「版本不符」会是假话
  if (remote.oid === undefined || !SHA256_PATTERN.test(remote.oid)) return onlyDownload("no-oid");

  // 内容确定不同：挪一份不是这个版本的文件进档案目录，会让档案里那份与远端
  // 声明的 size/oid 对不上，后续任何校验都失败。用户确知自己在做什么时走
  // 手动关联（规格 §7），那条路径显式放宽这一条
  if (facts.drift === "different") return onlyDownload("version-drift");

  const pick = (allowed: readonly AcquireAction[], restriction: AcquireRestriction): ActionsResult => {
    const actions = ACTION_ORDER.filter((a) => allowed.includes(a));
    return {
      actions,
      defaultAction: DEFAULT_PREFERENCE.find((a) => actions.includes(a)) ?? "download",
      restriction,
    };
  };

  if (!facts.inModelsRoot) {
    // 跨挂载点：rename 抛 EXDEV、硬链接跨文件系统不成立，只剩复制系动作。
    // referenced 在这里恒为 false（见 LocalCandidate.referenced 注释），不必分支
    return pick(["download", "copy", "move"], "outside-root");
  }

  if (facts.inRepoDir !== null) {
    // 移走会掏空那个档案（planFileMove 本就拒绝从档案目录移出）
    return pick(["download", "link"], "in-repo");
  }

  // 未归档文件。被配置引用时裸 move 会让配置悬空（localAcquire 不碰 models 表），
  // 换成显式的 move-with-refs；默认降为 link——原路径与档案路径同 inode，
  // 配置照旧可用、不额外占盘，是这个场景下的正确默认
  return facts.referenced
    ? pick(["download", "move-with-refs", "link"], "none")
    : pick(["download", "move", "link"], "none");
}
```

`mergeGroupMatch` 里把 `const order: AcquireAction[] = [...]` 改成复用 `ACTION_ORDER`，默认动作改用 `DEFAULT_PREFERENCE`（删掉本地那份 `preference`）。交集与 `basis` 逻辑不动。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/lib/acquire-match.test.ts`
预期：PASS。既有 `actionsFor` 用例改成传 `CandidateFacts`（补 `drift: "same"` 与 `referenced: false` 即保持原语义），逐个改，不删。

- [ ] **步骤 5：Commit**

```bash
git add src/lib/acquire-match.ts src/lib/acquire-match.test.ts
git commit -m "feat(version-drift): 动作矩阵补被引用维与 move-with-refs"
```

---

## 任务 5：本地 oid 三来源解析

**文件：**
- 创建：`src/server/localOid.ts`、`src/server/localOid.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { mkdirSync, mkdtempSync, rmSync, utimesSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import { resolveLocalOid } from "./localOid";

const OID = "cbb841a9ee0636b2ec172f5bb8df2ea8dfeb01e90fe7c6126581d662a0b4e43e";
let root: string;

function writeGguf(rel: string, body = "x"): string {
  const abs = path.join(root, rel);
  mkdirSync(path.dirname(abs), { recursive: true });
  writeFileSync(abs, body);
  return abs;
}

function writeSidecar(rel: string, etag: string): string {
  const abs = path.join(root, rel);
  const dir = path.join(path.dirname(abs), ".cache/huggingface/download");
  mkdirSync(dir, { recursive: true });
  const p = path.join(dir, `${path.basename(abs)}.metadata`);
  writeFileSync(p, `f1bfb127c64f7072bdd2cad55f258b9c8b2910fe\n${etag}\n1786945907.547407\n`);
  return p;
}

beforeEach(() => {
  root = mkdtempSync(path.join(tmpdir(), "localoid-"));
});
afterEach(() => {
  rmSync(root, { recursive: true, force: true });
});

describe("resolveLocalOid", () => {
  it("缓存优先：file_meta 有值时不读边车", () => {
    const abs = writeGguf("loose/a.gguf");
    writeSidecar("loose/a.gguf", "b".repeat(64));
    expect(resolveLocalOid(abs, "a".repeat(64))).toBe("a".repeat(64));
  });

  it("缓存为空时读边车", () => {
    const abs = writeGguf("loose/a.gguf");
    writeSidecar("loose/a.gguf", OID);
    expect(resolveLocalOid(abs, null)).toBe(OID);
  });

  it("没有边车 → null（不算哈希）", () => {
    const abs = writeGguf("loose/a.gguf");
    expect(resolveLocalOid(abs, null)).toBeNull();
  });

  it("文件在边车之后被改过 → null", () => {
    const abs = writeGguf("loose/a.gguf");
    const side = writeSidecar("loose/a.gguf", OID);
    utimesSync(side, new Date(1_000_000), new Date(1_000_000));
    utimesSync(abs, new Date(2_000_000), new Date(2_000_000));
    expect(resolveLocalOid(abs, null)).toBeNull();
  });

  it("边车格式变形 → null，不抛错", () => {
    const abs = writeGguf("loose/a.gguf");
    const dir = path.join(root, "loose/.cache/huggingface/download");
    mkdirSync(dir, { recursive: true });
    writeFileSync(path.join(dir, "a.gguf.metadata"), "{\"etag\": \"whatever\"}");
    expect(resolveLocalOid(abs, null)).toBeNull();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/server/localOid.test.ts`
预期：FAIL，`Failed to resolve import "./localOid"`

- [ ] **步骤 3：编写实现**

```ts
import { readFileSync, statSync } from "node:fs";
import path from "node:path";
import { parseHfSidecar } from "@/lib/hf-sidecar";

/**
 * 本地文件内容 oid 的解析（规格 §4.2）。**零哈希计算**——只读元数据。
 *
 * 优先级：
 * 1. `file_meta.full_sha256`（面板下载的由 download_tasks.sha256 播种）——由调用方传入
 * 2. hf CLI 的下载边车（同目录 `.cache/huggingface/download/<basename>.metadata`）
 * 3. 都没有 → null。补算完整哈希是用户显式动作，走 POST /api/v1/file-meta/checksum
 *    那条 202 路径，不在这里偷偷读满 17 GB（迁移设计最终审查 I7 的教训）
 *
 * 任何 IO 失败一律当「没有这份证据」处理：这是个尽力而为的加速通道，
 * 不该因为一个权限问题让整页 500。
 */
export function resolveLocalOid(absPath: string, cachedFullSha256: string | null): string | null {
  if (cachedFullSha256 !== null) return cachedFullSha256;

  const sidecarPath = path.join(
    path.dirname(absPath),
    ".cache/huggingface/download",
    `${path.basename(absPath)}.metadata`,
  );
  try {
    return parseHfSidecar(readFileSync(sidecarPath, "utf8"), {
      fileMtimeMs: statSync(absPath).mtimeMs,
      sidecarMtimeMs: statSync(sidecarPath).mtimeMs,
    });
  } catch {
    return null;
  }
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/server/localOid.test.ts`
预期：PASS，5 条

- [ ] **步骤 5：Commit**

```bash
git add src/server/localOid.ts src/server/localOid.test.ts
git commit -m "feat(version-drift): 本地 oid 三来源解析（零哈希计算）"
```

---

## 任务 6：候选带 referenced 与 oid，并返回未归档候选池

**文件：**
- 修改：`src/server/scanCandidates.ts`
- 测试：`src/server/scanCandidates.test.ts`

- [ ] **步骤 1：编写失败的测试**

在既有测试文件里追加（沿用该文件已有的 `makeArgs` / 临时目录夹具，先 grep 对齐实际名字）：

```ts
  it("候选带 referenced：引用集合命中的置 true，其余 false", () => {
    // 夹具在 models 根内造 loose/a.gguf 与 loose/b.gguf
    const { candidates } = collectScanCandidates({
      ...makeArgs(),
      referencedRels: new Set(["loose/a.gguf"]),
    });
    expect(candidates.find((c) => c.rel === "loose/a.gguf")?.referenced).toBe(true);
    expect(candidates.find((c) => c.rel === "loose/b.gguf")?.referenced).toBe(false);
  });

  it("models 根外的候选 referenced 恒为 false", () => {
    const { candidates } = collectScanCandidates({
      ...makeArgs({ extraHostDirs: ["/host/extra"] }),
      // 即便把根外文件的 rel 塞进引用集合也不该生效——根外文件不可能被配置引用
      referencedRels: new Set(["extra.gguf"]),
    });
    expect(candidates.filter((c) => !c.inModelsRoot).every((c) => c.referenced === false)).toBe(true);
  });

  it("unarchived 只含 models 根内、不属于任何档案的候选", () => {
    const { unarchived } = collectScanCandidates({
      ...makeArgs({ repoDirs: ["hf/u/r"] }),
    });
    expect(unarchived.every((c) => c.inModelsRoot && c.inRepoDir === null)).toBe(true);
    expect(unarchived.some((c) => c.rel === "hf/u/r/inside.gguf")).toBe(false);
  });
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/server/scanCandidates.test.ts`
预期：FAIL——`referencedRels` 不是合法入参，`unarchived` 不存在。

- [ ] **步骤 3：编写实现**

`ScanCandidatesArgs` 补：

```ts
  /** models 根相对路径的引用集合（server/filesApi.ts 的 buildRefMap 的键）。
   *  只对根内候选生效——根外文件不可能被 gguf_file 引用（ggufPathSchema 要求
   *  根内相对路径），塞进来也会被忽略 */
  referencedRels: ReadonlySet<string>;
```

`ScanCandidatesResult` 补：

```ts
  /** 未归档候选池（规格 §7.2）：models 根内、不属于任何档案目录的文件，
   *  供手动关联弹层直接取用。深度扫描已经遍历过整棵树，这批是白拿的，
   *  不必为手动关联另开一次扫盘 */
  unarchived: LocalCandidate[];
```

models 根那一路的 `candidates.push` 补 `referenced: args.referencedRels.has(f.rel)`；
根外那一路补 `referenced: false` 并加注释说明为什么不查表。
函数末尾：

```ts
  const unarchived = candidates.filter((c) => c.inModelsRoot && c.inRepoDir === null);
  return { candidates, unreachable, unarchived };
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/server/scanCandidates.test.ts`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/scanCandidates.ts src/server/scanCandidates.test.ts
git commit -m "feat(version-drift): 候选带引用标记，另出未归档候选池"
```

---

## 任务 7：scan 路由接线

**文件：**
- 修改：`src/app/api/v1/repos/[id]/scan/route.ts`

**要点：** 这是任务 3–6 的汇合点。候选的 `fullSha256` 现在要经 `resolveLocalOid` 补上边车来源；`matchLocalCandidate` 返回值形状变了；`actionsFor` 要传 `CandidateFacts`；响应增 `unarchived`。

- [ ] **步骤 1：改造候选构建与匹配**

`collectScanCandidates` 调用处补 `referencedRels`：

```ts
  const modelsRoot = getPanelModelsRoot();
  const refMap = buildRefMap(db, modelsRoot);
  const metaByRel = new Map(listFileMetaRows(db).map((r) => [r.path, r.fullSha256]));

  const { candidates, unreachable, unarchived } = collectScanCandidates({
    modelsRoot,
    extraHostDirs,
    repoDirs: listRepoDirs(db),
    // 缓存值取不到时回落到 hf CLI 边车：真机上绝大多数既有权重是用 hf CLI 下的，
    // download_tasks 里根本没有记录，只靠 file_meta 会让 drift 全是 unknown
    fullSha256ByRel: new Map(
      [...metaByRel.keys()].map((rel) => [
        rel,
        resolveLocalOid(path.join(modelsRoot, rel), metaByRel.get(rel) ?? null),
      ]),
    ),
    referencedRels: new Set(refMap.keys()),
    toHost,
    toPanel,
  });
```

**注意 `fullSha256ByRel` 的键集合问题**：`listFileMetaRows` 只覆盖登记过的路径，而候选来自整棵树。改为在 `collectScanCandidates` **内部**对每个根内候选调用一次 `resolveLocalOid` 更直接——但那样会把 IO 塞进一个原本零 IO 的模块。**采用的做法**：给 `ScanCandidatesArgs` 增一个可选注入点

```ts
  /** 根内候选的 oid 解析（rel + 绝对路径 → oid）。生产传 server/localOid.resolveLocalOid
   *  的封装，测试可注入假实现。缺省一律返回 null（等价于旧行为） */
  resolveOid?: (rel: string, absPath: string) => string | null;
```

并在 models 根那一路用 `args.resolveOid?.(f.rel, absPath) ?? null` 替换原来的
`fullSha256ByRel.get(f.rel) ?? null`（`fullSha256ByRel` 保留，由路由把缓存查表包进
`resolveOid` 里一并给出）。这样读盘仍在 server 层，`scanCandidates` 的纯度不变。

路由侧：

```ts
    resolveOid: (rel, absPath) => resolveLocalOid(absPath, metaByRel.get(rel) ?? null),
```

- [ ] **步骤 2：改造分组与响应**

```ts
  const groups = remoteResult.groups.map((g) => {
    const files = g.files.map((rf) => {
      const hit = matchLocalCandidate(rf, candidates);
      const facts =
        hit === null
          ? null
          : {
              inRepoDir: hit.candidate.inRepoDir,
              inModelsRoot: hit.candidate.inModelsRoot,
              drift: hit.drift,
              referenced: hit.candidate.referenced,
            };
      return {
        file: rf.path,
        candidate: hit?.candidate ?? null,
        drift: hit?.drift ?? null,
        ...actionsFor(rf, facts),
      };
    });
    return mergeGroupMatch(g.quant, g.kind, files);
  });

  return NextResponse.json({
    groups,
    unreachable,
    availableMounts: getDiscoveredMounts(),
    // 手动关联弹层的候选池（规格 §7.2）：models 内全部未归档文件，不限名不限大小
    unarchived,
  });
```

同步更新路由头注释：说明 drift 三值、候选池、以及 oid 的解析来源。

- [ ] **步骤 3：验证**

运行：`npx tsc --noEmit` → 0；`npx vitest run src/server src/lib` → 全绿

- [ ] **步骤 4：Commit**

```bash
git add src/app/api/v1/repos/\[id\]/scan/route.ts src/server/scanCandidates.ts
git commit -m "feat(version-drift): scan 路由接入三值判定与未归档候选池"
```

---

## 任务 8：档案页四路数据带上 drift

**文件：**
- 修改：`src/app/api/v1/repos/[id]/files/route.ts`、`src/lib/repo-files-view.ts`
- 测试：`src/lib/repo-files-view.test.ts`

**要点：** 这是规格 §5.1 的落点——档案目录**内**的文件现在只按 basename 匹配、一律显示「已下载」。要让它能显示「有更新」，`local[]` 必须带 drift。

- [ ] **步骤 1：编写失败的测试**

```ts
  it("档案目录内的文件 drift 为 different → 行状态 present 且 hasUpdate 为 true", () => {
    const rows = mergeRepoRows({
      ...baseInput(),
      groups: [{ quant: "Q4_K_M", kind: "model", files: [{ path: "a.gguf", size: 100, oid: OID_A }] }],
      local: [{ rel: "hf/u/r/a.gguf", size: 200, drift: "different" }],
    });
    expect(rows[0].state).toBe("present");
    expect(rows[0].hasUpdate).toBe(true);
    expect(rows[0].unverified).toBe(false);
  });

  it("drift 为 unknown → present 且 unverified 为 true，不算有更新", () => {
    const rows = mergeRepoRows({
      ...baseInput(),
      groups: [{ quant: "Q4_K_M", kind: "model", files: [{ path: "a.gguf", size: 100, oid: OID_A }] }],
      local: [{ rel: "hf/u/r/a.gguf", size: 100, drift: "unknown" }],
    });
    expect(rows[0].hasUpdate).toBe(false);
    expect(rows[0].unverified).toBe(true);
  });

  it("drift 缺省（旧夹具不传）→ 两个标志都是 false，行为与改动前一致", () => {
    const rows = mergeRepoRows({
      ...baseInput(),
      groups: [{ quant: "Q4_K_M", kind: "model", files: [{ path: "a.gguf", size: 100, oid: OID_A }] }],
      local: [{ rel: "hf/u/r/a.gguf", size: 100 }],
    });
    expect(rows[0].hasUpdate).toBe(false);
    expect(rows[0].unverified).toBe(false);
  });

  it("分片组只要有一片 different，整组算有更新", () => {
    const rows = mergeRepoRows({
      ...baseInput(),
      groups: [
        {
          quant: "Q4_K_M",
          kind: "model",
          files: [
            { path: "a-1.gguf", size: 100, oid: OID_A },
            { path: "a-2.gguf", size: 100, oid: OID_B },
          ],
        },
      ],
      local: [
        { rel: "hf/u/r/a-1.gguf", size: 100, drift: "same" },
        { rel: "hf/u/r/a-2.gguf", size: 100, drift: "different" },
      ],
    });
    expect(rows[0].hasUpdate).toBe(true);
  });
```

`baseInput()` 是该文件已有的夹具助手——grep 对齐实际名字。

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/repo-files-view.test.ts`
预期：FAIL——`RepoRow` 没有 `hasUpdate` / `unverified`。

- [ ] **步骤 3：编写实现**

`RepoRowInput.local[]` 元素补可选 `drift?: DriftState`（可选，理由与既有 `sharedWith` 同款：不逼所有夹具补齐）。

`RepoRow` 补：

```ts
  /** 组内有文件确定不是远端当前版本（规格 §5.1）。state 仍是 present——
   *  它确实已下载，只是版本旧了，显示成「未下载」会把这条信息弄丢 */
  hasUpdate: boolean;
  /** 组内有文件拿不到 oid、无从判定版本；与 hasUpdate 互斥展示（有更新优先） */
  unverified: boolean;
```

在 `mergeRepoRows` 的 `for (const file of group.files)` 循环里，`local !== undefined` 分支内累加：

```ts
        if (local.drift === "different") anyDifferent = true;
        else if (local.drift === "unknown" || local.drift === undefined) anyUnknown = true;
```

**注意**：`drift === undefined`（旧夹具/未接线的调用方）必须落进 `anyUnknown` 而不是
`anyDifferent`，但最终 `unverified` 只在**至少有一个文件显式给了 drift** 时才为 true，
否则第三条用例会红。用一个 `sawDrift` 标志区分：

```ts
    let sawDrift = false;
    let anyDifferent = false;
    let anyUnknown = false;
    // ... 循环内：
        if (local.drift !== undefined) {
          sawDrift = true;
          if (local.drift === "different") anyDifferent = true;
          else if (local.drift === "unknown") anyUnknown = true;
        }
    // ... 返回：
    hasUpdate: anyDifferent,
    unverified: sawDrift && !anyDifferent && anyUnknown,
```

路由侧：`scanRepoFiles` 产出的 `local[]` 逐条补 drift。需要远端清单做基准——
`files/route.ts` 已经取了 `remote`，按 basename 找到对应远端文件后调
`compareToRemote({ size, oid: resolveLocalOid(abs, cached) }, remoteFile)`。
远端不可达（`remote.groups === null`）时**不补 drift**（保持 undefined），
理由与该路由既有的「远端不可达时 strays 用宽口径」一致：没有基准就不假装能判。

`strays[]` 同样补 `drift`，供任务 12 的「版本不符」展示。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/lib/repo-files-view.test.ts`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/lib/repo-files-view.ts src/lib/repo-files-view.test.ts src/app/api/v1/repos/\[id\]/files/route.ts
git commit -m "feat(version-drift): 档案页四路数据带上版本关系"
```

---

## 任务 9：服务端第四道重验接入新维度

**文件：**
- 修改：`src/server/acquireGuard.ts`
- 测试：`src/server/acquireGuard.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
  it("被配置引用的未归档源：裸 move 被拒，move-with-refs 放行", () => {
    const facts = { inRepoDir: null, inModelsRoot: true, drift: "same" as const, referenced: true };
    expect(() => assertActionAllowed("move", remote, facts)).toThrow(AcquireGuardError);
    expect(() => assertActionAllowed("move-with-refs", remote, facts)).not.toThrow();
  });

  it("未被引用的未归档源：裸 move 放行，move-with-refs 被拒（没有引用可改）", () => {
    const facts = { inRepoDir: null, inModelsRoot: true, drift: "same" as const, referenced: false };
    expect(() => assertActionAllowed("move", remote, facts)).not.toThrow();
    expect(() => assertActionAllowed("move-with-refs", remote, facts)).toThrow(AcquireGuardError);
  });

  it("drift 为 different 时一切搬运动作被拒（走手动关联才放宽）", () => {
    const facts = { inRepoDir: null, inModelsRoot: true, drift: "different" as const, referenced: false };
    for (const a of ["move", "link", "copy", "move-with-refs"] as const) {
      expect(() => assertActionAllowed(a, remote, facts)).toThrow(AcquireGuardError);
    }
  });

  it("错误码是 ACTION_NOT_ALLOWED", () => {
    const facts = { inRepoDir: "hf/u/r", inModelsRoot: true, drift: "same" as const, referenced: false };
    try {
      assertActionAllowed("move", remote, facts);
      throw new Error("应当抛错");
    } catch (e) {
      expect((e as AcquireGuardError).code).toBe("ACTION_NOT_ALLOWED");
    }
  });
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/server/acquireGuard.test.ts`
预期：FAIL——签名还是旧的 `CandidateLocation`。

- [ ] **步骤 3：编写实现**

把 `assertActionAllowed` 的第三参从 `CandidateLocation` 换成 `CandidateFacts`，内部仍旧
`actionsFor(remote, facts)` 后判 `actions.includes(action)`。**不要**在这里复写矩阵——
矩阵只有 `lib/acquire-match.ts` 一份真源，这是迁移设计最终审查 I2 修复时立下的口径。

更新头注释：说明第四道重验现在同时覆盖位置、版本关系、引用状态三维，
以及手动关联为什么只放宽版本关系那一维。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/server/acquireGuard.test.ts`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/acquireGuard.ts src/server/acquireGuard.test.ts
git commit -m "feat(version-drift): 服务端动作矩阵重验接入版本关系与引用状态"
```

---

## 任务 10：执行器与队列支持可空 sha256 及 move-with-refs

**文件：**
- 修改：`src/server/download/localAcquire.ts`、`src/server/download/manager.ts`
- 测试：`src/server/download/localAcquire.test.ts`、`src/server/download/manager.test.ts`

- [ ] **步骤 1：编写失败的测试**

`localAcquire.test.ts`：

```ts
  it("sha256 为 null：仍读满全文算哈希，但不做比对，结果里带算出的值", async () => {
    const { sourcePath, targetPath } = makeFiles("hello");
    const handle = runLocalAcquire({
      sourcePath,
      targetPath,
      action: "copy",
      sameFs: true,
      expectedSize: 5,
      sha256: null,
    });
    const r = await handle.result;
    expect(r.ok).toBe(true);
    expect(r.sha256).toBe(createHash("sha256").update("hello").digest("hex"));
    expect(r.sha256Verified).toBe("skipped");
  });

  it("sha256 为 null 时大小校验仍然生效", async () => {
    const { sourcePath, targetPath } = makeFiles("hello");
    const handle = runLocalAcquire({
      sourcePath,
      targetPath,
      action: "copy",
      sameFs: true,
      expectedSize: 999,
      sha256: null,
    });
    await expect(handle.result).rejects.toThrow(/大小不符/);
  });
```

`manager.test.ts`：

```ts
  it("move-with-refs 物理上执行为 move：源消失、目标出现", async () => {
    // 夹具：models 根内 loose/a.gguf，档案目录 hf/u/r
    await manager.enqueueLocal({
      items: [
        {
          file: "a.gguf",
          sourcePath: path.join(root, "loose/a.gguf"),
          action: "move-with-refs",
          sameFs: true,
          size: 5,
          sha256: OID_OF_HELLO,
        },
      ],
      targetDir: "hf/u/r",
      label: "u/r",
    });
    await drainQueue();
    expect(existsSync(path.join(root, "loose/a.gguf"))).toBe(false);
    expect(existsSync(path.join(root, "hf/u/r/a.gguf"))).toBe(true);
  });

  it("sha256 为空的 local 任务（手动关联）完成后把算出的哈希写回任务行", async () => {
    await manager.enqueueLocal({
      items: [
        {
          file: "a.gguf",
          sourcePath: path.join(root, "loose/a.gguf"),
          action: "link",
          sameFs: true,
          size: 5,
          sha256: null,
        },
      ],
      targetDir: "hf/u/r",
      label: "u/r",
    });
    await drainQueue();
    const row = db.prepare("SELECT sha256 FROM download_tasks WHERE file = 'a.gguf'").get();
    expect(row.sha256).toBe(OID_OF_HELLO);
  });
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/server/download`
预期：FAIL——`sha256` 类型不接受 null；`local_action` 转型不认 `move-with-refs`。

- [ ] **步骤 3：编写实现**

`localAcquire.ts`：

```ts
  /** 期望 sha256（远端 LFS oid）。**null 表示不做比对**——手动关联（规格 §7）时
   *  用户已声明这份文件是对的，内容校验正是那一条被显式放宽的约束。仍然读满全文
   *  算哈希（进度语义不变、算出的值要回写供 file_meta 播种），只是不比 */
  sha256: string | null;
```

比对处：

```ts
    const actual = hash.digest("hex");
    if (req.sha256 !== null && actual !== req.sha256) {
      throw new Error(`内容不符：期望 sha256 ${req.sha256}，实际 ${actual}`);
    }
    // ...
    return {
      ok: true,
      bytes: read,
      sha256: actual,
      sha256Verified: req.sha256 === null ? "skipped" : "match",
      resumedFrom: 0,
    };
```

`sha256Verified` 的 `"skipped"` 取值：grep `sha256Verified` 确认联合类型的实际定义位置，
若尚无该取值则补上（下载器那侧不受影响，它永远给 match/mismatch）。

`manager.ts`：
- `EnqueueLocalItem.action` 类型改为 `"move" | "move-with-refs" | "link" | "copy"`
- `EnqueueLocalItem.sha256` 改为 `string | null`
- `DownloadTaskView.localAction` 同步加 `"move-with-refs"`
- 磁盘预检的 `copyTotal` 判据里 `it.action === "move"` 改为 `(it.action === "move" || it.action === "move-with-refs")`
- `buildLocalRequest`：

```ts
      // move-with-refs 物理上就是 move；差别只在完成之后要重写配置引用（见完成回调）。
      // 执行器不碰 db，这个区分不该泄漏进去
      action: task.local_action === "move-with-refs" ? "move" : (task.local_action as "move" | "link" | "copy"),
      sha256: task.sha256,
```

- 完成回调（`stmt.setFinished` 那条 completed 分支）内，`task.source === "local"` 且
  `task.sha256 === null` 时把 `result.sha256` 写回任务行：

```ts
          // 手动关联的任务入队时没有可比对的 oid（sha256 为 NULL）。执行器已经读满
          // 全文算过一遍，把它写回任务行——upsertFileMeta 本就会从「completed 且
          // sha256 非空」的下载任务播种 full_sha256，写回去这条缓存就自动到位了，
          // 不必另开一条写 file_meta 的路径
          if (next.source === "local" && next.sha256 === null && result.sha256) {
            stmt.setTaskSha256.run({ id: next.id, sha256: result.sha256 });
          }
```

新增 prepared statement `setTaskSha256`：`UPDATE download_tasks SET sha256 = @sha256 WHERE id = @id`。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/server/download`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/download/localAcquire.ts src/server/download/localAcquire.test.ts src/server/download/manager.ts src/server/download/manager.test.ts
git commit -m "feat(version-drift): 执行器支持免比对，队列识别 move-with-refs"
```

---

## 任务 11：移动后的配置引用事务重写

**文件：**
- 创建：`src/server/refRewrite.ts`、`src/server/refRewrite.test.ts`
- 修改：`src/server/download/manager.ts`（完成回调接线）

**要点：** `move-with-refs` 完成后，把指向旧路径的模型配置改指到新路径。
**glob 引用一律拒绝**——配置字段若是 `main/m1-*.gguf` 这样的分片 glob，把它改写成
单个新路径会毁掉组内其余分片的解析。这类情况让用户走档案页的「归位」（`planFileMove`
本就是整组语义）。

- [ ] **步骤 1：编写失败的测试**

```ts
import { describe, expect, it } from "vitest";
import { rewriteFileRefs, RefRewriteError } from "./refRewrite";

describe("rewriteFileRefs", () => {
  it("精确路径引用：改指到新路径，file_meta 一并迁移", () => {
    addModel({ name: "m1", gguf_file: "loose/a.gguf" });
    upsertMetaRow("loose/a.gguf");
    const n = rewriteFileRefs(db, "loose/a.gguf", "hf/u/r/a.gguf");
    expect(n).toBe(1);
    expect(repo.getModel("m1")?.gguf_file).toBe("hf/u/r/a.gguf");
    expect(metaPaths()).toContain("hf/u/r/a.gguf");
    expect(metaPaths()).not.toContain("loose/a.gguf");
  });

  it("多个模型引用同一文件：全部改指", () => {
    addModel({ name: "m1", gguf_file: "loose/a.gguf" });
    addModel({ name: "m2", mmproj_file: "loose/a.gguf" });
    expect(rewriteFileRefs(db, "loose/a.gguf", "hf/u/r/a.gguf")).toBe(2);
    expect(repo.getModel("m1")?.gguf_file).toBe("hf/u/r/a.gguf");
    expect(repo.getModel("m2")?.mmproj_file).toBe("hf/u/r/a.gguf");
  });

  it("glob 引用：抛 RefRewriteError，一个配置都不改", () => {
    addModel({ name: "m1", gguf_file: "loose/a-*.gguf" });
    expect(() => rewriteFileRefs(db, "loose/a-00001-of-00002.gguf", "hf/u/r/x.gguf")).toThrow(
      RefRewriteError,
    );
    expect(repo.getModel("m1")?.gguf_file).toBe("loose/a-*.gguf");
  });

  it("没有任何引用：返回 0，不抛错", () => {
    expect(rewriteFileRefs(db, "loose/orphan.gguf", "hf/u/r/orphan.gguf")).toBe(0);
  });
});
```

夹具（`addModel` / `repo` / `db` / `upsertMetaRow` / `metaPaths`）照抄 `src/server/filesApi.test.ts`
的同款写法——先读那个文件对齐实际形态，不要另发明一套。

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/server/refRewrite.test.ts`
预期：FAIL，`Failed to resolve import "./refRewrite"`

- [ ] **步骤 3：编写实现**

```ts
import type Database from "better-sqlite3";
import { createModelRepo } from "./repo/models";

/**
 * 一个文件换了位置之后，把指向它的模型配置改指到新路径（规格 §6.2）。
 *
 * 与 fileMove.ts 的区别：那里是「用户在文件页整组移动」，物理搬运与引用重写
 * 一起做且保持文件名不变；这里是 acquire 的 move-with-refs，物理搬运已经由
 * 队列执行器完成（且**会改名**——档案目录里必须用远端那个文件名），本函数只
 * 负责配置那一半。两者共用同一个 db.transaction() 形态，不共用代码：把 renameSync
 * 和事务拆开会让 fileMove 的「全部 rename 成功后才写库」这条时序保证失效。
 *
 * glob 引用一律拒绝：`main/m1-*.gguf` 这类分片 glob 改写成单个新路径会毁掉组内
 * 其余分片的解析。这种情况该走档案页的「归位」（planFileMove 本就是整组语义）。
 */
export class RefRewriteError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "RefRewriteError";
  }
}

function hasGlob(value: string): boolean {
  return value.includes("*") || value.includes("?");
}

export function rewriteFileRefs(db: Database.Database, fromRel: string, toRel: string): number {
  const repo = createModelRepo(db);
  const targets: { name: string; field: "gguf_file" | "mmproj_file" }[] = [];

  for (const model of repo.listModels()) {
    for (const field of ["gguf_file", "mmproj_file"] as const) {
      const configured = model[field];
      if (configured === undefined) continue;
      if (configured === fromRel) {
        targets.push({ name: model.name, field });
      } else if (hasGlob(configured)) {
        // 展开后是否覆盖 fromRel 要读盘才知道，而此刻文件已经搬走了——保守起见
        // 只要存在 glob 引用就拒绝，宁可让用户走归位，也不留下一个半改的配置
        throw new RefRewriteError(
          `GLOB_REF: 模型 ${model.name} 的 ${field} 是分片 glob（${configured}），` +
            `不支持单文件改指，请到档案页用「归位」整组移动`,
        );
      }
    }
  }

  if (targets.length === 0) return 0;

  db.transaction(() => {
    for (const t of targets) repo.updateModel(t.name, { [t.field]: toRel });
    // file_meta 的键是配置字段原始值，配置改了这行也要跟着走，否则备注/哈希缓存
    // 会挂在一个不再存在的路径上（与 fileMove.ts 的 metaMoves 同一考虑）
    db.prepare("UPDATE file_meta SET path = ?, probe_path = ? WHERE path = ?").run(
      toRel,
      toRel,
      fromRel,
    );
  })();

  return targets.length;
}
```

`manager.ts` 完成回调接线（紧接任务 10 写回哈希那段之后）：

```ts
          if (next.source === "local" && next.local_action === "move-with-refs") {
            try {
              rewriteFileRefs(db, path.relative(modelsRoot, next.source_path!), next.target_rel);
            } catch (error) {
              // 文件已经搬到位、配置没改成：任务本身是成功的（字节确实到了），
              // 但用户必须知道配置还指着旧路径。记事件而不是把任务标失败——
              // 标失败会诱导用户重试，而重试只会撞上「源已不存在」
              record(EVENT_FAILED, `${next.label} 配置引用重写失败: ${next.file}: ${errMessage(error)}`);
            }
          }
```

**手动关联的备注**（规格 §7.4）同一处写入。手动关联的判据是「local 任务且入队时
`sha256` 为 NULL」——常规 local 任务的 oid 由 acquire 路由强制要求非空，所以这个
推导是稳的，不需要新增列：

```ts
          if (next.source === "local" && next.sha256 === null) {
            db.prepare(
              "UPDATE file_meta SET mark = COALESCE(NULLIF(mark, ''), ?) WHERE path = ?",
            ).run("手动关联，非远端当前版本", next.target_rel);
          }
```

注意这段要在写回 sha256**之前**读 `next.sha256`（`next` 是入队时的行快照，不受回写影响），
实现时确认这一点。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/server/refRewrite.test.ts src/server/download/manager.test.ts`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/server/refRewrite.ts src/server/refRewrite.test.ts src/server/download/manager.ts
git commit -m "feat(version-drift): move-with-refs 完成后事务重写配置引用"
```

---

## 任务 12：acquire 路由接入 manual 与新动作

**文件：**
- 修改：`src/app/api/v1/repos/[id]/acquire/route.ts`
- 测试：`src/server/acquireGuard.test.ts`（路由无测试基建，判定已在 guard 侧覆盖）

- [ ] **步骤 1：改 schema**

```ts
const itemSchema = z.strictObject({
  file: z.string().min(1),
  action: z.enum(["download", "move", "move-with-refs", "link", "copy"]),
  sourceHostPath: z.string().min(1).optional(),
  /** 手动关联（规格 §7）：用户已声明这份文件是对的，跳过 L1/L2 的内容校验。
   *  放宽的只有这一条——路径范围、档案归属、动作矩阵全部照旧且服务端自算 */
  manual: z.literal(true).optional(),
});
```

- [ ] **步骤 1b：把第二道重验下沉并补全（审查报告 Standards-1 + Spec-5）**

现在第二道重验（L1 复验）内联在路由里：

```ts
if (rf.oid === undefined || !SHA256_PATTERN.test(rf.oid) || st.size !== rf.size) { … MISMATCH … }
```

两个问题，都属实：
- **不可测**：全库没有路由测试基建，这段判定没有任何用例守着（违反 CLAUDE.md「可测判定
  一律下沉 `src/lib/*.ts`」与迁移设计 §12/§14.3「路由薄壳、判定下沉」的既定分工），
  且重复了 `lib/acquire-match.ts` 里的 oid 可用性判断。
- **残缺**：迁移设计 §8.1 要求复验「L1 仍然匹配（size 等于远端声明；**basename 或缓存哈希命中**）」，
  实现只查了 size + oid 存在性，没查配对那一半。本计划任务 3 把配对放宽成 basename 优先之后
  这一条更要紧——服务端必须自己确认「这个源确实配得上这个远端文件」，否则客户端可以把
  任意同尺寸文件塞给任意远端条目。

在 `src/lib/acquire-match.ts` 导出一个纯函数供两侧共用：

```ts
/**
 * 源文件与远端条目是否成对（服务端第二道重验用，迁移设计 §8.1）。
 *
 * 与 matchLocalCandidate 的配对判据必须是同一条——那边是「从一堆候选里挑」，
 * 这边是「验一个指定的源」，两处口径不一致会出现「扫描给得出、提交却被拒」
 * 的自相矛盾。所以这里就是把那条判据单独摘出来，不另写。
 */
export function pairsWithRemote(
  remote: RemoteFileRef,
  local: { basename: string; fullSha256: string | null },
): boolean {
  const oidUsable = remote.oid !== undefined && SHA256_PATTERN.test(remote.oid);
  return (
    local.basename === basename(remote.path) ||
    (oidUsable && local.fullSha256 !== null && local.fullSha256 === remote.oid)
  );
}
```

`matchLocalCandidate` 内部改用 `pairsWithRemote`，不留两份判据。

在 `src/server/acquireGuard.ts` 新增（连同 `assertActionAllowed` 一起被 guard 的测试覆盖）：

```ts
/**
 * 第二道重验：源与远端条目成对，且远端有可用的 oid、大小一致。
 * manual 为 true 时只验成对性——大小与内容正是手动关联显式放宽的那两条（规格 §8）。
 */
export function assertRemoteMatch(
  remote: RemoteFileRef,
  local: { basename: string; fullSha256: string | null; size: number },
  opts: { manual: boolean },
): void {
  if (!pairsWithRemote(remote, local)) {
    throw new AcquireGuardError("MISMATCH", `源文件与远端条目对不上: ${remote.path}`);
  }
  if (opts.manual) return;
  if (remote.oid === undefined || !SHA256_PATTERN.test(remote.oid) || local.size !== remote.size) {
    throw new AcquireGuardError("MISMATCH", "本地文件与远端声明不一致（大小或内容校验值）");
  }
}
```

`src/server/acquireGuard.test.ts` 补四条：成对但大小不符 → 抛；不成对 → 抛；
哈希命中而 basename 不同 → 不抛；`manual: true` + 大小不符但成对 → 不抛。

路由里把那个内联 if 换成 `assertRemoteMatch(rf, { basename: path.basename(realSourcePath), fullSha256: localOid, size: st.size }, { manual: item.manual === true })`，
并按既有模式把 `AcquireGuardError` 映射成 400。

- [ ] **步骤 2：改重验流程**

在既有三道重验之后、`assertActionAllowed` 之前插入 drift 计算：

```ts
    const localOid = resolveLocalOid(realSourcePath, cachedOidOf(realSourcePath));
    // manual 时不把版本关系当约束：unknown 的字面含义就是「没有可用于约束的内容证据」，
    // 而这正是手动关联的处境——用户比面板更懂，这一条被显式放宽（规格 §8）
    const drift = item.manual === true ? "unknown" : compareToRemote({ size: st.size, oid: localOid }, rf);

    // 第二道重验已在步骤 1b 下沉到 assertRemoteMatch，这里只调用，不再内联判据

    const inRepoDir = repoDirOf(path.relative(modelsRoot, realSourcePath), listRepoDirs(db));
    const facts = {
      inRepoDir,
      inModelsRoot: isInside(modelsRoot, realSourcePath),
      drift,
      // 服务端自己查引用表，不信前端（规格 §8）
      referenced: refMap.has(path.relative(modelsRoot, realSourcePath)),
    };
    try {
      assertActionAllowed(item.action, rf, facts);
    } catch (error) {
      if (error instanceof AcquireGuardError) return guardErrorResponse(error.code, item.file, error.message);
      throw error;
    }
```

**手动关联的额外硬约束**（规格 §7.2）——服务端必须自己立住，不依赖前端只列未归档文件：

```ts
    if (item.manual === true && inRepoDir !== null) {
      return guardErrorResponse(
        "ACTION_NOT_ALLOWED",
        item.file,
        `手动关联只接受未归档文件，该源属于档案 ${inRepoDir}`,
      );
    }
```

入队项：

```ts
    locals.push({
      file: item.file,
      sourcePath: realSourcePath,
      action: item.action,
      sameFs: isInside(modelsRoot, realSourcePath),
      size: rf.size,
      // manual 时没有可比对的 oid：执行器只算不比，算出的值由完成回调写回任务行，
      // 再经 upsertFileMeta 播种到 file_meta.full_sha256
      sha256: item.manual === true ? null : rf.oid,
    });
```

`size` 对 manual 项要用**实测的 `st.size`** 而不是 `rf.size`——否则执行器的大小校验
必然失败（手动关联的前提就是大小可能不同）。改为：

```ts
      size: item.manual === true ? st.size : rf.size,
```

- [ ] **步骤 3：更新头注释**

三道→四道已在上一批改过；这次补：drift 参与重验、manual 放宽的边界与不放宽的清单、
`ACTION_NOT_ALLOWED` 的两种成因。

- [ ] **步骤 4：验证**

运行：`npx tsc --noEmit` → 0；`npx vitest run src/server` → 全绿

- [ ] **步骤 5：Commit**

```bash
git add src/app/api/v1/repos/\[id\]/acquire/route.ts
git commit -m "feat(version-drift): acquire 接受手动关联并把版本关系纳入重验"
```

---

## 任务 13：提交项携带 manual

**文件：**
- 修改：`src/lib/acquire-plan.ts`
- 测试：`src/lib/acquire-plan.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
  it("手动关联的行：提交项带 manual 与 sourceHostPath，动作不被降级", () => {
    const row = makeRow({
      action: "link",
      actions: ["download", "link"],
      manual: true,
      files: [{ file: "a.gguf", candidate: cand("/host/loose/a.gguf"), drift: "different", actions: ["download"], defaultAction: "download", restriction: "version-drift" }],
    });
    const items = buildAcquireSubmitItems([row]);
    expect(items).toEqual([
      { file: "a.gguf", action: "link", sourceHostPath: "/host/loose/a.gguf", manual: true },
    ]);
  });

  it("非手动行不带 manual 键（服务端 strictObject 会拒绝多余键）", () => {
    const row = makeRow({ action: "link", actions: ["download", "link"], files: [okFile()] });
    expect(buildAcquireSubmitItems([row])[0]).not.toHaveProperty("manual");
  });

  it("手动行的动作若不在 actions 里仍降级为下载（前端不许自造动作）", () => {
    const row = makeRow({ action: "copy", actions: ["download", "link"], manual: true, files: [okFile()] });
    expect(buildAcquireSubmitItems([row])[0].action).toBe("download");
  });
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/acquire-plan.test.ts`
预期：FAIL——`AcquireRow` 没有 `manual`。

- [ ] **步骤 3：编写实现**

`AcquireRow` 补：

```ts
  /** 这一行是用户手动关联的（规格 §7）：动作由用户在弹层里挑定，
   *  提交时带 manual 让服务端跳过内容校验。默认 false */
  manual: boolean;
```

`AcquireSubmitItem` 补 `manual?: true;`。

`buildAcquireSubmitItems` 的降级判据保持不变（`row.actions.includes(row.action) &&
f.actions.includes(row.action)`），但手动行的 `f.actions` 是 `["download"]`
（drift 为 different），会被误降。**手动行的判据只看 `row.actions`**：

```ts
        const supported = row.manual
          ? row.actions.includes(row.action)
          : row.actions.includes(row.action) && f.actions.includes(row.action);
        const action = supported ? row.action : "download";
        return {
          file: f.file,
          action,
          ...(action === "download" ? {} : { sourceHostPath: f.candidate!.hostPath }),
          ...(row.manual && action !== "download" ? { manual: true as const } : {}),
        };
```

`buildRows` 里新行一律 `manual: false`。手动关联产生的行由任务 15 单独构造。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run src/lib/acquire-plan.test.ts`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add src/lib/acquire-plan.ts src/lib/acquire-plan.test.ts
git commit -m "feat(version-drift): 提交项携带手动关联标记"
```

---

## 任务 14：档案页三处表现

**文件：**
- 修改：`src/app/(panel)/models/repos/[id]/repo-detail-view.tsx`、`src/app/(panel)/models/repos/[id]/acquire-dialog.tsx`、`src/i18n/messages/{zh,en}.json`

- [ ] **步骤 1：档案目录内的行**

`hasUpdate` 为 true 的行在「已下载」徽标旁加一个「有更新」徽标（`variant="outline"`，
琥珀色，与既有 `orphanBadge` 同款配色）；`unverified` 为 true 的行加一个弱化的
「未校验」徽标，点击触发 `POST /api/v1/file-meta/checksum`（202 后 toast 提示
「正在后台校验，算完刷新可见」）。两者互斥，有更新优先。

新增文案键（`pages.repos.detail` 下，与既有键同级；zh/en 同步）：

```
driftUpdateBadge      有更新 / Update available
driftUpdateTooltip    本机这份不是远端当前版本 / This copy is not the current remote revision
driftUnverifiedBadge  未校验 / Unverified
driftUnverifiedTooltip 拿不到校验值，无法判断是否为当前版本；点击后台补算 / No checksum available…
driftChecksumStarted  正在后台校验，算完刷新可见 / Verifying in the background…
```

- [ ] **步骤 2：档案目录外的「版本不符」**

散落文件行（`strayRels` 那一路）当对应 `drift === "different"` 时，把原先什么都不显示
改为一条说明：

```
driftStrayMismatch  本机有同名文件，但与远端当前版本不符（{delta}） / A file with the same name exists locally but does not match the current remote revision ({delta})
```

`{delta}` 由 `formatSize` 算差值并带符号，形如「大 347.3 MiB」/「小 1.2 GiB」。
差值计算下沉 `src/lib/format.ts` 旁边的新函数或直接复用 `formatSize`——
**判定不下沉**（这只是格式化），但要在 `src/lib/repo-files-view.ts` 里把
`localSize`/`remoteSize` 带到行上供组件取用。

- [ ] **步骤 3：弹层的 version-drift 限制文案**

`acquire-dialog.tsx` 的 `restrictionText` 补一支：

```
restrictionVersionDrift  本机这份不是远端当前版本，只能下载；确知它可用请走「手动关联」 / …
```

动作下拉的标签补 `move-with-refs`：

```
actionMoveWithRefs      移动并更新配置 / Move and update configs
actionMoveWithRefsHint  搬到档案目录，并把引用它的模型配置一起改指到新路径 / …
```

- [ ] **步骤 4：验证**

运行：`npx tsc --noEmit`、`pnpm run lint`、`pnpm run build`
i18n 对称性用脚本核：zh/en 键集合与顺序完全一致。

- [ ] **步骤 5：Commit**

```bash
git add src/app/\(panel\)/models/repos/\[id\]/ src/i18n/messages/
git commit -m "feat(version-drift): 档案页展示版本关系与新动作"
```

---

## 任务 15：更新到最新版

**文件：**
- 修改：`src/app/(panel)/models/repos/[id]/repo-detail-view.tsx`、`src/i18n/messages/{zh,en}.json`
- 修改：`src/app/api/v1/repos/[id]/files/route.ts`（下发运行中模型引用的文件集合）

- [ ] **步骤 1：服务端下发占用信息**

`files` 路由响应增：

```ts
  /** 当前运行中的模型所引用的文件（models 根相对路径）。空数组表示没有模型在跑。
   *  「更新到最新版」要靠它决定禁用——llama.cpp 是 mmap 读的，就地覆盖会让正在
   *  跑的推理读到半新半旧的字节 */
  lockedRels: string[];
```

运行中模型取自现有的运行时单例（grep `runningModel` 找到既有取法，`planFileMove`
的第三参就是它，照抄同一条取数），把它的 `gguf_file` / `mmproj_file` 经
`resolveModelFiles` 展开成物理路径。

- [ ] **步骤 2：按钮**

`hasUpdate` 的行加「更新到最新版」按钮：

- `row.localRels` 与 `lockedRels` 有交集 → 禁用 + title 说明「该文件正被运行中的模型
  使用，请先停止模型」
- 否则点击弹确认框，列出引用该组文件的模型配置名（`row.models`），确认后
  `POST /api/v1/repos/:id/acquire`，items 为该组全部文件、action 一律 `download`

文案键：

```
actionUpdate            更新到最新版 / Update to latest
updateLockedTitle       该文件正被运行中的模型使用，请先停止模型 / …
updateConfirmTitle      重新下载并覆盖 / Re-download and overwrite
updateConfirmBody       将重新下载 {count} 个文件覆盖本机副本。引用它的模型配置：{models} / …
updateConfirmNoRefs     将重新下载 {count} 个文件覆盖本机副本。 / …
```

- [ ] **步骤 3：验证覆盖真的会发生**

在 `manager.test.ts` 补一条：

```ts
  it("目标已存在但大小不同 → 不被跳过，正常入队重下", async () => {
    writeFileSync(path.join(root, "hf/u/r/a.gguf"), "old-content-longer");
    const r = await manager.enqueueDownload({
      files: [{ file: "a.gguf", size: 5 }],
      targetDir: "hf/u/r",
      source: "hf",
      repo: "u/r",
      label: "u/r",
    });
    expect(r.skipped).toEqual([]);
    expect(r.taskIds).toHaveLength(1);
  });
```

运行：`npx vitest run src/server/download/manager.test.ts`
预期：PASS（`partitionExistingTargets` 的判据本就是「已存在**且 size 相符**才跳过」，
这条用例是把这个前提钉死，防止将来有人把它改成只判存在性）

- [ ] **步骤 4：Commit**

```bash
git add src/app/api/v1/repos/\[id\]/files/route.ts src/app/\(panel\)/models/repos/\[id\]/repo-detail-view.tsx src/server/download/manager.test.ts src/i18n/messages/
git commit -m "feat(version-drift): 更新到最新版，运行中占用时禁用"
```

---

## 任务 16：手动关联弹层与提交链路

**文件：**
- 修改：`src/lib/model-file-picker.ts`、`src/components/models/model-file-picker.tsx`
- 修改：`src/app/(panel)/models/repos/[id]/repo-detail-view.tsx`、`src/i18n/messages/{zh,en}.json`
- 测试：`src/lib/model-file-picker.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
  it("单文件模式：分片组不归并，每个物理文件各一项", () => {
    const items = buildPickerItems(
      [
        { rel: "loose/m-00001-of-00002.gguf", size: 10, mtime: 0, refs: 0 },
        { rel: "loose/m-00002-of-00002.gguf", size: 10, mtime: 0, refs: 0 },
      ],
      { mode: "file" },
    );
    expect(items).toHaveLength(2);
    expect(items.map((i) => i.value)).toEqual([
      "loose/m-00001-of-00002.gguf",
      "loose/m-00002-of-00002.gguf",
    ]);
  });

  it("默认（组模式）行为不变：分片归并成一项 glob", () => {
    const items = buildPickerItems([
      { rel: "loose/m-00001-of-00002.gguf", size: 10, mtime: 0, refs: 0 },
      { rel: "loose/m-00002-of-00002.gguf", size: 10, mtime: 0, refs: 0 },
    ]);
    expect(items).toHaveLength(1);
    expect(items[0].value).toBe("loose/m-*.gguf");
  });
```

第二条用例的 `value` 期望值先跑一次现有实现确认实际形态再写死，**不要凭记忆填**。

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run src/lib/model-file-picker.test.ts`
预期：FAIL——`buildPickerItems` 只接受一个参数。

- [ ] **步骤 3：实现单文件模式**

`buildPickerItems(files, opts?: { mode?: "group" | "file" })`，`mode: "file"` 时跳过分片归并，
每个物理文件一项（`shards: 1`、`shardTotalDeclared: null`）。默认 `"group"`，既有调用方不改。

`ModelFilePicker` 组件补两个可选 props（保持既有调用方零改动）：

```ts
  /** 弹层标题/说明的文案命名空间；缺省 common.filePicker（既有行为） */
  namespace?: string;
  /** 触发器渲染；缺省是既有那个「浏览」按钮 */
  trigger?: React.ReactNode;
```

`field` 保持必填——手动关联传 `"gguf"` 即可（它只影响排序，不阻断）。

- [ ] **步骤 4：接入档案页**

`drift === "different"` 的散落行与「未下载」行上给次要入口「手动关联本机文件」。
候选来自 scan 响应的 `unarchived`（`LocalCandidate[]`），转成 `PickerFile[]`
（`rel` / `size` / `mtime` / `refs`——`refs` 用 `candidate.referenced ? 1 : 0`）后
以 `mode: "file"` 构建 items。

选中后构造一行 `AcquireRow`：

```ts
{
  quant: row.quant,
  kind: row.kind,
  files: [{
    file: remoteFileName,
    candidate: picked,
    drift: "different",
    actions: ["download", "link", "move", "move-with-refs"],
    defaultAction: "link",
    restriction: "none",
  }],
  action: "link",
  actions: picked.referenced
    ? ["download", "link", "move-with-refs"]
    : ["download", "link", "move"],
  restriction: "none",
  phase: "idle",
  progress: null,
  error: null,
  canFallbackToDownload: false,
  manual: true,
}
```

并入既有 `acquireRows` 打开确认弹层——手动关联复用同一条提交与进度链路，不另起一套。

文案键：

```
actionManualLink        手动关联本机文件 / Link a local file manually
manualPickerTitle       选择本机文件 / Choose a local file
manualPickerHint        列出 models 内全部未归档文件。选中的文件会被搬到档案目录并改名为「{remote}」；面板不再校验内容是否与远端一致 / …
manualBadge             手动关联 / Manual
```

- [ ] **步骤 5：验证并 Commit**

运行：`npx tsc --noEmit`、`pnpm run lint`、`pnpm test`、`pnpm run build`

```bash
git add src/lib/model-file-picker.ts src/lib/model-file-picker.test.ts src/components/models/model-file-picker.tsx src/app/\(panel\)/models/repos/\[id\]/repo-detail-view.tsx src/i18n/messages/
git commit -m "feat(version-drift): 手动关联弹层与提交链路"
```

---

## 任务 17：审查报告的三处代码缺口

来源：`docs/_internal/audit/AUDIT-2026-09-04-本地权重迁移代码review.md`。
该报告另有两条已由控制者裁定**不修**（`CROSS_DEVICE` 中文前缀属 §14.3 已声明遗留；
无 oid 行用静态文案而非禁用下拉，语义等价），本任务不要顺手改它们。

**文件：**
- 删除：`src/app/api/v1/paths/mounts/route.ts`
- 修改：`src/app/(panel)/files/unclaimed-table.tsx`、`src/app/(panel)/downloads/downloads-view.tsx`
- 测试：`src/lib/downloads-view.test.ts`（若不存在则按下方说明下沉一个新的 lib 判定）

- [ ] **步骤 1：删掉死端点（Spec-3）**

`GET /api/v1/paths/mounts` 全库零调用方——前端消费的是 scan 响应里的 `availableMounts`
（`repo-detail-view.tsx:698`），该端点不在迁移设计 §8 的 API 表里。删掉整个 route 文件。
先 `grep -rn "paths/mounts" src` 复核确无调用方再删；`getDiscoveredMounts` 本身保留
（scan 路由在用）。

- [ ] **步骤 2：未登记视图补「扫描」按钮（Spec-2）**

迁移设计 §9.3 明写「『扫描』按钮在此重算」，实现只在移动成功后调了
`router.refresh()`（`unclaimed-table.tsx:114`），没有显式入口。在该表格头部加一个
「扫描」按钮，点击 `router.refresh()`（`/files` 是 force-dynamic SSR 页，refresh 即重算），
按钮在刷新期间显示 loading 并禁用（用 `useTransition` 的 `isPending`）。

文案键（`pages.files` 下，zh/en 同步）：

```
unclaimedRescan      扫描
unclaimedRescanHint  重新扫描 models 目录，刷新未登记文件清单
```

- [ ] **步骤 3：让逐文件的 source_path / local_action 有真实读者（Spec-4）**

`archiveIfBatchDone` 会把每个 local 任务的 `source_path` / `local_action` 写进
`download_history.files` 的 JSON（`manager.ts:470-477`），但历史行只读批级列
（`downloads-view.tsx:585-586` 的 `entry.sourcePath` / `entry.localAction`），
逐文件那两个键**无人读**——迁移设计 §14.1 却声称「UI 的角标与 title 两条路径都真的读了」。

给它一个真实读者：历史行文件名那一列的 `title` 现在是
`entry.files.map((f) => f.file).join(", ")`，改成逐文件带上来源与手段。
判定下沉 `src/lib/downloads-view.ts`（新建或并入既有同名 lib，先 grep 确认）：

```ts
/** 历史条目的逐文件说明（下载任务只有文件名；local 任务补「← 手段 源路径」）。
 *  批级的 source_path/local_action 只够说明「这批不是下来的、是挪来的」，
 *  混合动作批次里哪个文件用了哪种手段只有逐文件 JSON 说得清 */
export function describeHistoryFiles(
  files: readonly { file: string; source_path?: string | null; local_action?: string | null }[],
  label: (action: string) => string,
): string {
  return files
    .map((f) =>
      f.local_action ? `${f.file} ← ${label(f.local_action)} ${f.source_path ?? ""}`.trimEnd() : f.file,
    )
    .join("\n");
}
```

用例三条：纯下载批次（原样只有文件名、无箭头）、纯 local 批次（每行带手段与源路径）、
混合批次（两种行并存且顺序不变）。

组件侧把 `title` 换成 `describeHistoryFiles(entry.files, actionLabel)`，
`actionLabel` 复用 `LocalActionBadge` 已有的那套动作文案，不新造一份。

- [ ] **步骤 4：验证**

运行：`npx tsc --noEmit`、`pnpm run lint`、`npx vitest run src/lib`
预期：全绿，新增用例通过

- [ ] **步骤 5：Commit**

```bash
git add -A src/app/api/v1/paths src/app/\(panel\)/files/unclaimed-table.tsx src/app/\(panel\)/downloads/downloads-view.tsx src/lib/downloads-view.ts src/lib/downloads-view.test.ts src/i18n/messages/
git commit -m "fix(version-drift): 审查报告三处缺口——删死端点、补扫描按钮、逐文件归档信息接上读者"
```

---

## 任务 18：审查报告的两处文档不实

**文件：**
- 修改：`docs/superpowers/specs/2026-09-03-本地权重迁移-design.md`（§14.1 与 §14.3）

- [ ] **步骤 1：改正 §14.1 的 `download_history` 那一行（Spec-4）**

现在写的是「逐文件真值放在 `files` JSON 的两个新可选键里，UI 的角标与 title 两条路径都真的读了」。
这句话在任务 17 步骤 3 之前是**不实的**（角标与 title 读的都是批级列，逐文件键无人读）。
任务 17 做完之后改写成准确的表述：批级列供角标与「这批是挪来的」提示，
逐文件键供文件清单 title 展开每个文件的手段与来源。

- [ ] **步骤 2：§14.1 偏差表补一行 `settings/scan-dirs`（Standards-3 / Spec-a-1）**

迁移设计 §8 的 API 表列了 `GET/PUT /api/v1/settings/scan-dirs`，实现把写持久化折进了
`POST /repos/:id/scan` 的 `extraDirs` 参数（空数组即清除）。这个偏离本身是合理的
（少一个端点，且写入与扫描同一次请求天然原子），但只记在代码注释里、没进偏差表。
补一行，写清「设计原文 / 实际做法与理由」。

- [ ] **步骤 3：§14.3 遗留清单补一条 `scan_extra_dirs` 的备份面（Standards-4）**

`scan_extra_dirs` 存在 SQLite `settings` 表，而 `buildExportYaml` 导出的是
defaults / models / namespaces / repos / presets 五段，不含它；`panel.yaml` 也不含。
即 panel.db 丢失时这份配置静默消失。

**控制者裁定：不修**，理由一并写进遗留条目——panel.db 就在 `data/` 目录里、与 models 同级，
整个目录可直接拷走，YAML 快照是「可 git 化的文本备份」而非唯一备份面；而自定义扫描目录
只是一两条路径，重填成本近乎零，为它加导出段要连带改导入侧与 schema，不划算。
将来若该配置膨胀（比如加了每目录的策略），再一并纳入导出。

- [ ] **步骤 4：Commit**

```bash
git add docs/superpowers/specs/2026-09-03-本地权重迁移-design.md
git commit -m "docs(version-drift): 改正迁移设计 §14 的两处不实表述并补两条遗留"
```

---

## 收尾

- [ ] 四件套全绿：`npx tsc --noEmit`（0）、`pnpm run lint`（无输出）、`pnpm test`（≥2700 且只增不减）、`pnpm run build`（成功）
- [ ] i18n 对称：zh/en 键集合与顺序完全一致
- [ ] 规格 §12 的八条验收要点逐条对照，把无法在开发机验证的（涉及 17 GB 真文件的那几条）明确标出留给真机
- [ ] 把实施补记回写规格 §13、把变更记录追加到 `docs/_internal/TASKS.md`

---

## 规格补充（计划阶段发现，需实施时留意）

1. **`move-with-refs` 不支持 glob 引用**（任务 11）。规格 §6.2 没说这个边界。分片 glob
   引用改指单个新路径会毁掉组内其余分片的解析，故一律拒绝并引导用户走「归位」。
   这条要在 §6.2 回写时补进规格。
2. **手动关联项的 `size` 必须用实测值**（任务 12）。规格 §8 只说放宽内容校验，但执行器
   的大小校验用的是入队时声明的 `expectedSize`；若沿用 `rf.size`（远端声明），
   手动关联必然因大小不符而失败——放宽内容校验的同时必须把 size 也换成实测值。
3. **手动关联的判据是「local 任务且入队时 sha256 为 NULL」**（任务 11）。这条推导依赖
   「常规 local 任务的 oid 由 acquire 路由强制非空」这个不变量。实施时在 acquire 路由
   那一侧加注释锁住它，将来若有人放宽了那条，这里的推导会静默失效。
