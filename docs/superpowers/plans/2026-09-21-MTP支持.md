# MTP 支持 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让用户能在面板里开启 MTP 投机解码、关联 MTP 加速权重，并且面板能自己分清「内嵌型主模型 / sidecar 挂件 / 不含 MTP」三类权重。

**架构：** 判据层读 GGUF 元数据（张量数 + `nextn_predict_layers`）得出三类判定，下沉为 `lib/mtp-kind.ts` 纯函数；配置层照 `mmproj_file` 的既有全链路复制一份 `draft_file`（schema → 参数生成 → 容器路径 → 引用一致性）；UI 层在编辑页开一节 MTP，开关的可开与否由判据驱动。

**技术栈：** TypeScript、zod（配置校验）、better-sqlite3（元数据缓存）、Next.js App Router（server 组件下发判据）、vitest（node 环境，纯逻辑测试）。

**设计文档：** `docs/superpowers/specs/2026-09-21-MTP支持-design.md`
**调研底稿：** `docs/_internal/research/06-MTP模型支持与权重关联调研.md`（含 2026-09-21 实测补记二）

---

## 两处对设计文档的修正

实现前先明确，计划以本节为准：

**1. `gguf_meta` 必须走 DROP + CREATE 迁移，设计 §7 说的「不写迁移」是错的。**

该表是显式列（`ggufMeta.ts:99` 的 SELECT 列清单），加字段绕不开 schema。而且不能用 `ALTER TABLE ADD COLUMN`——v12 给该表加 `chat_template` 时已经踩过并写明理由：存量行新列补成 NULL，NULL 天生歧义（分不清「这个 GGUF 确实没有」还是「旧版本没采这一列」），老部署升级后已缓存的模型会永远判错。照 v12 的手法 DROP + CREATE，纯缓存重建零风险。

**2. 判据分两层，设计 §2 只写了本地那一层。**

- **本地已下载的文件** → 读 GGUF 元数据，权威判定（任务 1 的 `mtp-kind.ts`）。建配置候选（`BatchCandidate` 注释写明「已下载完整」）走这条。
- **远端未下载的文件** → 读不到元数据，只能按文件名/目录名预筛，**仅用于「建议勾选」，不做硬判定**。下载向导走这条。

远端也能读元数据（HF 支持 Range，取前 2MB 即可，已实测与本地逐字段一致），但那属下一期，本期不做。

---

## 文件结构

**新建**

| 文件 | 职责 |
|---|---|
| `src/lib/mtp-kind.ts` + `.test.ts` | 由 GgufMeta 判定 `none` / `embedded` / `sidecar` 的纯函数 |

**修改**

| 文件 | 改动 |
|---|---|
| `src/core/gguf.ts` | `GgufMeta` 加 `tensorCount` / `nextnPredictLayers`；保留原本丢弃的 tensor_count |
| `src/core/gguf.testkit.ts` | `buildGguf` 的 tensor_count 当前写死 0，加 opts |
| `src/server/migrations.ts` | v19：DROP + CREATE `gguf_meta`，补两列 |
| `src/server/ggufMeta.ts` | CacheRow / SELECT / INSERT / toMeta 各加两列 |
| `src/core/schemas.ts` | `spec_type`、`spec_draft_n_max`、`draft_file` |
| `src/core/args.ts` | `--spec-type` / `--spec-draft-n-max` / `-md` |
| `src/server/runtime.ts` | `ResolvedModelPaths.draftRel` |
| `src/core/images.ts` | `{{draft_path}}` 占位符 |
| `src/server/fileMeta.ts` | 引用检查两处 |
| `src/server/namespaces.ts` | 路径重写两处 |
| `src/server/repoProfiles.ts` | 档案移动引用重写 |
| `src/lib/model-form.ts` | 草稿字段映射四处 |
| `src/app/(panel)/models/[name]/edit/edit-form.tsx` | 提交时的 null 处理 |
| `src/components/models/model-params-form.tsx` | 新增 MTP 节 |
| `src/lib/batch-create.ts` | 候选排除 sidecar |
| `src/components/models/batch-create-form.tsx` | 配套文件勾选 |
| `src/i18n/messages/{zh,en}.json` | MTP 节文案 |

---

### 任务 1：GGUF 判据层

交付物：给定一个 GGUF 文件，面板能判定它属于三类中的哪一类，判定结果进缓存。

**文件：**
- 修改：`src/core/gguf.ts`、`src/core/gguf.testkit.ts`、`src/server/migrations.ts`、`src/server/ggufMeta.ts`
- 创建：`src/lib/mtp-kind.ts`、`src/lib/mtp-kind.test.ts`
- 测试：`src/core/gguf.test.ts`、`src/server/db.test.ts`

- [ ] **步骤 1：写 mtp-kind 的失败测试**

创建 `src/lib/mtp-kind.test.ts`。夹具数值取自 2026-09-21 四个真实权重的实测值：

```ts
import { describe, expect, it } from "vitest";

import { resolveMtpKind } from "./mtp-kind";

/** 造一个只含判定所需两字段的最小 meta */
const meta = (tensorCount: number | null, nextn: number | null, blockCount: number | null = 65) =>
  ({ tensorCount, nextnPredictLayers: nextn, blockCount }) as Parameters<typeof resolveMtpKind>[0];

describe("resolveMtpKind", () => {
  it("没有 nextn 键 → none（Qwen3.8-27B-Heretic 实测：851 张量 / 64 层 / 无该键）", () => {
    expect(resolveMtpKind(meta(851, null, 64))).toBe("none");
  });

  it("nextn 为 0 视同没有", () => {
    expect(resolveMtpKind(meta(851, 0, 64))).toBe("none");
  });

  it("张量数远少于层数所需 → sidecar（mtp-Qwen3.8-27B-Q4_0 实测：18 张量 / 65 层）", () => {
    expect(resolveMtpKind(meta(18, 1, 65))).toBe("sidecar");
  });

  it("张量数与层数匹配 → embedded（UD-Q4_K_XL 实测：866 张量 / 65 层）", () => {
    expect(resolveMtpKind(meta(866, 1, 65))).toBe("embedded");
  });

  it("另一个内嵌型样本：Native-MTP-Preserved 实测 753 张量 / 41 层", () => {
    expect(resolveMtpKind(meta(753, 1, 41))).toBe("embedded");
  });

  it("blockCount 缺失时退化为张量数绝对值判定", () => {
    expect(resolveMtpKind(meta(18, 1, null))).toBe("sidecar");
    expect(resolveMtpKind(meta(866, 1, null))).toBe("embedded");
  });

  it("blockCount 为 0 时不做除法（避免 Infinity），同样退化", () => {
    expect(resolveMtpKind(meta(18, 1, 0))).toBe("sidecar");
  });

  it("tensorCount 缺失时无法判定挂件与否，保守当 embedded——有 nextn 就允许开开关", () => {
    expect(resolveMtpKind(meta(null, 1, 65))).toBe("embedded");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/mtp-kind.test.ts`
预期：FAIL，模块不存在。

- [ ] **步骤 3：实现 mtp-kind.ts**

```ts
import type { GgufMeta } from "@/core/gguf";

/**
 * 权重的 MTP 形态判定（2026-09-21 设计 §2）。
 *
 * 只看 GGUF 元数据，不看文件名——实测证明文件名不可信：`Native-MTP-Preserved`
 * 名字带 MTP 却是主模型，`Qwen3.8-27B-UD-Q4_K_XL` 名字不带却内嵌了 MTP 头。
 *
 * 判据是**张量数**而不是 block_count：sidecar 的 block_count 与主模型一样是 65、
 * general.name 都同叫 Qwen3.8-27B，只有 18 个张量暴露了它装不下 65 层。
 */
export type MtpKind = "none" | "embedded" | "sidecar";

/** 张量数 / 层数低于此值即判为挂件——「连一层的张量量都不够」。
 *  实测两端相差两个数量级（sidecar 0.28，主模型 13～18），阈值取中间极宽松 */
const SIDECAR_RATIO = 2;

/** blockCount 不可用时的退路：任何完整模型的张量都远超此数 */
const SIDECAR_ABSOLUTE = 50;

type MtpFields = Pick<GgufMeta, "tensorCount" | "nextnPredictLayers" | "blockCount">;

export function resolveMtpKind(meta: MtpFields): MtpKind {
  const nextn = meta.nextnPredictLayers;
  if (nextn === null || nextn === 0) return "none";

  const tensors = meta.tensorCount;
  // 张量数缺失（旧缓存行或解析截断）时无从分辨挂件，保守判 embedded：
  // 有 nextn 就让用户能开开关，配错了由 llama.cpp 的加载期报错兜底，
  // 比直接禁掉一个其实能用的模型强
  if (tensors === null) return "embedded";

  const blocks = meta.blockCount;
  if (blocks === null || blocks <= 0) {
    return tensors < SIDECAR_ABSOLUTE ? "sidecar" : "embedded";
  }
  return tensors / blocks < SIDECAR_RATIO ? "sidecar" : "embedded";
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/mtp-kind.test.ts`
预期：PASS（8 个用例）。此时 `GgufMeta` 还没有那两个字段，`tsc` 会报错——下一步解决。

- [ ] **步骤 5：扩 GGUF 解析器**

`src/core/gguf.ts` 三处改动：

① `GgufMeta` 接口加两个字段（贴着 `blockCount`）：

```ts
  /** 张量总数。判定 MTP sidecar 的依据——挂件只含 MTP 头，张量数比完整模型少两个数量级。
   *  头部固定位置就能读到，不需要解析张量表本身（那在 KV 段之后，本文件不解析） */
  tensorCount: number | null;
  /** `<arch>.nextn_predict_layers`：权重是否带 MTP 预测头。没有此键即不支持 MTP */
  nextnPredictLayers: number | null;
```

② `GGUF_INTEREST` 加一条（与既有 `.block_count` 同款的架构前缀后缀键）：

```ts
  /** MTP 预测头层数，见 lib/mtp-kind.ts */
  nextnPredictLayersSuffix: ".nextn_predict_layers",
```

③ 原本丢弃 tensor_count 的那一行（搜 `tensor_count：张量表在 KV 段之后`）改为保留：

```ts
  // tensor_count：张量表本身在 KV 段之后、本函数不解析，但这个计数在头部固定位置，
  // 是判定 MTP sidecar 的依据（见 lib/mtp-kind.ts），读出来留用
  const tensorCountBuf = await cursor.take(8);
  const tensorCount = tensorCountBuf.length >= 8 ? Number(tensorCountBuf.readBigUInt64LE(0)) : null;
```

并把 `tensorCount` 与解释出的 `nextnPredictLayers` 一并放进返回的 `GgufMeta`。`nextnPredictLayers` 的解释方式照抄 `blockCount` 那条（架构前缀 + 后缀拼键后从 `collected` 取值），取不到则 `null`。

- [ ] **步骤 6：扩测试构造器并补解析测试**

`src/core/gguf.testkit.ts` 的 `buildGguf` 现在把 tensor_count 写死成 `u64(0)`，加一个 opts：

```ts
export function buildGguf(
  kv: Array<[string, { t: number; v: unknown }]>,
  opts?: { version?: number; tensorCount?: number },
): Buffer {
```

对应那一行改成 `u64(opts?.tensorCount ?? 0)`。

`src/core/gguf.test.ts` 追加：

```ts
it("解析出 tensorCount 与 nextnPredictLayers（MTP 判定的两个依据）", async () => {
  const buf = buildGguf(
    [
      ["general.architecture", { t: 8, v: "qwen35" }],
      ["qwen35.block_count", { t: 4, v: 65 }],
      ["qwen35.nextn_predict_layers", { t: 4, v: 1 }],
    ],
    { tensorCount: 18 },
  );
  const meta = await parseGguf(bufferReader(buf));
  expect(meta.tensorCount).toBe(18);
  expect(meta.nextnPredictLayers).toBe(1);
});

it("没有 nextn 键时该字段为 null，不影响其余解析", async () => {
  const buf = buildGguf(
    [
      ["general.architecture", { t: 8, v: "qwen35" }],
      ["qwen35.block_count", { t: 4, v: 64 }],
    ],
    { tensorCount: 851 },
  );
  const meta = await parseGguf(bufferReader(buf));
  expect(meta.nextnPredictLayers).toBeNull();
  expect(meta.blockCount).toBe(64);
});
```

- [ ] **步骤 7：迁移 v19**

`src/server/migrations.ts` 末尾追加。**照 v12 的 DROP + CREATE 手法，不要用 ALTER TABLE ADD COLUMN**：

```
  // v19：gguf_meta 补 tensor_count / nextn_predict_layers 两列（MTP 形态判定，
  // 见 lib/mtp-kind.ts）。与 v12 加 chat_template 同一条理由，不用 ALTER TABLE：
  // 存量行新列会补成 NULL，而 NULL 在这里天生歧义——分不清是「这个 GGUF 确实
  // 不含 MTP」还是「旧版本压根没采这一列」。前者该判 none（开关置灰），后者该
  // 重新解析，混在一起会让老部署升级后已缓存的内嵌型模型永远开不了 MTP。
  // gguf_meta 是纯缓存（v8 file_meta 注释写明的语义），DROP 重建零风险。
  `
DROP TABLE IF EXISTS gguf_meta;
CREATE TABLE gguf_meta(
  path TEXT PRIMARY KEY,
  size INTEGER NOT NULL,
  mtime INTEGER NOT NULL,
  arch TEXT,
  block_count INTEGER,
  context_length INTEGER,
  file_type INTEGER,
  chat_template TEXT,
  tensor_count INTEGER,
  nextn_predict_layers INTEGER,
  parsed_at INTEGER NOT NULL
);
`,
```

- [ ] **步骤 8：缓存读写带上新列**

`src/server/ggufMeta.ts` 四处：`CacheRow` 类型加两个字段、SELECT 列清单加 `tensor_count, nextn_predict_layers`、INSERT 的列与 VALUES 各加两项、`toMeta` 的映射加两项（DB 的 `NULL` → `null`）。

- [ ] **步骤 9：改迁移版本断言**

`src/server/db.test.ts` 全文 **11 处** `toBe(18)` 改 `toBe(19)`，文件头第 10 行注释里的「（当前为 18）」同步改 19。

「全新库迁移后 gguf_meta 列齐」若已有同类用例则补两列；没有的话新增：

```ts
it("migration v19：gguf_meta 补 tensor_count 与 nextn_predict_layers", () => {
  const db = openDb(":memory:");
  runMigrations(db);
  const cols = (db.prepare("PRAGMA table_info(gguf_meta)").all() as { name: string }[]).map((r) => r.name);
  expect(cols).toContain("tensor_count");
  expect(cols).toContain("nextn_predict_layers");
});
```

- [ ] **步骤 10：验证**

```bash
pnpm exec vitest run src/lib/mtp-kind.test.ts src/core/gguf.test.ts src/server/db.test.ts src/server/ggufMeta.test.ts
pnpm exec tsc --noEmit
pnpm run lint
pnpm test
```
预期：全部 PASS / 无输出。

- [ ] **步骤 11：Commit**

```bash
git add src/core/gguf.ts src/core/gguf.testkit.ts src/core/gguf.test.ts src/lib/mtp-kind.ts src/lib/mtp-kind.test.ts src/server/migrations.ts src/server/ggufMeta.ts src/server/db.test.ts
git commit -m "feat(mtp): GGUF 元数据判定权重的 MTP 形态"
```

---

### 任务 2：配置 Schema 与启动参数

交付物：配置里能存 MTP 三项，启动容器时能正确生成 llama.cpp 参数。

**文件：**
- 修改：`src/core/schemas.ts`、`src/core/args.ts`、`src/server/runtime.ts`、`src/core/images.ts`
- 测试：`src/core/args.test.ts`

- [ ] **步骤 1：编写失败的测试**

`src/core/args.test.ts` 追加。该文件已有 `build(over)` 辅助（`:48`，把覆盖项并进 `buildArgs` 的入参对象）与 `valueOf(args, flag)`（`:53`，取 `--flag value` 的 value，flag 不存在则抛），直接用：

```ts
describe("buildArgs：MTP / 投机解码", () => {
  it("spec_type 为 none 时整块不下发", () => {
    const args = build({ server: { ...server, spec_type: "none", spec_draft_n_max: 2 } });
    expect(args).not.toContain("--spec-type");
    expect(args).not.toContain("--spec-draft-n-max");
  });

  it("spec_type 为 draft-mtp 时下发类型，并恒传 n-max（最优值依硬件，不能靠上游默认）", () => {
    const args = build({ server: { ...server, spec_type: "draft-mtp", spec_draft_n_max: 2 } });
    expect(valueOf(args, "--spec-type")).toBe("draft-mtp");
    expect(valueOf(args, "--spec-draft-n-max")).toBe("2");
  });

  it("开关打开且有 draftPath 时下发 -md", () => {
    const args = build({
      server: { ...server, spec_type: "draft-mtp", spec_draft_n_max: 2 },
      draftPath: "/models/main/MTP/mtp.gguf",
    });
    expect(valueOf(args, "-md")).toBe("/models/main/MTP/mtp.gguf");
  });

  it("有 draftPath 但开关是 none 时不下发 -md——配置里留着文件属暂时停用，偷偷传参会让「关掉」失效", () => {
    const args = build({
      server: { ...server, spec_type: "none", spec_draft_n_max: 2 },
      draftPath: "/models/main/MTP/mtp.gguf",
    });
    expect(args).not.toContain("-md");
  });
});
```

注意 `buildArgs` 的入参是**一个对象** `BuildArgsInput`（`args.ts:70`，含 `server` / `modelPath` / `mmprojPath` / `port` / `alias` / `gpu`），`draftPath` 作为新的可选字段加进这个接口，紧挨 `mmprojPath` 并照它的注释风格写一行说明。

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/core/args.test.ts`
预期：FAIL，`spec_type` 不是合法字段（zod/TS 报错）。

- [ ] **步骤 3：加 schema 字段**

`src/core/schemas.ts` 的 `serverConfigSchema`（约 :115）加两项：

```ts
  /** 投机解码类型。本期只两档：none 与 MTP——上游还有 draft-simple/eagle3 等，
   *  枚举留扩展位，但那些要处理跨仓库分片 draft 与 vocab 兼容校验，不在本期 */
  spec_type: z.enum(["none", "draft-mtp"]).default("none"),
  /** 一次起草的 token 数。默认取 2 而非上游的 3：实测 Qwen3.6-27B 卡 2、
   *  Gemma 4 卡 1，深了反而因回退变慢。最优值依硬件，面板给默认但不假装普适 */
  spec_draft_n_max: z.number().int().min(1).max(16).default(2),
```

`modelSchema` 里贴着 `mmproj_file`（约 :261）加：

```ts
  /** MTP 加速权重（sidecar）。字段名取 draft_file 而非 mtp_file：上游 -md 本就是
   *  所有 draft 型投机解码的公共入口，日后加 draft-simple 不必改 schema 与迁移 */
  draft_file: ggufPathSchema.optional(),
```

注意 `partialServerConfigSchema`（不带 default 的那份，见 `:110` 附近注释）若按字段逐条列出，也要同步加这两项。

- [ ] **步骤 4：生成启动参数**

`src/core/args.ts`，位置放在多卡切分那段（`if (server.split_mode !== undefined)` 附近）之后：

```ts
  // 投机解码：spec_type 为 none 时整块不下发，保持 llama.cpp 自身默认。
  // n-max 在开启时恒传——最优值强依赖硬件与量化配对，不能靠上游默认值。
  // -md 只在开关打开时下发：配置里留着 draft_file 但用户关了开关，属暂时停用，
  // 偷偷传参会让「关掉」这个动作失效。
  if (server.spec_type !== "none") {
    args.push("--spec-type", server.spec_type, "--spec-draft-n-max", String(server.spec_draft_n_max));
    if (draftPath !== undefined) args.push("-md", draftPath);
  }
```

- [ ] **步骤 5：容器内路径解析**

`src/server/runtime.ts`：`ResolvedModelPaths`（:62）加字段：

```ts
  /** draft/MTP 加速权重相对路径；模型未配置 draft_file 时为 undefined */
  draftRel?: string;
```

在解析 `mmproj_file` 的地方（:109 与 :636 两处）照同款补 `draft_file` 的解析与缺失报错，拼容器路径的地方（`${modelMount}/${rel}`）补 draft 一份。分片 draft 与 mmproj 同样传首分片。

`src/core/images.ts`（:96-103 的占位符表）加 `{{draft_path}}`。

- [ ] **步骤 6：运行测试验证通过**

运行：`pnpm exec vitest run src/core/args.test.ts`
预期：PASS。

- [ ] **步骤 7：验证并 Commit**

```bash
pnpm exec tsc --noEmit && pnpm run lint && pnpm test
git add src/core/schemas.ts src/core/args.ts src/core/args.test.ts src/core/images.ts src/server/runtime.ts
git commit -m "feat(mtp): 配置 schema 与 llama.cpp 启动参数"
```

---

### 任务 3：引用一致性

交付物：`draft_file` 与 `gguf_file`/`mmproj_file` 享有同等的引用保护——删被引用的文件会警告，移动命名空间/档案时路径会被重写。

**这批改动没有新逻辑，但漏一处就是数据一致性缺陷**：配置会静默指向不存在的文件，到启动才炸。

**文件：**
- 修改：`src/server/fileMeta.ts`、`src/server/namespaces.ts`、`src/server/repoProfiles.ts`、`src/lib/model-form.ts`、`src/app/(panel)/models/[name]/edit/edit-form.tsx`
- 测试：`src/server/fileMeta.test.ts`、`src/server/namespaces.test.ts`

- [ ] **步骤 1：编写失败的测试**

`src/server/fileMeta.test.ts` 追加。该文件已有 `world`（含 `db` / `root` / `repo`）、`touch(rel, content)`、`addModel({...})`、`upsertFileMeta(db, root, rel)`、`relinkFile(db, root, from, to)` 这套夹具，照 `:447` 那个 mmproj 用例的形态写：

```ts
it("draft_file 与 mmproj_file 同等参与重链：文件挪走后配置跟着更新", async () => {
  touch("main/mtp.gguf", "content");
  addModel({ name: "m1", gguf_file: "main/a.gguf", draft_file: "main/mtp.gguf" });
  await upsertFileMeta(world.db, world.root, "main/mtp.gguf");
  touch("lab/mtp.gguf", "content");

  relinkFile(world.db, world.root, "main/mtp.gguf", "lab/mtp.gguf");

  expect(world.repo.getModel("m1")?.draft_file).toBe("lab/mtp.gguf");
});

it("一个 sidecar 被多个模型共用时，重链后全部配置一起更新", async () => {
  touch("main/shared-mtp.gguf", "content");
  addModel({ name: "m1", gguf_file: "main/a.gguf", draft_file: "main/shared-mtp.gguf" });
  addModel({ name: "m2", gguf_file: "main/b.gguf", draft_file: "main/shared-mtp.gguf" });
  await upsertFileMeta(world.db, world.root, "main/shared-mtp.gguf");
  touch("lab/shared-mtp.gguf", "content");

  relinkFile(world.db, world.root, "main/shared-mtp.gguf", "lab/shared-mtp.gguf");

  expect(world.repo.getModel("m1")?.draft_file).toBe("lab/shared-mtp.gguf");
  expect(world.repo.getModel("m2")?.draft_file).toBe("lab/shared-mtp.gguf");
});
```

第二条对应 MTP 的真实用法：unsloth 一个 sidecar 服务全家族量化，被多个配置同时引用是常态。

`src/server/namespaces.test.ts` 的 `moveModelFiles` 路径重写也要一条同款用例。那个文件的 `World` 夹具搭建较重（`:49` 起，含 db/repo/runtime/service 四件套），此处不展开代码——**照 `:274` 起那个「gguf_file/mmproj_file 原样」的既有用例复制一份，把断言字段换成 `draft_file`**，形态不要自创。

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/server/fileMeta.test.ts`
预期：FAIL，`draft_file` 不在引用检查范围内。

- [ ] **步骤 3：逐处补齐**

| 位置 | 改动 |
|---|---|
| `fileMeta.ts:283` | `for (const field of ["gguf_file", "mmproj_file"] as const)` 数组加 `"draft_file"` |
| `fileMeta.ts:454` | `if (model.mmproj_file === path)` 旁边照同款加 `draft_file` 分支，`field` 值填 `"draft_file"` |
| `namespaces.ts:114` | 命名空间下全部模型的文件解析，加 `draft_file`（含 glob 展开） |
| `namespaces.ts:215` | `if (model.mmproj_file !== undefined)` 旁边加 `draft_file` 的路径前缀重写 |
| `repoProfiles.ts:397` | 档案移动的引用重写，按该处注释所述的「mmproj_file 引用重写是现成的」同款补 draft |
| `model-form.ts:51` | 字段映射表加 `draft_file: "draft"` |
| `model-form.ts:79` | 草稿类型加 `draft: string` |
| `model-form.ts:126` | 读入加 `draft: model.draft_file ?? ""` |
| `model-form.ts:227` | 写回加 `...(drafts.draft.trim() === "" ? {} : { draft_file: drafts.draft.trim() })` |
| `edit-form.tsx:152` | 提交体加 `draft_file: drafts.draft.trim() === "" ? null : drafts.draft.trim()` |

- [ ] **步骤 4：运行测试验证通过**

运行：`pnpm exec vitest run src/server/fileMeta.test.ts src/server/namespaces.test.ts`
预期：PASS。

- [ ] **步骤 5：自查漏网**

运行：`grep -rn "mmproj_file" src/server src/lib src/app --include="*.ts" --include="*.tsx" | grep -v "\.test\."`

逐行确认每一处要么已经补了 `draft_file`，要么有理由不补（例如 mmproj 专属的多模态逻辑）。把结论写进报告——这是本任务唯一能防住「漏一处」的手段。

- [ ] **步骤 6：验证并 Commit**

```bash
pnpm exec tsc --noEmit && pnpm run lint && pnpm test
git add src/server/fileMeta.ts src/server/namespaces.ts src/server/repoProfiles.ts src/lib/model-form.ts "src/app/(panel)/models/[name]/edit/edit-form.tsx" src/server/fileMeta.test.ts src/server/namespaces.test.ts
git commit -m "feat(mtp): draft_file 纳入引用检查与路径重写"
```

---

### 任务 4：编辑页 MTP 节

交付物：用户能在编辑页看到 MTP 节，开关的可开与否由权重判定驱动。

本任务无单测（vitest 是 node 环境、无 jsdom，组件测不了），靠 `tsc --noEmit` + eslint + 全量回归把关。

**文件：**
- 修改：`src/components/models/model-params-form.tsx`、`src/app/(panel)/models/[name]/edit/page.tsx`、`src/i18n/messages/{zh,en}.json`

- [ ] **步骤 1：server 侧下发判定**

编辑页的 `page.tsx` 已经为「思考强度」解析过一次 GGUF（`model-params-form.tsx:267` 的注释写明「page.tsx 用 chatTemplate 判定过一次」）。照那条现成路径，把 `resolveMtpKind(meta)` 的结果一并算好传给表单，**不在客户端解析 GGUF**。

新增 prop：

```ts
  /** 主 GGUF 的 MTP 形态（server 侧由 gguf_meta 判定）：决定 MTP 开关可开与否 */
  mtpKind: MtpKind;
```

- [ ] **步骤 2：渲染 MTP 节**

`model-params-form.tsx` 新增一节（与既有「配置」节并列，不并进去——MTP 有三个控件且需要说明文字）：

| 控件 | 行为 |
|---|---|
| MTP 开关（`spec_type`，none ↔ draft-mtp） | `embedded` 可开；`none` 置灰 + 文案 `mtpUnsupported`；`sidecar` 置灰 + 警告 `mtpIsSidecar` |
| 草稿深度（`spec_draft_n_max`） | 仅开关打开时可编辑，默认 2 |
| 加速权重（`draft_file`） | 可选，文件选择器；选了但开关没开时提示 `mtpDraftWithoutSwitch` |

`sidecar` 那一档是**配置错误**而非可用形态：sidecar 只有 18 个张量，当主模型必然跑不起来。它出现在主模型位只说明用户绕过了候选过滤（手填路径或旧配置），该报错而不是给开关。

- [ ] **步骤 3：文案**

文案放 `pages.modelEdit` 分组（`model-params-form.tsx:99,300` 两处 `useTranslations` 取的都是它）。zh：

```json
"mtpTitle": "MTP 加速",
"mtpHint": "开启后模型一次起草多个 token 再由主模型验证，能提速但会多占约 2GB 显存。",
"mtpSwitch": "启用 MTP",
"mtpUnsupported": "该权重不含 MTP 层，无法开启",
"mtpIsSidecar": "这是 MTP 加速权重，不能作为主模型使用",
"mtpNMax": "草稿深度",
"mtpNMaxHint": "一次起草的 token 数。最优值取决于硬件与量化组合，默认 2；调大不一定更快。",
"mtpDraftFile": "加速权重",
"mtpDraftFileHint": "可选。权重自带 MTP 层时留空即可。",
"mtpDraftWithoutSwitch": "已选加速权重但未开启 MTP，该文件不会生效"
```

en 对称：`"mtpTitle": "MTP acceleration"`、`"mtpHint": "Drafts several tokens at once for the main model to verify — faster, at roughly 2GB extra VRAM."`、`"mtpSwitch": "Enable MTP"`、`"mtpUnsupported": "This weight has no MTP layers"`、`"mtpIsSidecar": "This is an MTP draft weight and cannot be used as the main model"`、`"mtpNMax": "Draft depth"`、`"mtpNMaxHint": "Tokens drafted per step. The best value depends on your hardware and quantization; 2 by default, and larger is not always faster."`、`"mtpDraftFile": "Draft weight"`、`"mtpDraftFileHint": "Optional. Leave empty when the weight has MTP layers built in."`、`"mtpDraftWithoutSwitch": "A draft weight is selected but MTP is off, so it will not be used"`。

- [ ] **步骤 4：验证并 Commit**

```bash
pnpm exec tsc --noEmit && pnpm run lint && pnpm test
git add src/components/models/model-params-form.tsx "src/app/(panel)/models/[name]/edit/page.tsx" src/i18n/messages/zh.json src/i18n/messages/en.json
git commit -m "feat(mtp): 编辑页 MTP 节，开关由权重判定驱动"
```

---

### 任务 5：sidecar 防误选与下载向导配套

交付物：sidecar 不再出现在建配置候选里；下载含 MTP 子目录的仓库时能顺手勾选加速权重。

**两种判据不能混用**（见开头「对设计文档的修正」第 2 条）：

- 建配置候选面对的是**已下载的本地文件**（`BatchCandidate` 注释写明「已下载完整」），用任务 1 的元数据判据；
- 下载向导面对的是**远端清单**，读不到元数据，只能按 `MTP/` 目录段与 `mtp-` 前缀预筛，且**只用于建议勾选**，不做硬判定。

**文件：**
- 修改：`src/lib/batch-create.ts` + `.test.ts`、`src/components/models/batch-create-form.tsx`

- [ ] **步骤 1：编写失败的测试**

`src/lib/batch-create.test.ts` 追加。该文件已有 `makeRow(overrides)` 辅助（`:11`，产出一条默认的 present 模型行）与 `batchCreateCandidates(repoId, rows)`：

```ts
it("判定为 sidecar 的行不进候选——它是挂件，不是独立可创建的模型", () => {
  const rows = [
    makeRow(),
    makeRow({
      quant: "Q4_0",
      files: ["MTP/mtp-Qwen3.8-27B-Q4_0.gguf"],
      localRels: ["hf/o/r/MTP/mtp-Qwen3.8-27B-Q4_0.gguf"],
      mtpKind: "sidecar",
    }),
  ];
  const candidates = batchCreateCandidates("o/r-GGUF", rows);
  expect(candidates).toHaveLength(1);
  expect(candidates[0]!.quant).toBe("Q4_K_M");
});

it("mtpKind 为 embedded 的行照常入选——内嵌型是正经主模型", () => {
  const candidates = batchCreateCandidates("o/r-GGUF", [makeRow({ mtpKind: "embedded" })]);
  expect(candidates).toHaveLength(1);
});

it("archiveDraftFile 取出仓库里的 sidecar 路径", () => {
  const rows = [
    makeRow(),
    makeRow({
      quant: "Q4_0",
      files: ["MTP/mtp-Qwen3.8-27B-Q4_0.gguf"],
      localRels: ["hf/o/r/MTP/mtp-Qwen3.8-27B-Q4_0.gguf"],
      mtpKind: "sidecar",
    }),
  ];
  expect(archiveDraftFile(rows)).toBe("hf/o/r/MTP/mtp-Qwen3.8-27B-Q4_0.gguf");
});

it("仓库里没有 sidecar 时 archiveDraftFile 返回 null", () => {
  expect(archiveDraftFile([makeRow()])).toBeNull();
});
```

`makeRow` 的默认返回值里要补上 `mtpKind`（默认 `"none"`），否则 `RepoRow` 类型不完整。

- [ ] **步骤 2：运行测试验证失败**

运行：`pnpm exec vitest run src/lib/batch-create.test.ts`
预期：FAIL。

- [ ] **步骤 3：候选过滤与 draft 取值**

`batch-create.ts` 现有的候选过滤已经排除了 mmproj 分组（`:14-15` 的注释写明理由），让 sidecar 走同一条路——注释里补一句 MTP 的情形。新增 `archiveDraftFile(rows)`，与 `archiveMmprojFile`（`:56`）同构。

判定来源：`RepoRow`（`src/lib/repo-files-view.ts`）**已有 `kind: "model" | "mmproj"` 字段，但那是 `core/quant.ts:63` 按文件名前缀算的，不能用**——文件名不可信是本期的立论前提。新增独立字段：

```ts
  /** 权重的 MTP 形态（server 侧由 gguf_meta 判定，见 lib/mtp-kind.ts）。
   *  与按文件名判的 kind 是两回事：kind 管「这是不是 mmproj」，本字段管
   *  「这是不是 MTP 挂件」，后者只有读元数据才能确定 */
  mtpKind: MtpKind;
```

值在装配 `RepoRow` 的 server 侧填（用任务 1 的 `getGgufMeta` + `resolveMtpKind`，对 `localRels` 里的文件逐个算）。**纯函数层不做 IO**——`repo-files-view.ts` 与 `batch-create.ts` 只消费这个字段。远端未下载的行没有本地文件可读，填 `"none"`。

- [ ] **步骤 4：向导勾选**

`batch-create-form.tsx` 照 `attachMmproj` 同款（该文件 mmproj 相关共 13 处）加一份 draft 版本：状态、勾选框、提交时映射到 `draftFile`。文案复用任务 4 的 `mtpDraftFile`。

- [ ] **步骤 5：运行测试验证通过**

运行：`pnpm exec vitest run src/lib/batch-create.test.ts`
预期：PASS。

- [ ] **步骤 6：验证并 Commit**

```bash
pnpm exec tsc --noEmit && pnpm run lint && pnpm test
git add src/lib/batch-create.ts src/lib/batch-create.test.ts src/components/models/batch-create-form.tsx
git commit -m "feat(mtp): sidecar 不进建配置候选，下载向导支持勾选加速权重"
```

---

## 收尾

```bash
pnpm test
pnpm run lint
pnpm exec tsc --noEmit
HTTP_PROXY=http://10.22.33.1:20172 HTTPS_PROXY=http://10.22.33.1:20172 ./deploy/llamapad.sh build
```

`next build` 由 docker 构建覆盖（本地 shell 沙箱跑 `pnpm run build` 会被 SIGKILL，与机器内存无关）。

### 真机验收清单

本机现有三份可直接用的权重，覆盖三类判定：

| 权重 | 预期判定 | 预期界面 |
|---|---|---|
| `models/qwen3.8/Qwen3.8-27B-UD-Q4_K_XL.gguf` | embedded | MTP 开关可开 |
| `models/qwen3.8/Qwen3.8-27B-Heretic-Q4_K_M.gguf` | none | 开关置灰「不含 MTP 层」 |
| `…/unsloth/Qwen3.8-27B-GGUF/MTP/mtp-Qwen3.8-27B-Q4_0.gguf` | sidecar | 不出现在建配置候选里 |

1. 三份权重的判定与上表一致；
2. 给 `UD-Q4_K_XL` 开 MTP 并启动，容器日志出现 `common_speculative_init: adding speculative implementation 'draft-mtp'`——这是 MTP 真正激活的唯一确证，配置写对了但没激活的情况靠界面看不出来；
3. 用 Playground 发一轮对话，确认响应正常且比关掉 MTP 时快（或至少不更慢）；
4. 给同一个模型挂上 sidecar（`draft_file`）再启动，确认 `-md` 进了启动命令且能加载；
5. **mmproj + MTP 同用**（调研待验证项 #2，至今未闭环）：给一个带 mmproj 的模型开 MTP，确认视觉能力是否丢失。这是本期唯一的未知风险；
6. 关掉开关但保留 `draft_file`，确认启动命令里没有 `-md`；
7. 删除被 `draft_file` 引用的 sidecar 文件，确认面板给出引用警告而不是静默删除；
8. 把模型移到另一个命名空间，确认 `draft_file` 的路径跟着被重写、移动后仍能启动。
