# 更新日志

[English](CHANGELOG.md) | **中文**

本文件记录 llamapad 每个版本的变更，最新版本在最前面。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)；在 1.0 之前仍是预览版本，功能与配置格式可能调整。

## [0.2.1] - 2026-09-24

### 新增

- `GET /api/v1/runtime/status` 新增 `starting` 字段，列出启动或重启请求还没返回的模型，以及它们当前所处的阶段：`preparing`（校验、清理旧容器）、`pulling`（本地没有镜像，正在拉取）、`creating`（容器已创建，正在启动）。以前在拉镜像这几分钟里，容器还不存在，状态接口里看不到这个模型，客户端会以为没有模型在启动。详见 API 文档的 runtime/status 一节

## [0.2.0] - 2026-09-22

这个版本的重点是**多模型并行**：可以同时运行多个模型，推理中转按请求里的 `model` 字段分发。另外新增了 MTP 投机解码支持，以及带 HuggingFace 发现区的模型首页。

### 新增

- **多模型并行运行**
  - 启动一个模型不再停掉其他模型。容器名或端口与运行中的模型冲突、或宿主机端口已被占用时自动顺延，实际端口在列表和运行卡上都能看到
  - 默认模型：推理中转收到不带 `model` 的请求时发往它，可以在模型首页的运行区切换。默认模型只保存在面板进程内，面板重启后会从运行中的模型里重新选
  - 推理中转按请求体的 `model` 字段或查询参数 `?model=` 路由；请求的模型配置过但没在运行时返回 404；`GET /v1/models` 汇总全部运行中的模型
  - Chat 页可以选择与哪个运行中的模型对话；容器日志可以固定查看某个模型；状态栏显示默认模型和其余运行中的模型数量
  - 模型配置表单在端口与其他模型相同时给出提示（不拦截保存，启动时会自动顺延）
- **MTP 投机解码**
  - 按 GGUF 元数据判断权重是否带 MTP 层，把权重分成「内嵌 MTP 的主模型」「MTP 加速权重」「普通模型」三类；文件名只作为兜底参考
  - 编辑页新增「MTP 加速」一节：开关、草稿深度（默认 2）、关联加速权重（`draft_file`）
  - 档案页、文件选择器给权重标上 MTP 标签；加速权重不会出现在「创建配置」的候选里
  - 下载向导会建议勾选仓库里的加速权重；批量创建配置时可以逐行关联加速权重
  - `draft_file` 全链路跟随：导入导出、文件移动与重命名、命名空间移动、删除前的引用检查都会处理它
  - 开了 MTP、权重又不含 MTP 层且没关联加速权重时，启动前直接报错，不会先停掉正在运行的容器
- **模型首页**
  - `/models` 改为模型首页，展示正在运行的模型、最近更新的仓库，以及 HuggingFace 发现区
  - 发现区默认列出 HF 上的热门 GGUF 仓库，也可以就地搜索；卡片上可以直接进入下载，已经建过档案的仓库会标出来
  - 热门榜在面板进程内缓存 30 分钟（环境变量 `PANEL_HF_TRENDING_TTL_MINUTES` 可调），取数失败时回落到上一批缓存
- 档案页的权重列表支持按文件名搜索和按量化档位筛选
- 签发 API Token 时可以选择是否保存明文：保存后可以在列表里反复查看与复制；不保存则只在签发时显示一次（默认不保存）
- 新建下载弹层支持预填仓库并自动探测量化分组

### 变更

- 模型配置列表的地址从 `/models` 迁到 `/models/profiles`；二级导航两组对调，「档案」改名为「HuggingFace」
- 概览页的运行状态卡精简为摘要，停止、重启、设为默认等操作移到模型首页的运行区
- 模型列表去掉「切换」操作，运行中的行显示实际端口与服务入口；启动进度框按模型跟随状态与日志
- **内置默认上下文长度由 131072 降为 65536**。没有单独设置 `ctx_size` 的模型下次启动时会使用新的默认值；需要长上下文的模型请在配置里显式设置
- 推理中转对 JSON 请求一律按实际字节数读取请求体，上限 64MB，超过时返回 413（OpenAI 错误格式）。此前超过 4MB 或缺少 `content-length` 的请求会跳过读取，在多模型下可能被误发到默认模型
- 编辑页保存配置后用全局提示反馈结果，保存成功后返回配置列表
- 文件选择器改为双栏的多用途文件浏览器；启动弹层加宽，日志截断减少一半；新建向导的返回按钮按来源分流

### 修复

- 多模型下，第二个运行中的模型也受「运行中锁定」保护
- 运行记录按模型记账：悬空记录、异常退出检测与对账不再假设只有一个模型；多个模型同时运行期间的记录不写聚合指标，避免显存与速度数据互相污染
- 思考模式关闭时，模板档位之外的思考强度不再阻止保存和启动
- Chat 页参数栏按所选模型读取 llama-server 的实际参数，不再总是对照默认模型
- 删除失真的 `gpu_layers` 告警，改为以可卸载层数作为输入提示
- 弹层宽度被样式基类锁死在 384px
- 大数字计数（下载量等）进位到 1000k 时没有升到 M

## [0.1.0] - 2026-09-16

首个公开发布的预览版本。

### 新增

- **模型管理**：一键启动、停止、切换模型（Docker + GPU 加速），同一时刻运行一个模型
- **参数配置**：面板内表单编辑并展示合并后的最终参数；YAML 导入导出与自动快照；参数预设
- **命名空间**：自定义分组，跨空间共享 GGUF 文件，按引用安全删除
- **模型下载**：HuggingFace（官方与镜像站）和 URL 直链，断点续传、sha256 校验、代理可在面板里配置；输入仓库自动按量化分组，分片模型自动成组
- **README 与推荐参数**：解析 HF 仓库 README 中的推荐参数，可选用本地或外部模型做 AI 解析
- **新建向导与文件管理**：从选仓库、挑文件到保存配置一次完成；目录浏览，移动与重命名带引用检查
- **监控**：容器 CPU 与内存、llama.cpp 推理指标、GPU 显存与温度、宿主机磁盘与网络、实时日志、运行历史与启动前的显存预警
- **Playground**：面板自带对话页；`/llama-proxy/*` 提供推理接口中转，思考强度可按模型映射
- **鉴权与 API**：登录保护、API Token、REST API
- **中英双语界面**，面板内置文档中心
- **部署脚本** `llamapad.sh`：一条命令安装，提供交互式管理菜单与 `start`、`stop`、`status`、`logs`、`config`、`upgrade`、`doctor` 等命令

[0.2.1]: https://github.com/LanceLRQ/llamapad/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/LanceLRQ/llamapad/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/LanceLRQ/llamapad/releases/tag/v0.1.0
